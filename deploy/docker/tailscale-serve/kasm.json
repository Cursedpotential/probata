{
  "_byline": "Claude Code · Fable 5.1 · 2026-09-08 — TS_SERVE_CONFIG for the devbox's tailscale sidecar: https://kasm.tilapia-skilift.ts.net -> Kasm (self-signed 6901), plus Syncthing UI on :8384",
  "_note": "Claude Code · Sonnet 5 · 2026-09-08 -- SOURCE OF TRUTH ONLY, no longer live-mounted. A relative compose bind to this path does not survive Coolify's runtime app directory (no repo checkout there -- verified live, the bind became an empty directory). The live copy is now the `configs: kasm_serve_json` block in deploy/devbox.yaml; keep both in sync when editing.",
  "TCP": { "443": { "HTTPS": true }, "8384": { "HTTPS": true } },
  "Web": {
    "${TS_CERT_DOMAIN}:443":  { "Handlers": { "/": { "Proxy": "https+insecure://127.0.0.1:6901" } } },
    "${TS_CERT_DOMAIN}:8384": { "Handlers": { "/": { "Proxy": "http://127.0.0.1:8384" } } }
  }
}
