# TODO — 2026-09-10

> _Byline: Claude Code · Opus 5 (1M context) · 2026-09-10. Running owner-order ledger for today (global rule: every order lands here the moment it is given; status updated in the same turn it changes)._

## Owner orders — docstore to the cloud

| # | Order (owner, 2026-09-10) | Status |
|---|---|---|
| 1 | Push documentation + advanced tooling to the cloud; local stays for code indexing (code index may also go to cloud) | DONE — surreal-docs live, 490 docs / 11,448 chunks, 11/11 migration checks |
| 2 | Separate SurrealDB instance, NOT with the case store, own tailnet host/IP via tailscale serve, tag:docker | DONE — svc:surreal-docs 100.113.9.234, wss://surreal-docs.tilapia-skilift.ts.net |
| 3 | Always a bind-mount exchange folder on containers | DONE — /data/probata/exchange/surreal-docs -> /exchange; memory updated |
| 4 | Query output through DuckDB, compact, no noise; skill updated so everybody uses it | DONE — scripts/docstore/sq.py, plugin `query` skill, AGENTS.md section |
| 5 | Remove failed test data from the case store (export first) | DONE — quarantine export 50.5 MB, namespace removed, fct intact |
| 6 | Switch ingest to the cloud | DONE — .docstore/.env -> wss hostname; full ingest exit 0 |
| 7 | Commit and push non-deploy changes (explicit paths) | PENDING |
| 8 | Slash commands: /recall-doc, /recall-adr, /memory, /update-adr — installed | IN PROGRESS — recall.py built; commands + install pending |
| 9 | All processing happens inside the tool (caller sees only the compact result) | IN PROGRESS |
| 10 | Reranker | DONE — NIM llama-nemotron-rerank-vl-1b-v2 primary (D-158, ~260 ms), Voyage rerank-2.5 fallback. OPEN: NIM scores run low, so the commands' 0.3 'weak match' threshold must be recalibrated; recall latency up to 9.6 s needs a per-leg breakdown |
| 11 | Server-side cron job | IN PROGRESS — Coolify scheduled-tasks API confirmed; worker app pending |
| 12 | Full-text search, make it efficient | IN PROGRESS — chunk-level BM25, stopwords stripped, any-term fallback when AND finds < 3 docs (OR query is 4.4 s, per-term is fast) |
| 13 | HTTP API exposed for a front end | IN PROGRESS — DB-native DEFINE API rejected (500 + needs outbound net + keys in DB); API goes in the worker, keys in Coolify env |
| 14 | Graph of how docs interconnect, viewable in Surrealist | IN PROGRESS — 372 md links, 6,957 D-refs (309 docs), 4,387 ADR refs (313 docs) |
| 15 | Inventory: what else must be recalled; which tools to use or make | ANSWERED in chat 2026-09-10; tools list below |
| 16 | Use CocoIndex change detection ("the whole point of ccc"): only changed files re-index | IN PROGRESS — cron = git pull + incremental CocoIndex update, not full re-runs |
| 17 | Every re-index updates the cloud database | IN PROGRESS — flow_docs target is surreal-docs; server worker pending |
| 18 | Slash commands namespaced by plugin (/docstore:recall-doc etc.) | DONE — installed via repo marketplace `probata`, project scope; /docstore:recall-doc verified live |
| 19 | Make a plugin out of the NIM probe files (nim_streaming_recheck.py, nim_embed_rerank_recheck.py + results); check the existing plugin first, it may need tweaks; make it provider-agnostic via a variable, from a list we create; keep NIM separate | DONE — extended existing `llm-probes` (backup ~/.claude/backups/llm-probes-1.1.0-20260910) to 1.2.0: NIM rechecks packaged into nim-chat-probe / nim-embed-probe; new `provider-probe` skill driven by providers.json (`--provider` or `PROBE_PROVIDER`); broken `references/` paths fixed in 4 skills + README. Live-verified 2026-09-10: openrouter, ollama-cloud, groq, gemini, voyage OK; openai (no credits), mistral + cerebras (need billing) blocked at the account; anthropic/moonshot no key |

## Tools to make (from order 15)

1. Recall with server-side fusion + rerank
2. Decision extractor: DECISION_LOG table -> one record per D-number (fixes empty fn::current_decisions and mid-row chunking)
3. TODO extractor: MASTER-TODO items -> todo records (fixes empty fn::open_work)
4. Graph builder: links_to / cites / supersedes / mentions edges, deterministic
5. Sync worker on Coolify cron: ingest -> extractors -> graph -> index health
6. HTTP API for the front end
7. /memory command over memsearch

## Flags raised, not acted on

- ADR-0056 is `superseded` in the mapping CSV but AGENTS.md cites it as current — classification conflict, owner to rule.
- fn::docs_search adds raw BM25 to cosine similarity, so its vector leg never changes ranking.
- verify_docstore.py wrote probe records into canonical data (flipped 4 real handoffs); now refuses non-embedded targets.
- 81 docs/*.md are unmapped and never indexed: 71 under docs/private (gitignored; assumed intentionally excluded), 10 real gaps incl. today's handoffs and this TODO. Fix: path-rule auto-classification in flow_docs instead of SKIP.
- Owner option 2026-09-10: Cohere trial or Gemini free tier as rerank fallbacks (no Cohere key present; Gemini has no rerank endpoint).
