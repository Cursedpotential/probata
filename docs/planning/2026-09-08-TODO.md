# TODO — everything the owner ordered 2026-09-08 (one list, one status column)

> _Byline: Claude Code · Fable 5.1 · 2026-09-08 13:16 EDT. Living list; update in place, strike-through with date when done. Plan detail: `2026-09-08-cloud-services-phase-2-plan.md` (NOT yet approved). Inventory: `2026-09-08-contextforge-federation-inventory.md`._

## Done today
- ~~Devbox v2 plan written and approved (12:10)~~ → build running
- ~~opencode-server switched to `opencode web` (commit 4d2476b, auto-deployed 12:25)~~ → UI verification still pending
- ~~OpenList desktop storage → SMB path; old WebDAV storage disabled (kept); desktop `desktop-webdav` task disabled + processes stopped~~
- ~~OpenList `msalem` user (full permissions; OpenList allows one admin only)~~
- ~~Google Drive ×4 (matt.salemnet, matt.salem85, salemnma, caminstaller85) authorized with the owner's custom client → OpenList `/gdrive/<acct>` + rclone remotes `gd_*`; old `gd_net_rw` removed (backup kept)~~
- ~~OneDrive personal mattsalem85 → OpenList `/onedrive/mattsalem85` (online refresh API off)~~
- ~~Federation inventory doc (agent)~~
- ~~case-bible `cc_guard.py` hook removed by owner 13:21 (too strict); plugins reloaded; tools unblocked~~
- ~~Rules saved: owner `msalem` admin on every service · no curling non-doc sites · discuss before building · running TODO always current · **every turn = model router → sequential thinking → lists** (global CLAUDE.md, 13:17); `sequential-thinking` skill installed by owner 13:17, moved to `~/.agents/skills` (canonical), wired into the rule~~

## In flight
- [ ] **Devbox v2** — 15:40: FIVE builds failed (missing claude-rc.sh copy; surreal installer path ×2; `pinta` has no apt package on Noble → dropped, GIMP stays); 6th build (`sbutm5v1th9v2vyqcnudera1`) FAILED 17:3x at step 32/39 Homebrew (`su -` → dash). 17:45 Fable took over: Dockerfile fixed (HOME=/root during build — the Kasm base bakes HOME=/home/kasm-user, root cause of the surreal/rustup path failures; zstd for the ollama installer, `|| true` removed; Homebrew via `sudo -u kasm-user -H bash script`), steps being tested in a scratch container on the host BEFORE build 7. Agent idle, no uncommitted files. **2026-09-10 correction:** scratch test PASSED 09-08 17:43 (ollama installs; Homebrew 6.0.22); sidecar removed from devbox.yaml; both files STAGED, NOT committed; build 7 never started; running devbox is still the 09-08 morning image. Resume from `docs/handoffs/HANDOFF-2026-09-10-cloud-services-tailscale-devbox.md` Next steps. Running container is still the MORNING one (kasm-user, persist subset). Verification table runs after a green build. Then: remove its tailscale sidecar, add `svc:kasm` + `svc:syncthing` via host serve.
- [ ] **opencode web** — first deploy crash-looped (exit 126): Coolify does NOT check the repo out beside the rendered compose, so relative binds of repo files become empty dirs. Fixed 13:27 (inline entrypoint + Coolify `content:` binds for the tailscale serve configs of opencode-server AND openlist; devbox agent told to do the same for kasm.json). **opencode web VERIFIED 13:29:** 401 without auth, 200 + OpenCode HTML with the password, `/doc` 200. Tailscale hosts: Coolify's `content:` bind did NOT write the file either → serve configs moved to `/data/probata/config/tailscale-serve/*.json` (absolute binds, commit 7661f10). **VERIFIED 13:35: `https://opencode.tilapia-skilift.ts.net` → 401 (auth on), `https://files.tilapia-skilift.ts.net` → 200; tailnet shows clean names, no `-1` collisions.** Remaining risk: hostnames hard-coded in the serve files (no `${TS_CERT_DOMAIN}`) — a lost state dir would rename the node to `<name>-1` and split cert from host

## Queue (owner order of mention; plan phases in brackets)
1. [ ] **Filestash** UI over OpenList WebDAV [P1] — owner "doesn't love" OpenList's UI
2. [ ] **Observability stack** [P2]: Grafana (ONE server, ovh-files) + Loki + Mimir + Tempo + OTel Collector; **Coolify Log Drains (Custom) per server, "include in log drain" ticked on EVERY application** on both hosts; R2 bucket instead of MinIO; Portkey traces in
3. [ ] **Portkey** [P3]: keys self-hosted behind an nginx front, **all provider API keys moved** out of clients, per-consumer tokens (owner to confirm), `code` + `cheap` lanes; version already newest (1.15.2)
4. [ ] **ContextForge** [P4]: upgrade 1.0.4 → **1.0.10**, user auth (email) + m2m (API tokens/JWT), `msalem` admin, per-consumer tokens
5. [ ] **Federate our own tools** into ContextForge **virtual servers** grouped efficiently [P5]; **register every local MCP that can move** (inventory: 10 move / 8 stay / 6 drop; ContextForge is currently EMPTY — ADR-0046 drift); first fix `n8n-mcp` connect timeout
6. [ ] **Secure ingress for cloud MCP clients** [P6] — claude.ai + Case Bible/console: Cloudflare Tunnel + Access (two hands-on checks first)
7. [ ] **Claude via Agent SDK** (`claude setup-token`, runner script) + **Codex via OpenCode ChatGPT login** [P7]
8. [ ] **`msalem` admin retro-applied**: ContextForge, Coolify, n8n, pgAdmin, Syncthing GUI, Grafana (when built), Workbench/Authentik
9. [ ] **Tonight: monitoring + navigation dashboard** [P8] (links to everything above)
10. [ ] Newest versions everywhere — ContextForge is the only stale one found (12:35 check)
11. [ ] Coolify status drift: `opencode-server` showed exited / `openlist` showed restarting while containers were healthy or absent — re-check after the devbox lands
12. [ ] memsearch summarizer provider switch + project_review path fix (awaiting owner yes, from earlier today)
13. [ ] Portkey embed config: `nv-embed-v1` EOL → `nemotron-3-embed-1b` (re-embed implication)
14. [ ] Verify `claude --remote-control` flag exists for the devbox `/rc` launcher (agent reports)
15. [ ] **Relative-bind audit** (13:28): `deploy/compose.yaml` still has two repo-file binds (`./sql/bootstrap/schema_snapshot_20260907.sql` → postgres initdb, `./docker/graphiti/config.yaml`); check how that stack is deployed (Coolify vs host mirror) and convert to `content:`/absolute if Coolify-rendered. Devbox `kasm.json` handed to the devbox agent.
16. [x] ~~**coolify-write plugin update** (owner 13:27) — two gotcha rows in SKILL.md + `deploy_application` docstring: absolute host paths for mounted files (relative binds → empty dirs; Coolify `content:` bind did not write the file on 4.1.2 either); deploy verified only by the user-facing URL (13:33)~~
18. [x] **Replace the sidecars → Tailscale Services on the HOST tailscaled (owner 13:47 option 1)** — DONE 13:45–14:03: sidecars removed from opencode-server + openlist (commit 8cb06d1), machines `files`/`opencode` deleted, 16 Services registered via API, autoApprovers extended, hosts approved per service via `POST /services/{svc}/device/{id}/approved`, `tailscale serve --service` on both hosts. VERIFIED from the desktop: ovh-app all answer (contextforge 303, portkey 200, coolify-mcp 404, homepage 200, legal 200, legal-api 401, platform-api 401, platform-tools 200, desk 401); ovh-files ALL answer 14:05 (files 200, opencode 401, surreal 307, neo4j 200, temporal 200, weaviate 301, infisical 200, n8n 200, llm-probe 404, llm-probe-ui 307). **20 of 20 hostnames verified from the desktop.** Devbox (kasm, syncthing) after its deploy. Lesson: **register → advertise → approve host** — three separate steps; `serve --service` alone shows nothing in the admin panel.
    - OLD text: **Replace the sidecars** (owner 13:31–13:36, docs read: docker-params, machine-names, connect-docker-container, JIT): two candidates — (a) **Tailscale Services** (`TS_EXPERIMENTAL_SERVICE_AUTO_ADVERTISEMENT`; one node per host advertises `svc:files`, `svc:opencode`, `svc:kasm`, each its own MagicDNS name + cert; could even be the host's own tailscaled, zero containers) or (b) **TSDProxy** (one container per host, per-app labels). Owner decision pending. JIT access → P6 (bounded access windows for cloud/case-work).
17. [ ] Console cloud deploy + load content into surreal-case + register in ContextForge + repoint plugin `.mcp.json` (folds into P5)

## Parked / noted, not scheduled
- Docker AI Sandboxes **kits** — local restricted agent VM on the PC; Early Access; not for the VPS
- Docker **Model Runner** — only if a GPU box joins the tailnet
- Cloudreve — rejected (owns its objects; can't browse existing buckets)
- Collabora — rejected in favor of LibreOffice (in the devbox image)
- Desktop logs into the stack (Alloy on Windows) — dropped; Coolify drains cover the fleet

## Owner decisions still open
- **Aperture (Tailscale AI gateway) vs Portkey for P3** (owner sent the Aperture + app-connector docs 13:37): Aperture = identity-based gateway (tailnet login/tag, no per-consumer tokens), provider keys + routing + quotas + full request/response telemetry with retention + MCP/HTTP **connectors** with OAuth; docs for Claude Code / Codex / OpenCode. Beta; hosting model + pricing NOT stated in the docs fetched; provider keys live in the Aperture instance config. Portkey is canon (ADR-0042) — switching is a ruling. App connectors = fixed egress IP for SaaS allow-lists (not needed today).
- Approve the phase-2 plan (per phase is fine)
- P3: per-consumer Portkey tokens vs one shared token

## 22:48 — owner order: commit the docs restructuring and push
19. [ ] **Commit docs restructuring (owner 22:48, confirmed intentional 22:50: wiki removed, planning pruned, handoffs/ + awaiting-verification/ + COMPACT-SUMMARY/ created, NAMING.md edit, docs/private gitignored) and push to origin/main.** Stray browser exports (`docs/handoffs/docs.html`, `docs (2).html`, `HANDOFFS.md.html`, `awaiting-verification/pending-review.html`) left unstaged pending owner call.
