# DECISION LOG — Agno-MCP-Platform

> _Byline: Claude Code · Fable 5 · started 2026-07-09 (2026-07-10 entries: Claude Opus 4.8) ·
> Codex · GPT-5 · 2026-08-18 ADR-0059 source-clock, walk-lifecycle, and native-vector rulings_
> **Running, append-only design/decision log.** Every load-bearing decision lands here with
> date, lane, rationale, and status — a fast scan of "why is it this way?" without digging
> through chat. Complements (does not replace) the formal `docs/adr/` ADRs: when a decision is
> big/contested enough to need alternatives-considered and supersession, promote it to an ADR
> and link it here. **Append; strike (~~…~~) when reversed, don't delete.** Newest on top.

Lanes: **A** = restructure · **B** = ingestion/table redesign · **C** = infra/gateway. See
`docs/COORDINATION.md` for lane ownership and the live-status ledger.

---

## 2026-08-30

| # | Decision | Lane | Status | Rationale / notes |
|---|---|---|---|---|
| D-108 | **The Platform Workbench is browser-first React + Vite, served same-origin by its FastAPI API and deployed through the existing Coolify Workbench resource. TanStack Router owns browser routes; Storybook remains the Vite component workshop. A Tauri desktop host may be added after the browser product is complete, without changing PostgreSQL/custody authority or duplicating the separate Case Bible local-first sorter lane.** Next.js server/runtime machinery is retired from this client because the Workbench is a stateful browser application and does not need server components, server actions, SSR, or Next routing. This ruling does not claim that existing data tables have already migrated to Glide Data Grid; that migration proceeds one complete operational table at a time. | B/C | **owner-ruled, implemented, deployed, and live-verified 2026-08-30** | Owner: _"Just start with the browser mode on your side ... I want to preview what ... you're doing"_ and _"We can do a conversion ... or ... add-on to make your end a desktop app once we get it complete."_ The browser remains continuously previewable; the production container still exposes one same-origin FastAPI/Vite service, calls the existing storage/exec boundaries, and preserves PostgreSQL plus durable receipts as authority. Live receipt: commit `9437f4a586b1740227033f1354d6e392bc6ff5fb`, Coolify deployment `cqalhd223bkghikhr6qet1ao`, healthy stable routes and real intake data. Desktop filesystem, SQLite, and Tauri IPC stay future host adapters rather than browser dependencies. rel: ADR-0061, D-060, D-068, D-107. _Byline: Codex · GPT-5.6-Sol · 2026-08-30._ |

| D-109 | **ADR-0052 ruling Q6 / D-054(6) is AMENDED: `working.claim_candidate` is the narrated-assertion row, not the legal-document extractor. Three outputs were compressed into two names.** Q6 correctly removed `artifact_candidate` because *artifact* reads as a created work, then described `claim_candidate` as "MCL 722.23 factors, case/court/docket references, parties, motions, allegations, dates" — which is a **legal-document extractor**. Owner, 2026-08-29: _"A legal artifact or document extractor gets extracted from chats. It would be one of the things that gets extracted, along with the timelines and the analysis. It's another production, it's another work product. But it's not a claim. It's kind of the farthest thing from a claim. It's a fucking document."_ **Three distinct outputs:** (a) **claim** — a narrated assertion made in a chat; accumulates, never merged or rewritten; `working.claim_candidate`. (b) **entity** — dedup-merged; existing layers, untouched. (c) **created work** — a document, timeline, or analysis *produced* in a chat; a work product needing its own name; **deferred, not built.** Q6's merge semantics (entities merge, claims accumulate and are never rewritten) survive unchanged and are enforced by an append-only content trigger. **Also ruled 2026-08-29:** (1) **Review is pulled by the work, never pushed by the pipeline** — extraction output is `active` on write with `lifecycle` (`active`/`superseded`) and NO per-row approval gate; a `review_state DEFAULT 'pending'` on claims would queue tens of thousands of decisions in front of the single owner for material that cannot be filed, cited, or promoted. Human judgment goes to promotion, to resolved time anchors, to assertions about to be relied on, and to divergences blocking work in progress. `owner_disposition='unreviewed'` is a **resting state, not a backlog**. (2) **Assertions have exactly two generations** — gen 1 cites claims, gen 2 (synthesis) cites gen-1 assertions only, minimum two, enforced by composite FK; **there is no gen 3.** Owner: _"as long as it's not an analysis of an analysis, but the synthesis of the same first-generation analysis just based on a different iteration, we can maybe get a complete idea of everything."_ Synthesis **preserves variance** — divergent readings are recorded (`concurs`/`diverges`/`extends`), never averaged or resolved by edit. (3) **Ordering is captured as stated in `working.claim_temporal_edge`**, authored in PostgreSQL, projected nowhere yet; no `date_relative_to` free-text column, no `occurred_at`/`validity` on a claim. Decided on measured properties — ~10⁴ edges, 2–6 hop chains, and interval propagation rather than pattern matching as the hard part — not by deference to a prior ADR. (4) **No promotion columns anywhere in `sql/0052`**; AI chats are permanently context-only (D-082) and the phase boundary is structural. | B | **owner-ruled 2026-08-29/30; `sql/0052` authored, NOT applied (DB rebuild in progress)** | Implements the ADR-0053 §7 deferred "entity/claim/time/event candidates". Placement per owner: _"just land it next to it"_ — the pre-existing candidate layers (`analysis.extraction_candidate`, `analysis.entity_candidate`, `working.candidate_entity|fact|event`) are NOT reconciled, and raw landing tables are referenced, never altered. **Not yet implemented:** D-054(7) dead-letter + replay + alert and D-085 partial-bulk semantics — as written a constraint violation still aborts the run. **Genuinely open after searching `DECISION_LOG.md`, `PROJECT_CANON.md`, `docs/adr/`, the repo memory chain, and the `Agno-MCP-Platform` session-log slug:** `provenance_depth` (marking a claim quoted from a derived analysis document rather than the original chat), the `originates_with` split, and whether MCL 722.23 factors become an enumerated reference table. rel: **ADR-0062**, ADR-0052/D-054, ADR-0053, D-082, D-085, D-071, D-092, D-098; `docs/design/CLAIM-AND-ASSERTION-CANDIDATES-2026-08-29.md`, `docs/design/DUAL-GRAPH-IDENTITY-AND-WRITEBACK-2026-08-29.md`, `docs/design/GROUNDING-VS-UNAVAILABLE-SOURCES-2026-08-29.md`, `docs/reference/HASH-TAXONOMY-2026-08-29.md`, `sql/.agent-memory/0052_claim_and_assertion_candidates.sql.md`. _Byline: Claude · Opus 5 · 2026-08-30._ |

## 2026-08-29

| # | Decision | Lane | Status | Rationale / notes |
|---|---|---|---|---|
| D-107 | **AgentOS is retired completely; Agno survives only as a disabled bounded atomic-agent library adapter. Temporal owns durable workflow/run state, retries, timers, signals, long HITL waits, activity history, atomic-agent invocation state, and projection fan-out/reconciliation; n8n is the visual business/integration layer; ContextForge is the MCP gateway; Portkey is the sole model/embedding/reranking gateway; plain FastAPI is the platform HTTP host.** All nine current Agno agent definitions may be preserved, but none is mounted or callable until assigned an explicit Temporal task ID, reference-only input/output contract, case/horizon/authority scope, tool allowlist, and tested approval path. PostgreSQL remains canonical and emits immutable change references through transactional outbox/CDC; it retains independently queryable domain receipts and decisions, not a replacement copy of Agno operational sessions. **SurrealDB is manual-promotion-only until a separately governed automatic process is designed.** PostgreSQL changes may automatically drive Weaviate and the independent Semantica/Neo4j and SAT-RAG analysis paths, but change detection creates only a Surreal promotion candidate. Surreal changes only after the owner explicitly decides material is worthy of a governed walk, evidence/reference use, or another approved analytical purpose; Temporal executes and receipts that decision and MUST NOT infer it. An approved Surreal materialization may combine canonical PostgreSQL inputs with either Neo4j projection independently or both together: Semantica `evidence` and SAT-RAG `sat-temporal`. The receipt pins the PostgreSQL projection generation and every graph snapshot/version used so the Surreal result remains reproducible, provenance-complete, and strictly derived. | A/B/C | **owner-ruled 2026-08-29; implementation not started** | Owner: _"AgentOS dies"_; _"Temporal and n8n and ContextForge and Portkey handle most of that"_; _"the moment Surreal will be all manual ... It's going to be when I decided something gets promoted to worthy of a walk or evidence or reference or whatever"_; _"That might actually extract from both — well, either of Neo4j graphs — as well as PG."_ This supersedes D-101's undecided session/factory and analysis-only hold. It narrows D-093/D-095: Surreal remains the governed final analytical/walk engine, but is not presently an automatic projection sink. rel: D-101, D-096, D-095, D-093, D-068, D-006, ADR-0042, ADR-0052, ADR-0056. |
| D-106 | **Graph state, verified 2026-08-29. Neo4j/DozerDB is on `100.91.190.107` — the same host as PostgreSQL and Weaviate, as the owner said — not the `100.119.96.29` (`OVH3_HOST`) address the configs carry.** Every reference to `100.119.96.29` is a **retired-Graphiti artifact** (`data-graphiti.yaml`, `data-graphiti-case.yaml`) and that host does not answer; production `deploy/compose.yaml` correctly uses docker DNS `bolt://neo4j:7687`, so production is unaffected. Live databases before this change: `neo4j` (default, **627 nodes**, labels `Saga`/`Episodic`/`Entity`/`Community`), `memory` (**6,454 nodes**, same Graphiti labels plus `Person`/`Organization`/`Location`), `evidence` (**0 nodes**), `system`. **All 7,081 nodes in the graph belong to retired Graphiti (D-095). The Semantica graph `evidence` exists but is empty, and no SAT lane database existed at all.** Created **`sat-temporal`** per the owner's ruling that there must be at least two graph databases — one for Semantica, one for the temporal SAT-RAG lane. **`sat_temporal` as written in D-093 is an illegal Neo4j database name** — underscores are rejected; only ascii letters, numbers, dots and dashes are permitted — so the canonical name is **`sat-temporal`** and D-093's spelling must be corrected wherever it appears. **SECURITY:** the live instance authenticates with the compose file's documented fallback `graphiti-dev-password`, while `.env`'s `NEO4J_PASSWORD` is a different value that fails. Auth persists in the data volume, so `NEO4J_AUTH` no longer governs it. A default credential is protecting graph data in a custody matter and should be rotated. | B/C | **verified and partially actioned 2026-08-29; credential rotation outstanding** | Owner: _"There should be at least two Neo4j databases, one for semantica, one for our temporal SATrag."_ rel: D-093, D-095, D-104, ADR-0036. |
| D-105 | **`relationship_timeline` is its own lane and its own collection. It is NOT merged into `personal_history`.** This reverses the merge recorded in D-057. **Reason, owner 2026-08-29:** the relationship timeline contains events that are not part of the owner's personal history *because the other party had her own life* — things occurred inside the relationship that the owner did not witness, did not know at the time, and in some cases may never have known. Folding those into `personal_history` would assert the owner experienced or knew them, which is false and would corrupt the as-lived/hindsight boundary the platform is built on. Personal history is **what the owner lived**. The relationship timeline is **the relationship as a whole, including the other party's side**, which the owner may only learn later, through discovery, or never. Keep `RelationshipTimelineV1` as a distinct collection and a distinct lane. | B | **owner-ruled 2026-08-29; supersedes the D-057 merge** | This is a knowledge-boundary distinction, not a taxonomy preference, and it is the same distinction ADR-0045/ADR-0059 draw between occurrence and knowledge/realization: an event can exist on the relationship timeline with an `occurred_at` long before any date at which the owner could have known it. rel: **D-057** (merge reversed), ADR-0045, ADR-0059, D-103, D-065. |
| D-104 | **FINDING (not a ruling): the D-066 native-evidence Weaviate cutover was stood up and abandoned. Two identical Weaviate instances are running.** `deploy/data-weaviate.yaml` serves the old instance on REST 8081; `deploy/data-weaviate-native-v1.yaml` serves the "blue" instance on REST 8082 / gRPC 50052. Both are 1.38.7, both hold the same 7 collections and the same 3,035 objects. **The cutover never executed:** `Evidence_knowledge` is 0 in both, the D-066 target collection `EvidenceChunkV1` exists in neither, and the alias `EVIDENCE_VECTOR_ALIAS = "EvidenceChunks"` (`server/core/evidence_vector_store.py:23`) resolves to nothing. `exec.yaml` gates the whole thing behind `NATIVE_EVIDENCE_ENABLED`, documented as "set true only after native evidence canaries pass" — the canaries never passed. `DEFAULT_VECTORIZER_MODULE: none` is **correct by design** (the platform supplies self-provided vectors per D-066), not a defect. **Owner decision required: finish the cutover, or retire the blue instance.** Until then the platform pays for two full instances holding duplicate data, and `WEAVIATE_HTTP_PORT` defaults to `8082` — the instance that was never cut over to. | B/C | **discovered 2026-08-29; owner decision pending** | Also outstanding: the Agno Weaviate client in `server/core/knowledge_vectordb.py` must be replaced under D-101 (Agno has no role in the knowledge vector); `Relationship_timeline_knowledge` still exists as its own collection although D-057 merged relationship history into `personal_history`; and collection naming runs three conventions at once (lane-style `*_knowledge`, domain-style `Platform_*`, and D-066's `EvidenceChunkV1`). **Neo4j could not be verified** — `NEO4J_URI=bolt://100.119.96.29:7687` is a different host from the Postgres/Weaviate box and queries timed out; its state is unknown, not empty. rel: D-103, D-101, D-066, D-057. |
| D-103 | **The Weaviate collections span two different domains and must not be governed as one set. `Platform_code_knowledge` is development knowledge for development agents — out of scope for case evidence and case context.** Case-domain collections (`Legal_knowledge`, `Personal_history_knowledge`, `Relationship_timeline_knowledge`, `Evidence_knowledge`) carry custody and governance obligations. Platform-domain collections (`Platform_knowledge`, `Platform_code_knowledge`) do not — they exist to support building the system. `Platform_code_knowledge` is **empty (0 objects) and deferred**, retained against a possible future return to developing inside the framework; it is not dead and must not be dropped as unused. | B | **owner-ruled 2026-08-29** | Owner: _"This is development knowledge for development agents when I was intending on developing within the framework. Maybe that still happens down the road. But this is outside the scope of what's evidence or context in the case itself."_ **Open question this raises:** `Platform_context` holds **2,790 of the 3,035 total objects — 92% of the entire index** — and its name is ambiguous between the platform domain and the D-057 `context` case lane. Whether that collection is dev context or case context determines which governance applies to the overwhelming majority of vector data, and cannot be answered from the name. Resolve before any collection rename or the native-evidence cutover decision. rel: D-104, D-057, D-066. |
| D-102 | **`docs/pending-review/` is renamed `docs/awaiting-verification/` and carries its own `AGENTS.md` and `CLAUDE.md` stating in bold what it is.** The directory is a **verification queue, not an owner inbox**: every document in it claims completion, no claim has been checked, and **a different agent than the author** must verify the claim against the repository and the running system before it is promoted to the live docs or moved to repo-root `to_be_deleted/`. Nothing is ever deleted from it. Putting a document there does not close a task; it opens a review. | A | **owner-ruled 2026-08-29** | Owner: _"Pending review is for documents that were supposedly completed that need to be checked by a second person or agent to validate their completeness before being moved to to_be_deleted. It's kind of like purgatory. Because you all can't be trusted."_ The old name invited exactly the misreading it received: earlier the same day this agent described the directory to the owner as holding "real platform decisions" — i.e. an owner queue — which is wrong, and the owner had to correct it again. Rename executed with all 46 references across 14 documents repointed and zero stale links. rel: D-096. |
| D-101 | **Agno is no longer the knowledge home, the sole table writer, or the viewing/work surface. Agents survive only as task runners inside n8n and Temporal.** Settled sub-rulings: Agno gets **no role in the knowledge vector**; **providers and the model registry are not needed** (no chat routed through them); `server/core/session.py` and `server/agents/factory.py` are **undecided and possibly load-bearing**; `server/api/main.py` must be **explained to the owner before anything is proposed**. **Nothing may be removed, replaced, or refactored until the dissection in `HANDOFF-2026-08-29-agno-role-dissection.md` is complete and the owner rules on it** — including the option of doing nothing. | A/B | **owner-ruled 2026-08-29; dissection scoped, analysis not started** | Owner: _"Agno used to be the primary home of the knowledge bases… the sole writer of the tables… and this viewing surface and work surface. None of those things now."_ Verified inventory 2026-08-29: **28 production files plus 3 test files still import Agno**, across eight surfaces — and `agno.os.AgentOS` in `server/api/main.py` **is the application server**, not a library call, so every Agno decision is a decision about how the platform is served. Two standing conflicts surfaced by the scan and left for the dissection to adjudicate: `server/evidence/workflows.py` runs `agno.workflow` against D-068's Temporal-is-the-spine ruling, and `server/core/session.py` holds both the Postgres and SurrealDB handles against ADR-0056/D-061. `server/core/reranker.py` has zero callers. Nine agent modules exist where the owner expects "a couple." This packet exists specifically so the Graphiti failure (D-095 — a closed ruling that sat "pending" in the register for two days) does not repeat. rel: **D-095**, D-096, D-093, D-068, ADR-0056, ADR-0061. |
| D-100 | **Cleanup disposition, 2026-08-29: reversibility is judged by whether the content survives elsewhere, not by whether the operation can be undone in place.** Applying that test: the contaminated `main` history (`recovery/platform-contaminated-20260829`) was **deleted** — verified to hold zero unique content against the clean line, with its 145 MB Weaviate blob preserved outside git at `_preserved-from-main-20260829`; `.git` fell from 159 MB to 51 MB. Scratch databases `platform_build` and `platform_superseded_20260829` were **dropped**. Agno framework rows (`agno_schema_versions`, `agno_learnings`, and 3,516 telemetry rows) were **cleared**; `api_keys` and `agno_service_accounts` were **kept** as auth/config. **`ai_test_ingest` was snapshotted, identified, and then dropped on owner ruling.** | A/C | **owner-ruled 2026-08-29; all cleanup items resolved** | Owner: _"If the data lives somewhere else, if nothing is unique… is it truly irreversible? I would argue no. Plainly, is it fucking necessary? If it's not, fucking get rid of it."_ `ai_test_ingest` holds **445 SMS records with a complete custody chain unique to that database** — `evidence.raw_sms`, `evidence.acquisition`, `evidence.source`, `evidence.artifact_metadata`, `evidence.evidence_hash`, `evidence.ingest_run`, `working.normalized_record`, `working.event_source_record` — all **empty in `platform`**. Because this is a custody matter and the rows are evidence-shaped, it was snapshotted before any decision: `_db-snapshots/ai_test_ingest-20260829` (8 CSVs, 1,341 rows, SHA-256 per file). **Identification, which should have accompanied the first report and did not:** the contents are a **call log, not SMS** despite the `evidence.raw_sms` table name — source `calls-20251203231638.xml`, 139,868 bytes, from `C:\Users\matts\OneDrive\Case Bible\_SWEPT\Evidence\SMS Backup and Restore\`, exported from an Android handset via SMS Backup & Restore at 2025-12-04 04:16:38 UTC (backup set `009c4636-f98b-4ad3-b7ad-0886fccab25b`, 445 records claimed and 445 ingested), ingested 2026-08-02 08:34:52 UTC by parser `messages.sms-xml`. Its own acquisition row records `authority=unclear`, `device_custodian='household - chain not yet established'`, and `producible=False`. **Owner ruling on that evidence: drop it** — the source XML still exists, it was a deliberate test run against real rather than synthetic data, and the rows predate the current schemas so they carry none of the required fields. Dropped 2026-08-29. rel: D-098, D-094. |
| D-097 | **The four remaining `ai`→`platform` questions are closed. (a) Extensions: `platform` gets the same extension set as `ai` — owner: _"The same fucking extensions. I thought that was a given."_ Treat parity as the default for any future database provisioning question; do not re-ask. (b) Runtime grants become a forward migration, not a hand-run bootstrap script — grants live in their own numbered file, applied after a successful connection test so a bad grant fails loud instead of locking the runtime out. (c) The **app database (`platform`) must stay clean** — only what the application needs. Owner: _"I just need the app database fucking clean."_ (d) `ai` is parked read-only — never dropped — and only once the parity condition in D-098 is met.** | A/B/C | **owner-ruled 2026-08-29** | (a) and (b) close items 1 and 4 of the six. An earlier revision of this entry claimed reference/knowledge material should be written **into** the Case Bible database; that was wrong and is retracted — see **D-099**, which states the actual boundary. The consolidation packet's treatment of `casebible` as out of scope (`AI-TO-PLATFORM-CONSOLIDATION-2026-08-29.md:76,643`) is **correct** and must not be "fixed." rel: D-099, D-094, D-091, migration `0049`. |
| D-099 | **The Case Bible database is SOURCE-ONLY for this platform. The platform never writes to it.** Case Bible is an external index/catalog of where source material and historical/archived data can be found. The platform **may read from and import from it**. The platform **never writes, routes, consolidates, projects, or stores outputs back into it** — not context, not normalized records, not chunks, not candidates, not receipts, not workflow state, not evidence. Every platform-created artifact remains in the platform's own governed stores. Nothing that happens in this platform routes to Case Bible. | A/B/C | **owner-ruled 2026-08-29; corrects a repeated agent error** | Owner: _"Nothing that happens in this platform routes to case bible… The Case Bible Database is source. It is an index of where the fuck everything is that we can pull from. Nothing in this platform ever goes to that fucking database."_ **At least two separate agents inverted this**, reading the 2026-08-27 "bring everything into the Case Bible PG" instruction — which was about consolidating *legacy/source* material, not platform output — as a directive to route platform outputs there. Any document, packet, or plan that projects platform state into Case Bible is wrong on its face and must be corrected, not implemented. The Case Bible architecture guidance governs this read boundary only; the custody, lakehouse, sorting, forensics, and trusted-source skills do **not** apply, because the platform is not operating on Case Bible data. rel: D-097, D-094. |
| D-098 | **`ai` is parked read-only only after proven parity, and is never dropped.** The park precondition is a fact, not a date: (1) every in-scope object and row is copied to `platform` and reconciled by exact count and hash; (2) nothing is still connecting to `ai` — including `server/core/url.py`, `.env`, `deploy/compose.yaml`, `deploy/data-pg.yaml`, `deploy/exec.yaml`, and `scripts/railway/up.sh`, all of which still default to `ai`; (3) reference/knowledge material is landed in Case Bible PG per D-097(c); (4) a retained snapshot exists. Until all four hold, `ai` stays writable and untouched. **Park means read-only with the snapshot retained. It does not mean drop, and no agent may read any owner instruction as authorization to drop `ai`.** | A/B/C | **owner-ruled 2026-08-29; precondition NOT met — zero rows migrated as of this entry** | Owner asked _"Is all of the data properly migrated? Does the new database function as the old was supposed to? If so, get rid of it."_ — answered: **no.** Consolidation Phases 0–5 are all unstarted; migration `0049` is scaffolding only and is not even committed; `server/core/url.py:22` still reads `getenv("DB_DATABASE", "ai")`, so the running application is still on `ai`. The conditional was recorded; the precondition was not satisfied, so no park and no delete is authorized by it. Consistent with the standing no-delete/quarantine rule. rel: D-097, D-094, D-091. |
| D-096 | **No agent may close a task without recording its ruling in this log in the same change.** A decision that exists only in a chat transcript is not a decision the platform can act on. Any agent that receives an owner ruling — including an inline, partial, or shorthand one ("2 ok 3 sure 4 ?? k 5 ok") — appends it here before reporting the task complete, with the verbatim owner language preserved. Status registers (`MASTER-TODO`, `HANDOFFS.md`, `INDEX.md`, ADR consequence sections) must be corrected in the same change when the ruling invalidates them. | A | **owner-ruled 2026-08-29; standing rule** | Root cause of the 2026-08-29 reconciliation. Five of the six "pending owner decisions" in `awaiting-verification/AI-TO-PLATFORM-CONSOLIDATION-2026-08-29.md` had been answered on 2026-08-27 and were never written down, so each fresh agent re-derived them as open and re-asked. Owner, 2026-08-29 00:47: _"Then why the fuck did I have to order you to go through and use two different tools to reconcile?"_ and 00:51: _"When I give you a to-do, it's a to-do."_ Recovery required DuckDB queries over `~/.codex/sessions/` — session logs are not an acceptable system of record. rel: D-094, D-095. |
| D-095 | **Graphiti is retired and is not a dependency, a gate, or a baseline for anything. There is no bake-off, because there is no candidate and no replacement store.** PostgreSQL remains the belief/canonical authority. SurrealDB is the governed analytical/walk projection lane that brings the pieces together. Two temporally-aware RAG paths remain in service and depend on Graphiti for nothing. Memgraph is a named, repeatedly-discussed candidate for the graph tier — not selected, not scheduled, not blocking. **The agent-layer implementation of memory/graph is explicitly deferred by owner order; deferred is not blocked, and no lane may cite agent-memory work as its gate.** | B | **owner-ruled 2026-08-27, restated and broadened 2026-08-29** | The 2026-08-27 ruling ("Graphiti is retired, the target is zero callers, there is no replacement store, and no new ledger is authorized") was dispatched as P-09 / GAP-008 and implemented as the zero-caller path, but never propagated: `HANDOFFS.md` R4 still read "Research complete / implementation pending" and `COORDINATION.md:494` still read "Graphiti remains the baseline until an observed bake-off" two days later. Owner, 2026-08-29: _"Every time I try to deploy it, it doesn't work right. This is fucking stupid."_ Corrected in `HANDOFFS.md` R4 and `COORDINATION.md` R10 block on 2026-08-29. rel: D-093, R4, R10, ADR-0056, P-09 / GAP-008. |
| D-094 | **`ai` → `platform` consolidation rulings, recovered from the 2026-08-27 session and recorded here for the first time.** (a) **Target:** everything consolidates into the Case Bible PostgreSQL instance; the target database name is accepted as exactly `platform` and nothing else. AgentOS's database and every other out-of-scope store is going away, being renamed, or being reconfigured — consolidate there and verify completeness. (b) **Runtime role:** `platform_runtime` is the runtime login (LOGIN); NOLOGIN `context_import_writer` and `context_reader` are granted to it; `context_owner` is granted to `platform_admin`. `PLATFORM_DATABASE_PASSWORD` is required only on `--apply` and is never printed or persisted. (c) **`schema_version` shape:** exactly compatible with `apply_0036_live.py` — uuidv7 PK, bytea hash, `migration_id`/status fields, unique active `migration_id`. (d) **Bootstrap:** database created owned by `platform_admin`; `REVOKE CONNECT/TEMPORARY ON DATABASE platform FROM PUBLIC` and verify; migration 0036 executes under `SET ROLE platform_admin`/`context_owner` so ownership is intended. (e) **`ai` is preserved untouched** — not parked yet, never dropped. **Still genuinely open: the runtime-grant forward migration (owner's literal "4 ?? k"), and the park window/date for `ai`.** | A/B/C | **owner-ruled 2026-08-27; recorded 2026-08-29; two sub-questions still open** | Verbatim owner language recovered from the 2026-08-27 12:03 turns in `~/.codex/sessions/2026/08/27/` via DuckDB; extract retained at workspace root as `_owner-turns-0827-0829.csv`. The consolidation packet lists all six as pending; only two are. Extends and does not supersede D-091. rel: D-091, D-086, migration `0049`, `awaiting-verification/AI-TO-PLATFORM-CONSOLIDATION-2026-08-29.md`. |

---

## 2026-08-27

| # | Decision | Lane | Status | Rationale / notes |
|---|---|---|---|---|
| D-093 | **A platform-owned SATemporal GraphRAG lane and a pinned/decomposed Semantica lane run concurrently for explicit side-by-side evaluation, never as parallel authorities.** Both receive the same immutable PostgreSQL-authorized eligibility manifest and pre-ranking horizon boundary, then return the same lane-labelled result/citation/provenance envelope without automatic fusion. DozerDB isolates the graph projections in named databases: existing `evidence` remains the Semantica graph; new `sat_temporal` owns the custom legal SAT structure/version/action/text model and temporal/rule-graph retrieval; historical `memory` and default `neo4j` remain untouched until separately reconciled. Temporal durably fans out and joins the lanes; n8n/Workbench starts, observes, and compares runs. PostgreSQL remains canonical and stores manifests, candidates, governance, evaluation results, and receipts; Weaviate collections and graph projection generations remain separate; SurrealDB remains the final PG-reconciled temporal graph, walk, and analysis engine. Neo4j/DozerDB superuser access is reserved for the owner and developers. Runtime services and agents never receive it directly: fixed-database graph gateway operations hold credentials server-side, expose no caller-selected database parameter, and return receipts to PostgreSQL. Direct developer graph edits are noncanonical unless returned to PostgreSQL as attributable candidates/overrides and otherwise may be replaced by a rebuild.** | A/B/C | **owner-ruled 2026-08-27; implementation pending** | The custom lane is a platform-owned composition, not one published algorithm: legal SAT-Graph supplies structural/version/action concepts; STAR-RAG rule-graph/PPR and TG-RAG time hierarchy are optional, separately benchmarked retrieval components. Semantica may be progressively decomposed and only useful MIT-licensed pieces retained behind its stable lane contract. DozerDB multi-database isolation is already proven, but its pinned version lacks usable RBAC; the fixed-database gateway prevents ordinary callers from choosing another graph while preserving developer superuser access. First implementation compares extraction stages before retrieval stages so differences remain attributable. rel: D-073, D-074, D-077, D-078, D-080, ADR-0036. |
| D-092 | **Canonical content is never redacted during intake, raw extraction, normalization, storage, indexing, analysis, or ordinary query. Redaction happens only when the owner explicitly requests it while preparing a derived court-facing document or export.** A redaction operation creates a separate attributable output and receipt; it never edits, replaces, masks, or deletes source bytes, raw rows, normalized rows, evidence, governed facts, or ordinary query results. Canonical schemas and default query paths must not require a redaction predicate or carry workflow redaction state. | B | **owner-ruled 2026-08-27** | Removes ingestion/query complexity and preserves full-fidelity source authority. Court-production redaction is a late, explicit presentation operation over selected content. Any prior proposal for ingest-time, canonical-row, timeline-curation, or default-query redaction is superseded to that extent. rel: D-085, R12 export/review. |
| D-091 | **The rebuilt application starts with a fresh PostgreSQL database named `platform`; it does not repurpose or migrate in place inside `ai`.** The migration/admin identity is `platform_admin`, the runtime login is `platform_runtime`, and domain capability roles remain narrowly named (for example, `context_import_writer`). The existing `ai` database was never accepted as a live application database and remains untouched as a legacy/source database until preservation review; it is never dropped or overwritten. Migration 0036 is held: it must not be applied to `ai` and may target `platform` only after a consolidated baseline, role/grant design, preservation inventory, cutover plan, and retargeted apply tooling are reviewed. | A/B/C | **owner-ruled 2026-08-27; fresh database not yet provisioned; 0036 unapplied** | Extends D-086 from service naming into database/role naming and avoids replaying heavily superseded schema history into the replacement application. This is a fresh database on the existing PostgreSQL service unless a later infrastructure decision says otherwise; it is not permission to delete legacy data. Historical `ai`, `agno_app`, and `ai.agno_*` observations remain accurate history/legacy inventory, not forward deployment targets. |

---

## 2026-08-26

| # | Decision | Lane | Status | Rationale / notes |
|---|---|---|---|---|
| D-090 | **Universal parsing is capability-first and package-substitutable, while n8n analysis/control nodes remain downstream of parsing and normalization.** Required reusable extraction capabilities include Apache Tika text/metadata/MIME/language extraction, EXIF/IPTC/media metadata, image and scanned-document OCR, scanned-PDF OCR, universal archive extraction, semantic text splitting, and broad file-metadata extraction. The current community candidates are `n8n-nodes-tika`, `n8n-nodes-exif-data`, `n8n-nodes-tesseractjs`/`tesseractjs7`, `n8n-nodes-pdf-tesseract-ocr`, `n8n-nodes-universal-archive`, `@bitovi/n8n-nodes-semantic-text-splitter`, and `n8n-nodes-file-metadata`. Each package must pass representative-corpus correctness, metadata fidelity, bounded-memory, failure, security/licensing, and pinned-version/rebuild tests. A package failure removes only that implementation: the capability remains required and receives a contract-compatible platform wrapper or owned n8n node. Separately, Text Classifier, Sentiment Analysis, Summarization Chain, Default Data Loader, Recursive Character Text Splitter, Compare Datasets, Sort, and Date & Time belong in post-normalization enrichment, verification, analysis, and presentation Activities; Local File Trigger, HTTP Request, parameterized Postgres, Extract From File, Compression, XML, and the shared Error Trigger provide intake, transport, format-envelope, persistence, or control mechanics. Parsers only parse; these nodes must not silently add analysis to a parser Activity.** | B/C | **owner-ruled 2026-08-26; validation pending** | Preserves the owner-required capabilities without binding the platform to an unreliable community package, while retaining n8n's off-the-shelf value above the common normalized contract. Temporal remains the durable orchestrator and each independently retryable responsibility remains a separate Activity; an n8n mini-workflow may implement an Activity body but may not collapse raw extraction, normalization, enrichment, review, and persistence into one opaque flow. rel: D-077, D-087–D-089. |
| D-089 | **Hash Activities use durable attempt/member staging with short PostgreSQL transactions; they never hold one transaction or pooled connection while reading an external source.** `context.hash_batch` records one Activity attempt and its exact source/generation subject; `context.hash_batch_member` appends ordered digest membership durably with enforced subject FKs and construction tags. Begin, append, finalize, and abort use bounded transactions. A restarted attempt verifies or resumes identical staged members; a later retry may verify/reuse a completed result. Finalization alone writes the successful Activity receipt, canonical hash receipts/manifests, and the batch's guarded open→completed transition.** | B/C | **integration correction 2026-08-26; implemented locally; migration executed successfully in a rollback-only live PG18 transaction 2026-08-26; deployment pending** | Replaces the first adapter draft's transaction-local TEMP staging, which was memory-bounded but held a transaction for the entire file read and lost staged progress on worker loss. Atomic means the canonical finalize transaction is all-or-nothing; it does not require keeping a database transaction open during slow I/O. This preserves Temporal retry safety, bounded memory, PostgreSQL pool health, and exact receipt/result binding together. The live rollback validation found and removed three invalid `activity_receipt` foreign keys that referenced undeclared columns, then proved the migration executes and leaves the preflight relation inventory unchanged after rollback. |
| D-088 | **Universal-import raw-all H3 and normalized-record hashing use exact, enforceable construction tags.** The universal raw generation includes every ordered raw row, including rejected, malformed, unknown, unparsed, and envelope spans. It uses the SBV-tested empty-genesis fold formula but the distinct tag `h3-chain-platform-rawall-genesisempty-v1`; it MUST NOT reuse `h3-chain-sbv-genesisempty-v1`, whose membership is SBV logical records. Exact otherwise-unparsed/envelope spans use `h2-rawspan-v1`. Normalized records use `normalized-record-postgresql18-jsonb-text-utf8-sha256-v1`, defined as SHA-256 over the UTF-8 bytes of PostgreSQL 18 `jsonb::text`, and SQL requires those bytes to equal the stored payload's rendering.** | B/C | **integration correction 2026-08-26; implemented locally, deployment pending** | Narrows D-087 without changing its five-computation model. One construction label must never name two membership sets or serializers. The raw fold still calls the single reusable SBV custodyhash implementation; only the platform raw-all membership tag changes. Record-set Activity receipts point to the exact generation-scoped digest set, while single-result hash Activities point to the exact hash receipt row. |
| D-087 | **Universal import uses five separate hash computations, and only the raw-byte integrity family is named H1/H2/H3.** `hash_source_activity` computes H1 over the exact retained source bytes; `hash_raw_records_activity` computes H2 over each exact raw logical record/span before decoding or normalization; `hash_raw_generation_activity` computes H3 as the precisely tagged order-sensitive fold of those raw H2 members. `hash_normalized_records_activity` and `hash_normalized_generation_activity` compute separately named normalized-record and normalized-generation-manifest digests and MUST NOT call them H2 or H3. Comparing byte coverage/accounting to the source and rechecking normalized membership are separate verification/reconciliation Activities, not a fourth hash level and never an H1=H3 comparison.** | B/C | **owner-corrected 2026-08-26; implemented in contracts + Go workflow/hash Activities, deployment pending** | Corrects D-075/D-076 and D-077's phrase “normalized H2/H3” for the universal context-import path. The older Case Bible/evidence H1-genesis construction remains a separately tagged compatibility/evidence-promotion family and is never used to label normalized context digests; legacy rows are not rewritten. Computing raw integrity H1/H2/H3 during context intake does not promote the source to evidence or grant evidentiary authority: promotion remains a later governed action that independently verifies the selected source/lineage. Exact implementation boundary and live status: `docs/reviews/2026-08-25-schema-audit/SBV-GO-TEMPORAL-RUNTIME-BOUNDARY.html`. |
| D-086 | **Owned naming is "The Platform" / `platform-*`; Agno and AgentOS identifiers become legacy compatibility aliases only, and renaming an owned resource is explicitly NOT Agno framework removal.** Human-facing umbrella = **The Platform**. Owned technical prefix = `platform`. Core API = `platform-api`. Operator UI = `platform-workbench`. Domain services/workers = `platform-<domain>`. Owned environment variables = `PLATFORM_*`. No new branded product name may be invented. `agno` is PRESERVED wherever it names a real installed package, import path, library-created table, library-read environment variable, upstream image, external URL, or an accurate historical fact — until the corresponding N05 dependency-removal packet replaces it. | A/C | **owner-approved 2026-08-26** (scheme confirmed in `PLATFORM-NAMING-MIGRATION-PROPOSAL.md`; registry frozen and census completed the same day) | Blind textual replacement is prohibited: a bare `agno` match also hits `agnostic`, the 16 library-created `ai.agno_*` tables, `agnohq/*` base images, `from agno.*` imports (356 lines / 70 files) and `os.agno.com`. `OS_SECURITY_KEY` is **third-party** — verified read by `agno/os/auth.py:199` and `agno/os/app.py:1213` in the installed package — so renaming it would silently disable the only auth gate on every route; it is frozen until N05. Append-only history (`docs/adr/**`, this log, `CHANGELOG.md`, `docs/planning/**`, `docs/awaiting-verification/**`, `_stale/**`, `knowledge/**`) is corrected by dated supersession note, never rewritten. Execution is split into non-overlapping semantic packets N02 (code/config aliases) and N04 (current-truth docs) — both safe to run in parallel now — then N03 (Coolify/compose/DNS, after N02), N06 (alias cutover, after a live caller census), with N05 (actual Agno framework/runtime retirement) and N07 (repository/directory identifiers) as separately gated tracks. Full registry, exact old→new mappings, DO-NOT-CHANGE list, repository + live-deployment census (7,029 lines / 681 files / 27 Coolify apps), stop gates and receipts: `docs/reviews/2026-08-25-schema-audit/PLATFORM-NAMING-CENSUS-AND-HANDOFF.md`; owner view: `PLATFORM-NAMING-MIGRATION.html`. Open owner choices: UC-1 replacement DNS name (blocks N03; recommended `platform.mitechconsult.com`), UC-3 shared `agno` docker network (recommended: leave as legacy, revisit at N05), UC-5 GitHub repository rename (recommended: defer past N06), UC-6 local directory rename (recommended: DO NOT — it keys the memory/recall lanes). rel: → author ADR if the alias-and-cutover contract needs alternatives-considered; no existing ADR governs naming. _Byline: Claude Code · Opus 5 · 2026-08-26._ |
| D-085 | **The Timesketch fork is a governed round-trip context-curation service, not a read-only visualization.** It displays both context/candidate timelines and evidence-approved/governed timeline entries. The owner can select and modify timeline items individually or in bulk—tags, classifications, display summaries, temporal proposals, entity links, grouping, inclusion, importance, dispute/verification labels, ~~redaction markings,~~ and review state—and submit the changes back to PostgreSQL. Each submission is an immutable attributable batch with per-item before/after hashes, expected source version, rationale, idempotency key, validation result, and compensating-reversal lineage. **Redaction markings are superseded by D-092: redaction is not timeline curation and occurs only in an explicitly requested derived court-facing output.** | A/B | **owner-ruled 2026-08-26; redaction clause superseded 2026-08-27 by D-092** | The fork never overwrites raw source bytes/messages, custody evidence, or an approved timeline/fact version, and never writes canonical PG tables or external stores directly. Every modification to an evidence-approved entry returns as a context-layer amendment candidate linked to the exact approved entry and evidence lineage. The approved version remains active and unchanged until that candidate is independently re-reviewed and reconciled; acceptance creates a governed successor rather than an update. Other edits become typed context annotations, resolution assertions, candidate decisions, or successor derived-context versions. PostgreSQL accepts/rejects each item, emits outbox events, and a new projection generation returns the reconciled result to the fork. Partial bulk success is explicit; conflicts fail closed; undo is a new compensating batch, not deletion. |
| D-084 | **The personal-case timeline surface will be a maintained Timesketch fork, not merely a generic adapter or an embedded timeline widget.** The fork retains Timesketch's chronology/search, annotations, analyzer/aggregator extension points, OpenSearch event index, and Vue application shell, while replacing or disabling DFIR-specific importers, analyzers, vocabulary, and default views with relationship, life-event, investigation, and legal workflows. PostgreSQL remains canonical; the fork is a rebuildable governed projection populated only through transactional outbox/change detection and replayable projection generations. | A/B/C | **owner-ruled 2026-08-26** | Timeline candidates may be extracted from **any context source**, including AI chats, but carry explicit `candidate/context-only` authority until independent evidence and governed review establish a fact. The PG timeline contract must losslessly map to Timesketch's required `datetime`, `message`, and `timestamp_desc` fields plus bounded attributes, stable source IDs, entity refs, temporal uncertainty, authority/verification/dispute state, and projection receipts. Fork-originated tags/comments/analyzer observations return only as typed candidates or review commands; they never write canonical facts/evidence directly. No upstream files are deleted: DFIR modules are retained, disabled, or moved to `to_be_deleted` only under the repository deletion policy. |
| D-083 | **AI-chat extraction fans out into typed investigation and created-work families; it is not limited to claims.** Preserve actual generated documents, code, and attachments as created works with immutable versions and exact chat-span or asset lineage. Extract potential claims and events into the claim chart/list; legal issues, concerns, contradictions, and evidence needs into the investigation register; AI observations into a separately typed observation-candidate stream; and proposed strategies into a strategy-candidate stream. A proposed brief, motion, affidavit, exhibit plan, discovery request, or other court paper may become a draft legal work product only through an attributable selection/materialization event. | B | **owner-ruled 2026-08-26** | None of these outputs gains factual or evidentiary authority from the chat. Candidate items require review; created works remain authored artifacts; governed export still requires R12 citation/review gates. Canonical fan-out: `AI chat -> extraction run -> {claim/event candidates, investigation concerns/evidence needs, observation candidates, strategy candidates, created-work versions}`. All branches retain exact context-only lineage to the originating conversation/message/span. |
| D-082 | **AI chats are permanently context-only and are never promoted to evidence.** Extraction may turn material discussed in a chat into event candidates, claim candidates, investigation leads, and evidence needs. Those items enter the claim chart/claim list and guide a search for real evidence. A discussed event or claim becomes an established/verifying fact only through governed review of independent custody-backed evidence; the AI chat itself is never an evidence anchor, corroborating source, or proof that the event occurred. | B | **owner-ruled 2026-08-26** | Clarifies D-057/ADR-0053 and narrows D-069: the general owner-promotion path does not apply to AI-chat conversations/messages. Provenance back to the chat is retained as lead/extraction lineage, clearly typed as context-only, but it contributes zero evidentiary support weight. Canonical flow: `AI chat → extracted event/claim/lead → claim chart/list → evidence search → custody-backed evidence → governed fact`. |

---

## 2026-08-25

| # | Decision | Lane | Status | Rationale / notes |
|---|---|---|---|---|
| D-081 | **Whole-platform reconciliation is divided by semantic authority transition into R0-R14 bounded workstreams, each with a mandatory authority/input/output/source-path/clock/idempotency/CDC/census/migration/test/handoff contract. R9 independently reconciles every sister-store receipt before Surreal; R14 independently verifies migration and live end-to-end completeness.** | A/B/C | **owner-directed 2026-08-25** | Prevents agents from dropping cross-domain requirements or declaring a database slice complete in isolation. Master dependency map and assignment rules: `docs/reviews/2026-08-25-schema-audit/RECONCILIATION-DOMAIN-WORKSTREAMS.md`. |
| D-080 | **The canonical engine is PostgreSQL 18 augmented by `pg_duckdb`, PostGIS, and pgvector.** PG therefore handles canonical relational/JSON state, change/outbox control, analytical and object-store scans through DuckDB, raw/normalized geospatial data through PostGIS, and canonical/local vector representations plus reconciliation through pgvector. External databases are workload-optimized rebuildable serving/analysis projections—not compensations for missing PG authority: Weaviate serves search, Neo4j serves the Semantica-originated semantic graph, and Surreal serves the final reconciled cross-domain temporal graph and walks. | B/C | **owner-ruled 2026-08-25** | Clarifies D-078/D-079. Metadata, lineage, governance, projection jobs/receipts, and normalized truth always return to/stay in PG. `pg_duckdb` is inside PG, not a separate truth database; pgvector does not displace the ruled Weaviate search surface. |
| D-079 | **Raw geodata and normalized geospatial representations live in PostgreSQL/PostGIS.** Geo ingest, provenance, coordinates/geometries, source versions, and relevant derived geo events/features remain PG-authoritative. PG change detection triggers geo processing; governed/relevant temporal-geospatial facts and typed source references are then included in the PG-reconciled Surreal aggregation so they can be analyzed against communications, claims, realizations, and other events. | B/C | **owner-ruled 2026-08-25** | Corrects D-078 only where its shorthand grouped geo with sister databases. PostGIS is the geo home inside PG; Surreal receives relevant governed geo facts/projections, not a competing raw-geo truth store. |
| D-078 | **PostgreSQL is the canonical source-and-control plane for the entire lifecycle. All downstream processing starts from PostgreSQL change detection/outbox events. Weaviate, Neo4j, geo, and other sister databases are specialized derived processors/projections; every projection/result writes its receipt, provenance, status, and source linkage back to PostgreSQL. Weaviate is the search surface and every object/hit resolves to exact PostgreSQL source/chunk/version authority. Neo4j is primarily Semantica's semantic candidate/relationship graph and every node/edge resolves to exact source provenance. SurrealDB receives only a PostgreSQL-authorized, version-pinned, cross-store reconciled manifest of promoted/governed material for final temporal-graph analysis and walks.** | B/C | **owner-ruled 2026-08-25** | No sister database may become an independent truth store or feed Surreal directly without PG reconciliation. Ingest, normalized representations, candidates/facts, governance, projection jobs/receipts, and aggregate readiness remain in PG. Specialist payloads stay in their proper engines; PG retains their identities, metadata, authority, and lineage. |
| D-077 | **n8n owns the visual business/agent flow; Temporal owns durable Workflow execution and schedules every independently tracked Activity. Hashing is a standalone callable capability exposed through separate Temporal Activities for H1 computation, normalized H2/H3 computation, promotion verification, and later evidence reverification.** Hashing must not be embedded inside parsing, normalization, storage, promotion, or evidence code. n8n starts/signals Temporal and may itself be called as an Activity body; it must not bypass Temporal for load-bearing work. Activities exchange immutable references/manifests, not files or full record batches. | B/C | **owner-ruled 2026-08-25** | Refines D-068 into concrete boundaries. Temporal/n8n entered the architecture only days ago, so incomplete workflow coverage is expected integration work, not a failed rollout. The legacy `ChatTranscriptIngest` boundary is nevertheless wrong after D-069: its `custody_activity` performs ingest-time evidence writes and `store_activity` combines normalization/storage. Target workflow and gap map: `docs/reviews/2026-08-25-schema-audit/TEMPORAL-N8N-WORKFLOW-AND-GAPS.md`. Deferred SBV/ChatMiner consolidation remains behind one parser Activity contract. |
| D-076 | **H3 is the ordered completeness seal for one normalized source generation.** For the platform evidence chain: `chain_0 = H1`; for every `h2-canonical-v2` in deterministic source sequence, `chain_i = sha256(utf8(chain_{i-1}_hex \|\| H2_i_hex))`; the final value is H3. Normalization may compute a provisional head. Promotion independently verifies H1, the complete ordered H2 membership/count, and H3 before accepting the H2/H3 assertions into custody. H3 covers the complete normalized source generation even when only selected records are promoted as evidence; it proves integrity/completeness, not blanket evidence approval. Later reverification appends results. New writes use the precise tag `h3-chain-h1genesis-hexconcat-v1`; legacy `h3-chain-v1` rows remain read-only and are disambiguated by writer. SBV's `h3-chain-sbv-genesisempty-v1` remains a separate raw-import integrity receipt and is never conflated with evidence H3. | B | **owner-directed resolution 2026-08-25** | Reconciles D-069/D-075 with the two test-proven constructions. The H1-genesis chain matches `h2-canonical-v2` and the Case Bible 1,918-link proof. H3 is per source/parser-canonicalization generation, not global across unrelated sources and not based on promotion order. Any parser/canonicalization change creates a new generation and chain; history is never rewritten. |
| D-075 | **H2 is computed during normalization for each individual message/normalized record, verified at evidence promotion, and may be independently reverified during evidence processing.** The normalization-time H2 is a provisional content fingerprint, not yet a custody assertion. Successful promotion recomputes/verifies H2 against the selected normalized content and its original-source/H1 lineage, then records the accepted custody H2. Later evidence reverification appends a verification event/result; it never replaces the accepted hash or rewrites history. | B | **owner-ruled 2026-08-25** | Refines D-069's "hashes at ingest are fingerprints" boundary: H1 identifies the original source; H2 is the per-message/per-record normalization process. Computation, promotion verification, and later evidence reverification are distinct lifecycle events. H3 timing is not changed by this ruling. |
| D-074 | **Semantica (awksite) is the governed semantic extraction layer.** It extracts entity, relation, event, temporal, claim, and conflict candidates with exact source provenance; it supports entity resolution and conflict-resolution proposals, but it does not declare canonical truth or write final walk beliefs. Governed review/promotion establishes facts before they enter the final Surreal analytical graph. | B | **owner-ruled 2026-08-25** | Incorporates Semantica as a first-class stage rather than an optional side worker. Preserves ADR-0043's candidate boundary while broadening the required production role to provenance, entity resolution, and conflict proposal. |
| D-073 | **SurrealDB is the final temporal-graph aggregation, walk, and analysis engine.** Evidence modalities remain in their proper authoritative/specialist homes; governed, established facts and typed provenance references are projected into Surreal, where the complete cross-source temporal graph is assembled and the final as-lived/hindsight walks and delta analysis execute. | B/C | **owner-ruled 2026-08-25** | Refines D-070 and supersedes ADR-0056's description of Surreal as merely experimental. PostgreSQL remains custody/governance authority; Surreal does not own original bytes or silently promote candidates. The final analytical product is computed in Surreal from governed cross-modal facts. |
| D-072 | **The platform is permanently one owner and one personal case. Do not build multi-Matter tenancy, Matter-to-CourtCase hierarchies, cross-Matter isolation, or evidence scope-binding machinery.** Existing Matter/CourtCase IDs and `case_id='primary'` are compatibility scaffolding only and must not proliferate into new domain architecture. | A/B | **owner-ruled 2026-08-25** | Reasserts D-041 and supersedes D-060 / ADR-0055's multi-proceeding identity consequence. Any later flattening is a separate migration; no destructive change is authorized here. |
| D-071 | **Sender, recipients and participants STAY on the message record.** The `message_participant` → `entity` address-book link is ADDED for entity resolution, never a replacement for the record's own `sender` / `recipients` / `participants` / `sender_entity_id` columns. Any "trim" that removes participant columns from a message or spine row is rejected. | B | **owner-ruled 2026-08-25 ~03:25** | Owner, on the shapes page's spine-trim line "participants/sender/recipients/sender_entity_id (→ message_participant)": removing sender/recipients from a message table is "about the dumbest… shit I've ever [seen]". `docs/schema/three-message-shapes.html` Section C re-based the same night (generator `scripts/schema_report/build_shapes.py`): merge recommendation struck, three tables stay separate (D-069 companion ruling 03:07), participant columns marked KEPT. Recorded by Claude Code · Fable 5. |
| D-070 | **Graphiti is retired for now.** Memory/graph lane = SurrealDB (ADR-0056 governed projection + walk memory) with n8n as the agent layer and Temporal as the durable spine (D-068); the graph engine is an OPEN choice between Cognee and Memgraph. ADR-0014/0031/0037/0038/0039 (Graphiti) are suspended, not deleted. | C | **owner-ruled 2026-08-25 03:12** | Owner: "Graphiti is dead, for the moment anyways — remember Surreal and n8n and Temporal, and either Cognee or Memgraph." Any doc or diagram that shows Graphiti as a live lane is stale as of this entry. Recorded by Claude Code · Fable 5. |
| D-069 | **Ingest lands EVERYTHING as context; nothing is evidence until the owner promotes it. Promotion verifies the record fingerprint against the original file (H1), and only then does custody (H1/H2/H3 chain, `evidence.*`, immutability) begin. Per-record hashes computed at ingest are fingerprints, not custody.** | B | **owner-ruled 2026-08-25 03:02; wording CONFIRMED by owner 03:10.** Same ruling: the raw landing tables (`evidence.raw_*`) MOVE to the context layer — they are ingest output, not evidence. Separately ruled 03:07: the three message tables (first-party / acquired-third-party / AI-chat) stay SEPARATE; a merged message table is rejected. | Owner: "evidence doesn't come at ingestion. Everything gets ingested as context. Then I decide what gets promoted to evidence. Then the hash gets verified and the process begins… we can hash messages as they go into the normalized tables, that's fine, but it's not immutable." **Supersedes** ADR-0044's ingest-time custody for primary evidence and `sql/0009`'s "evidence.raw_* is the ONLY insert target"; `custody.py::ingest_artifact()` writing `evidence.*` at intake is now wrong-direction. ADR-0055 promotion is the only path into `evidence.*`. Consequences and sequencing: `docs/schema/README.md` + the reviewed plan in `docs/reviews/2026-08-25-schema-audit/`. Recorded by Claude Code · Fable 5. |

## 2026-08-24

| # | Decision | Lane | Status | Rationale / notes |
|---|---|---|---|---|
| D-068 | **Pipeline architecture: n8n is the agent/integration layer, Temporal is the durable spine; the custody path is an in-code Temporal interlude at a logical breakpoint** | B | owner-ruled 2026-08-24 (evening) | Owner's target structure, stated and then refined the same night: agents and modular workflow chunks are built IN n8n (visual, community nodes, exposed via webhook/MCP); Temporal executes the sequence durably and owns human-in-the-loop via the existing Signal gate (n8n is the notification/approve-reject surface that fires the Signal); HITL results flow back to whichever n8n agent comes next. **The custody path — ingest, hash, store — stays in-code Temporal activities**: owner ruling verbatim intent, "that can be one of the logical breakpoints in the workflow where it hands it back to Temporal … durably recorded, and if it fails or stalls it restarts, then hands back to the workflow to continue in n8n" with the platform's patterns/agents/categories/customizations. Promotion stays gated: n8n agents prepare and propose; the PG promotion table's approval is what flips evidence status; Surreal receives projections of approved events (PG remains truth). Framework implication: n8n agents join the P1 knowledge-activity bake alongside Agno/PydanticAI (callable over HTTP/MCP), and LangGraph is back under evaluation (owner, same day). Supporting research: `docs/research/N8N-CAPABILITY-ASSESSMENT-2026-08-25.md` — key caveats absorbed: first-class n8n Agents are self-hosted **Preview** (no queue mode, no native Weaviate — wrap via Workflow-tool/MCP), n8n has no durable execution (removed stalled-job retry in 2.0), so nothing n8n does is load-bearing for durability; at-least-once triggers require Temporal workflow IDs derived from object keys for dedup. **Strategic rationale (owner, same night):** deferring to a mature framework is what makes the sister-project merge tractable — the Case Bible corpus, TraceIQ geo data, the workbenches, and Legal-Workspace can "come together regardless of shape" as thin n8n adapter workflows instead of bespoke glue code, per the standing minimize-custom-code rule. rel: D-067, D-060 (promotion gate), TODO-201 (search modes + multi-query), temporal-awareness mandate 2026-08-24. _Byline: Claude Code · Fable 5 · 2026-08-24._ |

## 2026-08-18

| # | Decision | Lane | Status | Rationale / notes |
|---|---|---|---|---|
| D-067 | **Temporal adoption ruled: persistence inside the existing PG18; live one-move cutover; workbench console is the approval surface** | B | owner-ruled 2026-08-23 | Owner answered the three asks in `docs/plans/TEMPORAL-INTEGRATION-PLAN-2026-08-23.md` §5: (1) Temporal's persistence lands as two databases (`temporal`, `temporal_visibility`) inside the existing `data-pg-files` PG18 on ovh-files — no second database server, per the data-vector six-corruption lesson; (2) the chat-transcript ingest cutover happens in ONE move on live traffic, fix-forward, per the standing replace-in-place/no-parallel-stacks rule; (3) approvals use the existing knowledge-workbench console — the reference project's separate approval web-ui is discarded. Owner caveat recorded verbatim: the workbench is not live yet ("none of that is live and functional yet"), so **deploying the workbench becomes a prerequisite inside P2**, not an assumption. Also folded in from review: the CLI-only context-chat ingest path is named as the P1.5 candidate, and the Temporal worker needs its own Coolify app image carrying the full `server/` dependency stack (first real P0 build artifact). rel: the plan doc, D-066, ADR-0045 (walk derivation untouched), owner rulings 2026-08-23. _Byline: Claude Code · Opus 5 · 2026-08-23._ |
| D-066 | **Evidence vectors leave Agno's JSON-metadata schema for a native, immutable Weaviate V1 collection** | B/C | accepted design; activation held | Evidence chunks project from the canonical PostgreSQL spine into `EvidenceChunkV1`, with typed `occurred_at`, range-indexed `source_available_from`, explicit source/projection/embedder identity, and self-provided 4096-d nv-embed vectors. After exact count/hash/canary reconciliation, a separately approved operator action switches the stable `EvidenceChunks` alias. The old Agno evidence collection remains intact for rollback. Migrations `0026`–`0029`, collection creation, production backfill, alias movement, reader rebinding, and deployment remain held. rel: **ADR-0059**, ADR-0040, D-042, D-065; runbook `docs/plans/WEAVIATE-NATIVE-EVIDENCE-CUTOVER-RUNBOOK-2026-08-18.md`. _Byline: Codex · GPT-5 · 2026-08-18._ |
| D-065 | **First-party and acquired-third-party messages use separate derived projections; occurrence, source availability, and plural realization stay separate; healthy checkpoints resume while terminal failures rewalk** | B | accepted; owner ruling 2026-08-18; activation still held | One canonical authored normalized spine remains. First-party source availability equals occurrence. An acquired-third-party conversation preserves its actual sender/recipients/participants with the owner absent and becomes source-visible only at custody-backed acquisition. Each message may link to zero-to-many realization events; those events do not replace or backdate source availability. Chunks/embeddings are derived from the correct projection. A healthy exactly reconciled checkpoint resumes the same walk identity; drift/revocation/mismatch or another terminal integrity failure seals an immutable non-resumable snapshot and starts an explicitly linked `rewalk_of`. This narrowly supersedes ADR-0045 A/A.4 and ADR-0056 decision 10; their one-authored-spine, governed-projection, and activation-hold decisions stand. rel: **ADR-0059**, ADR-0045, ADR-0056, D-042, D-061, D-064. _Byline: Codex · GPT-5 · 2026-08-18._ |

## 2026-08-16

| # | Decision | Lane | Status | Rationale / notes |
|---|---|---|---|---|
| D-064 | **Phase-0 Surreal investigation contracts accepted with shared-Context walks, sealed failure snapshots, HITL realization midpoint, and source-family corroboration** | B | accepted; S3 narrowed by D-065 | Owner rulings: **S1 A** — after parity, the as-lived agent reads evidence/memory only through reconciled Surreal, with PostgreSQL limited to canonical control/approval metadata and no broad-store fallback. **S2 B** — one shared Surreal Context per product/environment; Matter scopes plus first-class `walk`/`walk_step` records and walk-bound experiential beliefs isolate executions without corpus duplication. **S3 A amended** — ~~revocation/drift/outage pauses and seals an immutable historical snapshot; reconciliation starts a linked rewalk~~ **D-065/ADR-0059 narrows this: healthy reconciled pauses resume the same identity; terminal integrity failures seal and start a linked rewalk**, preserving the experiential before/after delta. **S4 A** — a walk may form labeled uncertain candidates only from its eligible horizon. **S5 C amended** — preserve the realization interval, propose its midpoint, require attributable HITL approval/adjustment, and withhold it until reviewed. **S6 A** — derivative copies count as one source family unless provenance review proves independence; raw-hit and independent-source counts remain separate. E1–E5 and all R9 activation holds remain unchanged. rel: **ADR-0056**, **ADR-0057**, ADR-0058, ADR-0059, D-061–D-063, D-065. _Byline: Codex · GPT-5 · 2026-08-16; supersession note Codex · GPT-5 · 2026-08-18._ |

## 2026-08-15

| # | Decision | Lane | Status | Rationale / notes |
|---|---|---|---|---|
| D-063 | **Investigation Search and scoped Behavioral Analysis use immutable manifests, bounded discovery, and explicit hindsight/as-lived modes** | B | accepted; design only 2026-08-15 | Workbench target includes Find Evidence, Reconstruct Event, and Discover Patterns. Behavioral runs freeze owner-selected people/events/conversation groups/non-contiguous dates/sources, analyze the closed set, then return separately logged discoveries for optional scope revision. Mode is immutable: hindsight, as-lived-so-far, or paired realization delta. Internal narcissistic-pattern, borderline-pattern, DARVO, gaslighting, triangulation, splitting, coercive-control, and reactive-behavior lenses are allowed but remain distinct from authenticated diagnosis; Case Prep translates to conduct-first evidence language. rel: **ADR-0058**, D-057, ADR-0045. _Byline: Codex · GPT-5 · 2026-08-15._ |
| D-062 | **Candidate claims drive bounded cross-system evidence assembly; reviewed established facts materialize immutable Surreal evidence subgraphs** | B | accepted; design only 2026-08-15 | A claim candidate generates an auditable query plan across PG, Weaviate, Neo4j, messaging/documents, and TraceIQ geo. Results are classified as support, contradiction, qualification, context, duplicates, unresolved identity, or missing expected evidence. Human/governed review creates a new immutable fact; corrections relate/supersede rather than rewrite. Every fact resolves through exact source spans and chunk generations to custody-backed originals. rel: **ADR-0057**, ADR-0052/0053/0056. _Byline: Codex · GPT-5 · 2026-08-15._ |
| D-061 | **SurrealDB returns as a governed analytical projection and experimental Spectron-compatible walk-memory runtime; PostgreSQL remains authority** | B | accepted; design only 2026-08-15 | The platform owns a Spectron-compatible API/implementation and may use an official binary if available without depending on it. Surreal receives promoted manifests, approved spans/full normalized content, chunks, versioned vectors, fact/evidence graphs, traces, and future TraceIQ projections; original binaries remain custody-controlled and hash-referenced. Partial approval never exposes a whole source. Graphiti remains until bake-off. This does not reactivate the parked legacy deployment or revive Agno's Surreal operational adapter. rel: **ADR-0056**, ADR-0043, ADR-0045. _Byline: Codex · GPT-5 · 2026-08-15._ |
| D-060 | **Matter and CourtCase are separate identities; legacy textual Knowledge partitions map through an explicit bridge** | B | accepted + implementation authorized 2026-08-15 | One enduring Matter may contain multiple CourtCase proceedings, while the product remains single-owner and initially single-matter. `case_id='primary'` remains a valid Knowledge partition key and is mapped, never cast, to the seeded Matter. New case-work writes carry `matter_id` and `court_case_id`; legacy UUID `case_id` fields remain compatibility fields until reconciled. Knowledge-to-Evidence promotion is transactional, idempotent, provenance-preserving, unreviewed/HITL-required, and unsafe for legal use by default. This narrowly supersedes D-041's identity-model consequence, not its single-owner/multi-client prohibition. rel: **ADR-0055**, R9 handoff. _Byline: Codex · GPT-5 · 2026-08-15._ |

## 2026-08-13

| # | Decision | Lane | Status | Rationale / notes |
|---|---|---|---|---|
| D-059 | **Every run produces an itemized durable report; Langfuse is correlated diagnostics, never the authority** | C | accepted + implementation in progress 2026-08-13 | Reports name every ran/skipped/failed unit, reason, output, duration, remediation, and review history. Postgres workflow rows + append-only `ops.workflow_run_review_action` are authoritative; review actions bind into ADR-0047's hash chain. Agno OTel traces share `trace_id` with reports and may mirror to explicitly enabled Langfuse. Workbench supplies drill-down and reason-required acknowledge/approve/override; pytest emits ignored JSON+interactive HTML. rel: **ADR-0054**, ADR-0047, D-058. _Byline: Codex · GPT-5 · 2026-08-13._ |
| D-058 | **AI-chat domain routing is CPU-first hybrid; LLM calls verify only the uncertain tail** | B | implemented as uncalibrated challenger 2026-08-13 | `hybrid` combines the deterministic `keyword-v1` baseline with a lazy, optional ONNX Sentence Transformer challenger (`BAAI/bge-small-en-v1.5` default, configurable). Encoder inputs are bounded to avoid silent 512-position truncation. Semantic assignments remain `pending_review` plus searchable `context` until an owner-labeled gold set supports SetFit/logistic multilabel training and held-out threshold calibration. Runtime/model failure safely falls back (hybrid) or emits `classification_failed` context (CPU-only). Remote LLM classification is reserved for ambiguity/novelty/disagreement and must be an independent verifier, not a per-chunk tax. rel: **ADR-0053 §4**, D-057. _Byline: Codex · GPT-5 · 2026-08-13._ |
| D-057 | **AI-chat knowledge ingest uses five lanes, post-chunk multi-label routing, selective confidence HITL, multimodal assets, and a human investigation register** | B | accepted + implementation in progress 2026-08-13 | The global vocabulary is `platform`, `legal`, `personal_history`, `context`, `evidence`; `relationship_timeline` merges into `personal_history`. AI chat may auto-route only to the first four; `evidence` remains custody-only. Raw truth is explicit `chat_conversation` → ordered `chat_message` with `role` retained and participants removed; no horizon/disclosure columns. Chunks are message-safe and canonical once, then classified multi-label; one embedding is reused across lane projections. High confidence auto-routes, ambiguity/failure stays searchable in `context` and enters review, nothing is discarded. Normalized tags support search. Every created work/attachment is preserved; lightweight/native extraction precedes Docling and then provider-plural VLM OCR, with Colab-via-MCP backup only. Entity/claim/time/event extraction is asynchronous; a human-curated `investigation_event` register links concerns, candidates, evidence needs, and primary evidence, and only a human promotes to timeline. As-experienced/hindsight walk tables/views are deferred. rel: **ADR-0053** (authoritative), ADR-0044/0045/0050/0051/0052, D-046/D-054/D-056. _Byline: Codex · GPT-5 · 2026-08-13._ |

## 2026-08-12

| # | Decision | Lane | Status | Rationale / notes |
|---|---|---|---|---|
| D-056 | **AI-chat context lane drops `disclosure_tier` entirely — `working.context_record` is just normalized data (reverses OQ-8 / D-042)** | B | ruled 2026-08-12 (owner verbatim: "No nothing is fucking written like that at all Normalized data just normalized fucking data … all that stuff is a different fucking table just add the normalized fucking data ingest the fucking data") | `working.context_record` no longer carries a `disclosure_tier` column. Migration `sql/0023` (idempotent `DROP COLUMN`, applied live 2026-08-12) + hot-patched `server/analysis/context_chat_ingest.py` (INSERT/SELECT no longer reference the column — `context_chat_ingest.py` is the ONLY reader/writer of `context_record`). The 1,617 `source='claude-ai-export'` test rows were wiped and re-ingested clean. **The horizon (as-lived / how-I-saw-it / discovered / hindsight) is a QUERY-level distinction** derived from the clocks + HITL realization events (ADR-0045 A.4 `working.realization_event` + the derived `vw_record_disclosure` view) — never a stamp on the normalized row: NOT `contemporaneous` (the old hardcode → every row a false as-lived assertion; this was the N3 defect) and NOT `hindsight` (which would re-introduce horizon logic at the ingest layer). **This REVERSES OQ-8** (ADR-0045 / D-042), which said "the AI-chat context lane auto-asserts hindsight at write" — ADR-0045's OQ-8 clause is struck through with this dated correction. SCOPE: `context_record` ONLY. `working.normalized_record` (evidence spine) KEEPS `disclosure_tier` as an asserted hint per ADR-0045 Decision C — UNCHANGED. Also this session: fixed the `require_tracked_code` PreToolUse hook (case-bible plugin) whose `\bpy\b` interpreter alternation matched the `py` in `.py` FILE EXTENSIONS, false-blocking legitimate `docker cp /tmp/x.py c:/app/y.py` staging against prod-reach; replaced the `\b` with `(?<![\w.])` so extensions can't pose as interpreters (real `python`/`bash`/`py`-launcher invocations still match; verified). rel: ADR-0045 (OQ-8, A.4, C), D-042, D-055, `sql/0023`, `server/analysis/context_chat_ingest.py`. _Byline: Claude Code · Fable 5 · 2026-08-12._ |
| D-055 | **Evidence path converges onto the change-detection spine · desktop ingest route is a first-class requirement** | B | ruled 2026-08-12 (owner verbatim: "The evidence path should also be utilizing the change detection workflow / Although a manual Trigger is fine for now / Our desktop route is necessary") | (1) **Evidence workflow must stop writing Weaviate inline** and go PG-first → spine — this is exactly ADR-0052 Phase 2 (evidence-workflow convergence, signed same day); owner adds that a **MANUAL trigger is acceptable for now** — i.e., before the always-on worker (Phase 1) exists, the evidence path may use the manual drain (`ingest.context-drain` pattern, one impl three front doors per ADR-0046), but it must NOT keep the inline sink-write. No new code authorized by D-055 itself; it schedules Phase 2 behavior and blesses the manual stand-in. (2) **The desktop ingest route (CLI on desktop → tailnet PG) is NECESSARY**, not a temporary workaround: design constraints that push ingest in-cluster (SBV_SERVICE_PASS for the Go engine, `/r2` mount for asset materialization) must be solved for desktop — options to choose when built: desktop-side `/r2` mount vs staging blobs in PG and letting an in-cluster step materialize them; desktop SBV auth passthrough vs Python-fallback-accepted. rel: ADR-0052 (Phase 1/2), D-048..D-054, DEBT (in-cluster-only constraints). _Byline: Claude Code · Kimi K3 · 2026-08-12._ |
| D-054 | **ADR-0052 design-point rulings: CDC shape + extraction semantics locked for the spine ADR** | B | ruled + **SIGNED** 2026-08-12 (owner answered all 8 open questions via structured Q&A, then signed the ADR: "sign 52" → status ACCEPTED; ~~still DRAFT awaiting final sign-off~~ corrected same day) | (1) **CDC mechanism** = transactional outbox + NOTIFY-as-wakeup + per-sink cursors (as drafted; logical replication rejected — WAL-slot disk risk too heavy for a single-owner platform). (2) **Outbox shape** = PER-TABLE outbox tables (`working.context_record_event` etc.), NOT one shared `ingest_event` (owner override of the draft's lean). (3) **NO SIZE AXIS in the engine split** — owner verbatim: "everything needs to go to the Go engine unless there's not a Go parser available or if for some reason the Go parser fails and we have a Python parser that functions." Coverage-based Go-primary + logged Python failure-fallback replaces the drafted f(format,size) gate; D-051 router's format keying stays. (4) **Trigger cost accepted**: FULL outbox row per insert, trigger-written (strongest transactional guarantee; ~2× ingest write amplification accepted at case scale). (5) **Extraction runs REGARDLESS of custody-approval** ("always extract regardless") — resolves ADR-0051's carried-over open question; in exchange candidate rows carry the source record's `disclosure_tier` + custody-approval flag and the horizon binds at promotion/materialization/retrieval, never at extraction. (6) **Two candidate tables**, renamed: `working.entity_candidate` + `working.claim_candidate` (~~`working.artifact_candidate`~~ — "artifact" reads as created works in owner usage; entities merge, claims accumulate and are never rewritten). (7) **Dead-letter policy**: dead-letter TABLE + replay tool + mandatory alert on count>0 (operator-console cdc-status surface); nothing silently dropped. (8) **Worker placement**: standalone always-on Coolify app on the data tier, NOT an agentos-api sidecar; Watch Paths set at app creation per the 2026-07-21 rule. rel: ADR-0052 (updated in place), ADR-0051 inv 4 + open questions. _Byline: Claude Code · Kimi K3 · 2026-08-12._ |
| D-053 | **Explicit format+engine override surface made REAL and STRICT — `--format` bypasses the detection router, never falls back** | B | built + tested 2026-08-12 (owner Task 15, pre-GUI MVP: operator explicitly specifies (a) the FORMAT and (b) the parse ENGINE per ingest, CLI/API flags now; SBV GUI selectors later) | D-049 landed the `engine=`/`format=` kwargs on `parse_chat_export`/`ingest_chat_file`/`ingest_chat_archive` and the CLI flags, but `format` was only honored on the Go path and merely advisory for Python (the registry self-detected); D-051's router ignored it on the auto path. This entry closes that gap — **surfacing the existing seam, not a second mechanism:** `format_router.resolve_format_override(format, engine)` maps an explicit format (a router format_id like `claude-ai-export`, a Go importer id like `chatgpt-official-json`, or a `transcripts.<name>` registry tool id) to `(engine, go_id, python_tool_id)` WITHOUT running detection; `engine="auto"` + format resolves Go-primary (same preference the router encodes). **STRICT contract:** an explicit override never falls back — unknown format, `--engine go` with a Python-only format, `--engine python` with a Go-only format, an unknown `transcripts.*` id, and a pinned parser REJECTING the file all raise ValueError listing the valid values (no mesh, no cross-engine swap); the CLI catches ValueError/FileNotFoundError and exits 2 with a one-line `error:` on stderr instead of a traceback. New `_parse_via_pinned_tool()` runs exactly the named registry tool. **Fail-fast validation is LOCAL:** new `GO_IMPORTER_FORMATS` in `format_router.py` is a hand-maintained MIRROR of the `Format*` consts in `vendored/sbv/internal/importer.go` (maintenance contract in the comment — update both in the same change); SBV's own `ImporterByFormat` still hard-errors ("unknown import format") as the second line. Flags absent = D-051 router behavior byte-identical. **No non-CLI API entry exists yet** (no FastAPI route, no registered `ingest.*` tool; `server/tools/ingest/` holds only the drain tool) — the programmatic entries ARE the two `ingest_chat_*` kwargs, already same-name/same-precedence; no new surface invented. **Verified:** 18 new tests in `tests/test_format_engine_override.py` (mirror integrity, resolver semantics, router bypass proven via a fail-on-call spy, both engines, all loud-error cases, CLI argparse exit-2, CLI dry-run end-to-end DB-free) + 11/11 existing engine-dynamic tests; ruff check clean on touched files. rel: D-049, D-050, D-051, ADR-0049, `format_router.py::resolve_format_override`, `context_chat_ingest.py::_parse_via_pinned_tool`, `scripts/ingest_context_chat.py`. _Byline: Claude Code · Fable 5 · 2026-08-12._ |
| D-052 | **Archive-as-whole-unit: context_asset + context_archive (Option B) + mount-based materialization** | B | schema APPLIED live + code built/tested 2026-08-12 (owner: "whether or not it's zipped or unzipped it's processed as a whole unit and ... the data within it is properly parsed and saved") | `sql/0022` (APPLIED live): **`working.context_archive`** (manifest per zip, dedup by archive sha256, holds users/projects/memories.json metadata) + **`working.context_asset`** (one row per generated doc/code/image, R2 blob index + small-text inline, `content_hash` UNIQUE = idempotent). **Option B again:** separate context tables, NOT the evidence-tier `working.artifact_registry` (which requires assertion_type/evidence_strength/review_status — routing context assets there would breach CONTEXT-never-EVIDENCE, same reason as context_record). `server/analysis/context_assets.py`: classify by extension → document|code|image|data|other (owner's doc-store/code-store split), write blobs via the **R2 rclone MOUNT** (`CONTEXT_BLOB_ROOT`, mirroring `custody.blob_root()` — NOT boto3, zero new deps), content-addressed keys. Wired into `chat_archive.ingest_chat_archive(materialize_assets=)` so a zip is one unit. **Verified:** 0022 validated via txn+ROLLBACK then applied (context_archive 9 cols/context_asset 12 cols, dedup + category CHECK enforced); 3/3 asset unit tests + 8/8 archive+asset; controlled REAL-write proof (synthetic zip → temp blob root → live PG: 3 assets classified doc/image/code, metadata captured, blobs written, then CLEANED UP — no test data left canonical); real dry-run scope on `user_data_export` = **169 assets / 34.5 MB** (99 doc, 54 image, 10 code, 6 other). **Not yet:** the real 169-asset materialization runs IN-CLUSTER where `/r2` is mounted (off-box shell has no mount), same pattern as the Go engine needing in-cluster SBV creds; baseline regen after 0021+0022. rel: ADR-0051, D-048, D-050, D-051, sql/0022, `context_assets.py`, `custody.py::blob_root`. _Byline: Claude Code · Opus 4.8 · 2026-08-12._ |
| D-051 | **Detection ROUTER (detect-once, Go-primary) replaces blind fall-through + Perplexity-contexts parser** | B | built + tested 2026-08-12 (owner: "I thought we were designing a router so that the whole fail-through doesn't have to be in a row like failing each one") | `server/analysis/format_router.py`: reads a bounded head, matches format SIGNATURES ONCE, returns (format_id, python_parser_id, preferred engine) — no more running every parser until one stops raising. **Go is the primary route:** a format routes to the SBV Go engine when a `go_format` decoder exists (chatgpt-official → go), else Python; `parse_chat_export(engine="auto")` now detects → routes Go-first with graceful fallback (Go → same-format Python parser → full mesh). Explicit `--engine`/`--format` still overrides. Also NEW parser `transcripts.perplexity-contexts` (`server/tools/parsers/ai_chat/perplexity_contexts.py`) for the real Perplexity export (`{conversations:[{context_uuid,context_title,mode,entries:[{query,answer}]}]}`, mode=COPILOT) — each entry → user(query)+assistant(answer). **Verified on all 3 REAL formats:** chatgpt→detect go/parse 124 (fell back to python off-box, SBV auth), claude→python/1617, perplexity→python/**7007 records (was UNPARSEABLE before)**; 34/34 tests. rel: ADR-0049, ADR-0051, D-049, D-050, `format_router.py`, `perplexity_contexts.py`. _Byline: Claude Code · Opus 4.8 · 2026-08-12._ |
| D-050 | **ZIP-aware ingest front door — real exports are archives, not bare JSON** | B | built + tested 2026-08-12 (owner: "the real ones come in a zip file with multiple different folders and json logs and files") | Real AI-chat exports arrive as a ZIP: Claude `data-*.zip` = conversations.json + users/projects/memories.json; ChatGPT/Perplexity `user_data_export_*.zip` = conversations-*.json + a spreadsheet + an `assets/` folder (PDFs/PNGs/.md/.py/.sql). New `server/analysis/chat_archive.py`: opens the zip WITHOUT full extraction (memory/disk-safe — assets can be hundreds of files), classifies every member (conversation_logs / metadata / assets / other), extracts ONLY the conversation log(s) to temp (zip-slip guarded — flattened to basename), and routes each to the engine-dynamic `ingest_chat_file`; sidecar metadata + assets are INVENTORIED and reported, never silently dropped. CLI auto-detects `.zip`. **Verified:** 5/5 unit tests (synthetic Claude + ChatGPT-with-assets zips, classification, only-logs-extracted, no-log error, zip-slip) + 32/32 across the context-lane suite; LIVE dry-run on a REAL Claude export zip → extracted conversations.json only, parsed 1617 records / 105 conversations, 3 metadata deferred. **Two gaps SURFACED (not silently swallowed) by a real Perplexity-style zip (`user_data_export_2025-12-15.zip`):** (1) its bespoke `entries`/`query`/`answer` conversation format has NO parser (all `parse.transcript` candidates rejected it) — needs a new parser; (2) its **169 assets** (44 pdf · 54 png · 54 md · 8 py · 2 sql) are the generated-document/code-snippet materialization the owner wants routed to a document/code store (Stage-2, still deferred — now visible in the archive inventory). rel: ADR-0051, D-048, D-049, ADR-0052 (extraction), `server/analysis/chat_archive.py`. _Byline: Claude Code · Opus 4.8 · 2026-08-12._ |
| D-049 | **Parse step is ENGINE-DYNAMIC — Go (SBV) OR Python (registry), explicit override, one downstream** | B | built + tested 2026-08-12 (owner: "the pipeline needs to be dynamic ... it shouldn't be dedicated to one format or the other") | The context-ingest parse step is no longer hardwired to the Python parsers. `parse_chat_export(path, engine="auto"|"python"|"go", format=...)` dispatches: **python** = in-process `parse.transcript` registry mesh (chatminer et al.); **go** = the SBV Go service (memory-safe universal decoder, ADR-0049) via new `server/analysis/sbv_transcript.py::parse_via_sbv` (SBV `/imports` + `/imports/{id}/records` → mapped to the SAME NormalizedRecord); **auto** = MVP default → python (detection ROUTER deferred until after POC per owner — the explicit `engine`/`format` override IS the MVP mechanism). Threaded through `ingest_chat_file` + CLI (`--engine`, `--format`). The engine choice changes ONLY who parses — destination (PG `context_record` → change-detection → Weaviate/Graphiti) is fixed (owner invariant: "the destination doesn't change just how it gets parsed is changeable"). CONTEXT tier = light custody, so the Go path imports+reads records only (skips the heavy H1/H3 reconciliation `sbv_sms.py` does for evidence SMS). **Verified:** 27/27 unit tests (dispatch routing + SBV-row→NormalizedRecord mapping via fake client, DB/SBV-free); live SBV reachable + healthy from box (`100.72.169.40:8085`), Go path reaches the live import endpoint (full live import needs `SBV_SERVICE_PASS`, present in-cluster). **Not yet:** GUI engine toggle in the SBV UI (owner wants it there too — front-end follow-up); detection router (post-POC); Go decoders for Claude/Perplexity/Gemini (only ChatGPT so far, D-047/4accbf2). rel: ADR-0049, ADR-0051, D-048, `server/analysis/sbv_transcript.py`, `server/tools/_sbv_client.py`. _Byline: Claude Code · Opus 4.8 · 2026-08-12._ |
| D-048 | **AI-chat CONTEXT ingest is PG-first — `working.context_record` is the source of truth, change-detection projects to Weaviate/Graphiti (Option B: separate table)** | B | **APPLIED to live + drain tool built 2026-08-12** (owner ruling, verbatim: "it's all supposed to go back into pg And then change detection will move it into vector db"; picked **Option B** — separate context table — over reusing `normalized_record`; then "make sure it's written as a tool ... so we can easily trigger that batch process") | Reverses the pre-2026-08-12 design where `server/analysis/context_chat_ingest.py` wrote chat chunks DIRECTLY to Weaviate `platform_context` + the Graphiti CASE lane and deliberately never touched Postgres. **Now:** parse → `store_context_records()` into **`working.context_record`** (Postgres SOURCE OF TRUTH) → `sync_pending_context(sink)` reads rows `WHERE <sink>_synced_at IS NULL` and projects them to Weaviate + Graphiti, stamping each synced. **Option B (owner pick):** context lives in its OWN table, NOT `working.normalized_record` — because normalized_record REQUIRES `artifact_id → evidence.evidence_hash` (custody blob) and context_record has NO evidence FK and is referenced by NO evidence surface, which IS the CONTEXT-never-EVIDENCE boundary (ADR-0044, owner 2026-08-01). **Migration `sql/0021_context_record.sql`** (17 cols, record-grain, `content_hash` UNIQUE for idempotent re-ingest, `weaviate_synced_at`/`graphiti_synced_at` NULL=pending as the CDC signal, partial indexes over pending rows, `lane` CHECK pins 'context'). The SQLite `IngestLedger` is RETIRED — PG is the dedup/sync authority now. **Change-detection is CDC-SHAPED but not the full spine:** the consumer is pending-row polling (inline after ingest, or `--no-project` defers to a worker drain), not a DB trigger/outbox/cursor (invariant 4 for all paths still deferred, ADR-0051). **Batch-drain tool (owner: "written as a tool ... so we can easily trigger that batch process easily and quickly until we do get the full workflow built out"):** registered capability **`ingest.context-drain`** (`server/tools/ingest/context_drain.py`) wraps `sync_pending_context` for both sinks — the manual stand-in for the CDC worker, callable from CLI + agent + MCP (invariant 7, one impl three front doors). CLI `scripts/drain_context.py` drives the SAME registered tool. **Verified:** 20/20 unit tests pass (15 ingest + 5 drain tool, DB-free via fakes); migration validated against LIVE Postgres inside a txn that ROLLED BACK (17 cols + 6 indexes, ON CONFLICT dedup 1,0, `lane` CHECK rejects 'evidence', nothing persisted), then **APPLIED + COMMITTED to live** (table present, 0 rows); CLI dry-run drain ran live end-to-end (CLI→tool→analysis→real PG, 0 pending). **Follow-up (not blocking):** regen `sql/bootstrap/schema_baseline.sql` (needs pg_dump from the workstation, sql/README.md procedure); wrap the drain in the eventual always-on CDC worker (ADR-0052). rel: ADR-0051 (Current-reality update), ADR-0044, ADR-0050, ADR-0052, D-045, sql/0021, sql/README.md, `ingest.context-drain`. Parser/context lane (this chat); does NOT touch Lane D KB files. _Byline: Claude Code · Opus 4.8 · 2026-08-12._ |
| D-047 | **Custody hashing decoupled into `pkg/custodyhash` (canonical single-source, matching-model)** | B | done 2026-08-11 (owner: decouple the hashing into its own module/matching model so PG/DuckDB/SBV/atomic/backup all agree; "everything modular, especially SBV") | Extracted the SBV H1/H2/H3 construction out of `internal/custody.go` into a standalone importable Go package `github.com/lowcarbdev/sbv/pkg/custodyhash` — the single source of truth. `internal/custody.go` is now thin shims delegating to it; `internal/engine.go`'s **3 hand-inlined chain folds** now go through `custodyhash.FoldChain`, killing the duplicated `sha256(chain+"\n"+h2)` so streaming and batch `ChainH3` can never diverge and the `"\n"` separator lives in exactly one place. **Fixed a latent tag bug found during extraction:** the package first used the AMBIGUOUS legacy `h3-chain-v1`; corrected to the precise `CanonH3 = h3-chain-sbv-genesisempty-v1` (+ `CanonH3Legacy = h3-chain-v1`, read-only) — the exact two-valid-construction collision the custody canon warns about. **Matching model:** any execution site (a PG function, DuckDB UDF, atomic CLI call, backup workflow, or a small Go/FastAPI HTTP mirror) produces byte-identical hashes by binding to THIS construction; H1 stays the independent Python cross-check (`custody.py::_sha256_file`). Verified: `go build -tags fts5 ./...` clean; `pkg/custodyhash` known-answer tests pass (fold pinned); internal custody automation test computes H1 unchanged. Pre-existing, unrelated: `TestFacebookJSONGoldenVariantsExactCustodyAndSourceOrder` fails on baseline too (UTF-8 golden encoding, not hashing — flagged, not mine). **Follow-up (not built):** the small `net/http`/FastAPI wrapper (`/h1`,`/h2`,`/h3`) so non-Go callers hit the same binary. rel: `vendored/sbv/CUSTODY.md` §"Decoupled module", ADR-0049. Parser/chunking lane (this chat); does NOT touch Lane D's KB files. _Byline: Claude Code · Opus 4.8 · 2026-08-11._ |
| D-046 | **Chonkie wrapped directly for specialty chunking — CPU-friendly local, heavy remote (Colab/GPU-rental via MCP)** | B | **local runtime complete 2026-08-16; remote/preview deferred** | Owner: wrap Chonkie directly (not just the 2 chunkers Agno surfaces), build into the platform, CPU-friendly-local / heavy-remote. **GUI claim CORRECTED** (verified via GitHub): Chonkie has NO GUI — its `Visualizer` is a terminal Rich component; it ships a REST API (`chonkie[api]`), so any GUI is ours to build (or previews via SBV GUI, ADR-0049). **Installed + production-pinned torch-free:** `chonkie[semantic,code,table]==1.7.0`; production requirements contain no torch/transformers. **Observed runtime:** the neutral ingest port stores Chonkie-derived normalized rows with source-record hashes/boundaries and versioned receipts; the dedicated chat path uses message-safe semantic grouping; Agno readers use the same direct wrappers. Versioned IDs state their actual unit (`chars`, `tokens`, or `rows`); final disposable scratch receipts proved Markdown 1 logical record → 3 `chonkie.recursive@1.7.0:1500-chars` rows and SBV Go 1 → 1 with fallback disabled. **Execution split remains:** LOCAL = Semantic(model2vec)/Recursive/Sentence/Token/Fast/Code/Table; REMOTE-only = Neural(torch)/Late(long-ctx)/Slumber(LLM). Remote executor + preview GUI remain deferred and must never cause a local torch install. Chunking is the unit the multipass classifier routes to lanes. rel: ADR-0050/0051, D-045, docs/planning/agno-chunking-strategy.md §8. _Bylines: Claude Code · Opus 4.8 · 2026-08-10; Codex · GPT-5 · 2026-08-16._ |
| D-045 | **Ingest-pipeline architecture written down (ADR-0051) + docs-vs-architecture audit fixes** | B | done 2026-08-10 (owner directive "do the docs get it right" + emphatic architecture statements this session) | Owner re-stated the intended flow many times on 2026-08-10 because it was captured nowhere. Authored **ADR-0051** (ingest pipeline: **one pipeline for everything**; SBV parses+previews then **HANDS OFF**; extraction — chunk→multipass→artifacts→entities→timeline — is a SEPARATE stage **triggered by PG change-detection**; **HITL verifies**; **custody tier is the ONLY evidence/context branch**; **one chat → many lanes**; knowledge-base-first). Status **Accepted as direction / NOT YET BUILT** — the ADR documents the current-reality gap it authorizes closing: (a) TWO disconnected ingest paths (`server/evidence/workflows.py` custody→parse→store→knowledge with one-domain-per-file and NO extraction, vs `server/analysis/context_chat_ingest.py` chunk→Weaviate+Graphiti); (b) Semantica (ADR-0043) unwired in both; (c) NO PG change-detection exists; (d) AI chats bypass SBV (0 AI-chat decoders in the Go engine, ADR-0049 Gap 2); (e) no HITL gate after an extraction stage because there is no extraction stage in the workflow. **Audit outcome on existing docs:** ADR-0050 (knowledge-base STRUCTURE) verified CORRECT and already indexed (README:63) — no change. **Drift fixed:** three more stale "SBV demoted to shadow / pure-Python effective primary" claims in `server/evidence/workflows.py` (module docstring lines 15-18, workflow `description` line 708, `NAMED_WORKFLOWS` line 822) corrected to SBV-primary (D-040) — the earlier D-044 sweep caught only one of four; `docs/BUILD_PLAN.md` four-domain vocab corrected to ADR-0050's six lanes with migration map. Verified: workflows.py parses, `tests/test_sbv_demotion.py` 12/12. rel: ADR-0049, ADR-0050, ADR-0051, ADR-0043, D-040, D-044. _Byline: Claude Code · Opus 4.8 · 2026-08-10._ |
| D-044 | **ADR-0048 status corrected PROPOSED → ACCEPTED/REALIZED · Takeout family PARKED as one unit · SBV "shadow" doc-drift swept · `SBV_PRIMARY_ENABLED` deleted** | B | done 2026-08-10 (owner rulings via structured Q&A + mid-turn confirmation) | (1) **ADR-0048 was NOT "awaiting sign-off"** — the architecture shipped in PR #18 (`aacf21c`, 08-06): `vendored/sbv/internal/{importer,engine,custody}.go` + **9** decoders (sms_xml, facebook_json, google_chat, google_voice_html, messaging_csv, messaging_html, messaging_txt, ndjson, email) with `universal_engine_test.go`. Status rewritten to ACCEPTED/REALIZED with the two halves stated separately. (2) **Owner confirmed 2026-08-10: "Timeline JSON is pushed out just the same way as Takeout, the rest of it complete"** — so the parking covers the whole **Google Takeout family**, not Timeline alone; ADR Scope + Consequences both annotated as parked-reference. `internal/google_timeline_importer.go` verified ABSENT. (3) **Doc-drift sweep on the lifted demotion**: the stale "SBV is shadow / pure-Python sms_xml is effective primary" claim was still asserted in ADR-0048, `server/evidence/workflows.py:590` docstring, and `docs/COORDINATION.md:184` — all three corrected (strike-through + dated note) against D-040 + live `_sbv_enabled()` + git order (`de1ca9f` demote 08-02 → `aacf21c` promote 08-06). The stale docstring had already misled a `/smart-explore` subagent into reporting shadow-status as fact. (4) **`SBV_PRIMARY_ENABLED` removed** from `compose.yaml` and `deploy/exec.yaml` (owner call) — dead knob, gates nothing, pinned by `tests/test_sbv_demotion.py`; tombstone comments left in place; safe to delete from the Coolify app env too. (5) Google Timeline corrected "⏳ Planned" → **PARKED** in `TOOL_CATALOG.md` + both copies of `js-mcp-server.md`. (6) `docs/planning/parser-iterations-inventory.md` FROZEN as a dated snapshot (its paths pre-date ADR-0035 subnamespacing). Verified: `tests/test_sbv_demotion.py` 12/12 pass, both YAMLs `safe_load` clean. rel: ADR-0048, D-040, DEBT.md item 0 (still open — ADR-0044 blob ban unenforced). _Byline: Claude Code · Opus 5 · 2026-08-10._ |
| D-042 | **Rulings sweep: ADR-0045 (amended) / 0046 / 0047 SIGNED · D-008 SIGNED · OQ-7/8/9/10 resolved** | A/B/C | done (owner rulings 2026-08-09, via structured Q&A) | (1) **ADR-0045 Accepted, Option A + A.4 amendment**: found-out knowledge in its own `working.realization_event` table (one event covers many records; contradiction events double as the lie register; algorithm proposes → owner approves in batches via native `@approval`); `visible_from` computed live = COALESCE(earliest approved realization, occurred_at); evidence spine never modified. (2) **ADR-0046 Accepted** (MCP exposure contract). (3) **ADR-0047 Accepted** (audit-everything ledger). (4) **D-008/RESTART-0001 evidence schema SIGNED as drafted** — S9 population unblocked. (5) **OQ-8**: tiers HITL-only with one exception — AI-chat context lane auto-asserts `hindsight` at write. (6) **OQ-10**: Milvus→Weaviate cutover ruled VERIFIED → pymilvus removed from Dockerfile (its grpcio pin was the requirements.txt conflict source). (7) **OQ-9**: Phase 5a verified SHIPPED — fork main == tag `v0.2.4-forensic`, image pinned in docker/tools/Dockerfile; COORDINATION.md note stale; residual: confirm heic tag state in v0.2.4. (8) **OQ-7**: 00_analysis_graph.surql reviewed — design (horizon as store-enforced permissions; agent can never widen its own horizon) already absorbed by ADR-0045/S6; archived to `_stale/`, `database/` dir retired. (9) **OQ-2**: `extracted-code/` LOCATED at `Projects/the-platform-workspace/extracted-code/` (repo sibling, one level up; `extracted-code.zip` backup beside it) — canon §9 + REPO_STRUCTURE repointed, TODO markers cleared. Rulings sheet now fully resolved. rel: ADR-0045/0046/0047, Dockerfile, docs/PROJECT_CANON.md §6+§9, docs/REPO_STRUCTURE.md. |
| D-043 | **S10 compose consolidation: deploy/ = one file per Coolify app** | C | done 2026-08-10 (owner approved mapping via structured Q&A; HANDOFF-2026-08-09-S10) | 17 compose files `git mv` root -> `deploy/<app>.yaml` (matching the deploy/data-weaviate.yaml pattern); 3 dead tiers (browser/ui/data — browser+ui killed, data superseded by the 5-way split) -> `_stale/*.SUPERSEDED`; `compose.data-surreal.yaml` stays at root with a PARKED header (not Coolify-managed, owner-gated deletion); root keeps only `compose.yaml` (local/dev). Verified against LIVE Coolify first (22 apps enumerated via API): 13 main-branch apps repointed (`docker_compose_location` PATCH) in the same operation, before push; branch-scoped apps (librechat*/nocodb -> infra/*, workbench -> workbench/sprint) initially deployed their own root paths. **2026-08-13 true-up:** before fast-forwarding `workbench/sprint`, its live Coolify app was repointed to `/deploy/workbench.yaml`, its watch paths were corrected, deployment commit `c4bda68` became healthy, and its report proxy returned the validation run 4/4 with Eastern display metadata. LibreChat/NocoDB branch pointers remain gated. Historical planning docs keep old paths as point-in-time records; live docs (REPO_STRUCTURE, COORDINATION, scripts/phase2_standup.sh) updated. Flagged for later: duplicate Coolify app registrations (librechat x2, librechat-mongo x2, nocodb x3), all exited. rel: HANDOFF-2026-08-09-S10, docs/REPO_STRUCTURE.md, scripts/phase2_standup.sh. _Byline: Claude Code · Fable 5 · 2026-08-10; true-up Codex · GPT-5 · 2026-08-13._ |
| D-041 | **NEVER multi-case, NEVER multi-user** | A | done (owner ruling 2026-08-09) | Owner ruling: single case, single operator, permanently — stop designing for the other future. Consequences applied same day: case_id stays TEXT `'primary'` (no unification migration; legacy UUID case_id columns are historical — closes OQ-5); multi-user auth stripped from every plan segment; single `horizon_agent` DB role; ops.audit_ledger (ADR-0047) single-operator by design. Also formally voids the "revisit when multi-case/multi-user" items from the 2026-08-09 horizon system design. rel: ADR-0045, ADR-0047, canon §9 case_id note. |
| D-040 | **SBV promoted from demoted-shadow to owner-chosen PRIMARY SMS-XML parser** | B | done (2026-08-05, PR #18; entry backfilled 2026-08-09) | The 2026-08-02 demotion (below) set an explicit restore condition: import-scoped reads. `feat/sbv-universal-parser` built exactly that (import_id binding on every row, H1/H2/H3 canon checks, claimed = accepted + rejected + accounted-duplicate reconciliation, rejection fetch) and merged to main 2026-08-05/06 (`aacf21c`, PR #18). `_sbv_enabled()` now gates only on SBV_SERVICE_PASS — no SBV_PRIMARY_ENABLED override. tests/test_sbv_demotion.py rewritten 2026-08-09 to pin the promotion contract + re-pin the forensic guarantees (bodyless-media retention, outbound roles 2/4/5/6) against `_map_universal_record` AND sms_xml.py; a role-derivation bug on the universal path (owner messages misattributed to the counterparty when SBV metadata lacks a role key) was found and fixed in that pass. rel: server/tools/parsers/messaging/sbv_sms.py, tests/test_sbv_demotion.py, docs/DEBT.md parser-lane item 1 (LANDED). |
| D-039 | **Six-clock ruling RATIFIED; horizon clock = visible_from** | B | done; source-clock formula superseded by D-065 | The 2026-08-01 design conversation (docs/COMPACT-SUMMARY-2026-08-01.md) established the clock family sql/0008 implements, but no DECISION_LOG/ADR entry existed — which is how sql/0018 later keyed the horizon predicate on the superseded `knowledge_time` with nothing citable to collide against (audit findings N1/FA). ADR-0045 originally fixed the horizon clock as ~~**visible_from = COALESCE(realized_at, occurred_at); acquired_at = custody metadata only**~~. **D-065/ADR-0059 supersedes that universal formula: source availability equals occurrence for first-party messages and acquisition for acquired-third-party messages; realization remains zero-to-many linked knowledge.** `knowledge_time` remains frozen as row-write audit time. rel: ADR-0045, ADR-0059, D-065, sql/0008, sql/0018. _Supersession note: Codex · GPT-5 · 2026-08-18._ |

## 2026-08-02

| # | Decision | Lane | Status | Rationale / notes |
|---|---|---|---|---|
| D-032 | **Studio's model picker is fed by the AgentOS `Registry`, NOT `GET /models`** | C | done | Owner-visible bug: the os.agno.com Studio picker offered one model. Root cause read from agno source (`agno/os/router.py::get_models`, byte-identical in 2.8.0 and 2.8.6): `/models` returns the DISTINCT models already **attached** to registered agents/teams — a usage report, not a catalog. All 6 agents + 3 teams share the one object `build_model()` returns, so the set collapses to 1. There is no config path into it, and growing it would mean attaching decoy agents to the roster the user sees. Docs give the supported path instead: Studio's agent builder shows "Model: select from registered models" (`/agent-os/studio/agents`) and the `Registry` is documented as the home for "model provider instances … that Studio depends on" (`/agent-os/studio/registry`), served at `GET /registry?resource_type=model`. So `AgentOS(registry=Registry(models=…))` is now built from the SAME verified list `AgentOSConfig.available_models` publishes (`server/api/config.yaml`, 37 probe-verified ids from `scripts/update_available_models.py`) — one source of truth, no roster pollution. New `server/core/model_registry.py`; `build_model()`/`_try_provider()`/`_model_id()` gained an optional exact-`model_id` argument (selection behaviour unchanged when omitted). **`/models` still returns 1 by design** — that is agno's documented semantics for that endpoint, not a residual bug. rel: `server/core/model_registry.py`, `server/api/main.py`, `tests/test_model_registry.py`. |

## 2026-07-29

| # | Decision | Lane | Status | Rationale / notes |
|---|---|---|---|---|
| D-031 | **ADRs 0036–0039 ACCEPTED** (DozerDB multi-DB isolation · Graphiti-MCP-via-ContextForge write-enabled · agents use graphiti-core natively · hosted structured-output extraction LLM) | C | done (acceptance; execution items open) | Owner "let's do it" 2026-07-29. Reality-checked before flipping: 0037's Streamable-HTTP blocker cleared by lived evidence (graphiti virtual server serving in ContextForge since ≤07-10, see D-028); 0039 already implemented in practice (nemotron guided-JSON 07-04, lane now Portkey per ADR-0042); 0038 consistent with ADR-0041's Agno-native orchestration ruling. Open execution items: DozerDB named-DB write verification + per-DB backups (0036), verify write surface + retire `:8071` no-auth door (0037). rel: ADR-0036..0039, canon §5 updated same change. |
| D-030 | **LiteLLM RETIRED — Portkey = THE model gateway** | C | done (docs; teardown pending) | Owner ruling 2026-07-29 (doc-patch pass). Portkey has carried Graphiti + exec-tier since 07-19 (11-provider failover, `docker/gateway/portkey/`); dual-gateway split-brain ends. Teardown of the LiteLLM container + OpenCode model-config remap = separate owner-gated task. → author ADR-0042 (created same change). Same pass also synced canon §5 to ADR-0040 (Weaviate locked, Milvus sidelined) + ADR-0041 (Memgraph additive), fixed agno 2.6.13→2.8.0 current-state refs (AGENTS.md, CONVENTIONS.md, canon §8), and backfilled ADR index rows 0036–0041. rel: ADR-0042, ADR-0040, ADR-0041. |

## 2026-07-11 (merged 2026-08-05 from `docs/agno-memory-expertise`)

> Renumbered on merge: this branch minted its own **D-030..D-035** while `main`
> independently used those numbers for different decisions, so every entry below
> shifted **+3** (D-030->D-033 ... D-035->D-038). Content is otherwise unchanged.
> Where a decision has since been overtaken it is struck in place, not removed.

| # | Decision | Lane | Status | Rationale / notes |
|---|---|---|---|---|
| D-038 | **Key/provider rotation = Portkey (already deployed :8787 behind ContextForge); the two candidate Nexus repos are NOT adopted for production** | C | decided (owner) | Owner (2026-07-11): "I think Portkey will do that." Portkey is already live on :8787 behind the ContextForge gateway (per infra ledger) and provides the wanted behavior: rotate KEYS/PROVIDERS behind a FIXED embedding model (can't rotate embedders — mixed dims hard-error; CAN rotate keys/providers for the same model). Research (`nexus-gateway-notes.md`) found both owner-named repos too young/untrusted to hold production keys: `Ranatoasted571/nexus-proxy` does NOT proxy `/v1/embeddings` at all (chat/completions only — disqualifying); `AlphaBitCore/nexus-gateway` CLAIMS an `/v1/embeddings` route but is unverified, ships a TLS-intercepting compliance proxy + desktop agent + seeded default admin creds — not worth the trust exposure when Portkey already solves it. LiteLLM remains the live fallback (carries the Graphiti `embed-text` nv-embed-v1 4096-d lane). Nexus thread CLOSED. rel: D-010 (embed-text dim-lock), portkey-gateway-later memory (deferral now resolved — Portkey is the answer). |
| D-037 | **Ingest sequencing + two-tier normalization: (a) hash→land needs NO embeddings and NO chunking; (b) full-text search is the FIRST retrieval layer; (c) chunking follows LATER on the same per-domain split; (d) DuckDB is embedded IN Postgres (pg_duckdb) — AI-chat + KB content land there WITHOUT the heavy forensic normalization that evidence/messages require** | B | decided (owner) | Owner (2026-07-11): "we don't necessarily need the embedding functioning right off the bat… it's going to be saved in DuckDB… even the chunking isn't right away, we're doing full-text search to begin with while we tweak the chunking, which will follow the same kind of domain split"; + "DuckDB is embedded in PG. AI chats and knowledge for the knowledge base don't necessarily need to be normalized the same way that text messages and different things need to be in PG — they can just live in DuckDB." **KEY DISTINCTION — DuckDB is NOT a separate store**: it's the `pg_duckdb` engine inside the same Postgres 18 instance (verified in stack). So "land in DuckDB" and "land in PG" are the same DB — the difference is table format + normalization rigor. **Two data classes:** (1) EVIDENCE / text messages / forensic records → fully normalized into PG relational `source.*` tables with custody hashes (H1/H2/H3), bitemporal columns, HITL identity resolution (RESTART-0001/0002 — the court-defensible path); (2) AI-CHAT TRANSCRIPTS + KB content → land in DuckDB columnar (can read the R2 parquet lake), light touch, NOT force-fit into the normalized forensic schema. **Sequencing (both classes):** ingest = parse → custody hash → land (no vectors, no chunks) → full-text search available immediately (PG tsvector / DuckDB FTS — engine TBD at build) → chunking (hybrid semantic+fixed, `docs/planning/agno-chunking-strategy.md`) tuned afterward, applying the SAME legal/timeline/code/general split as the D-036 KB collections → embed per-domain last. This DECOUPLES parser-tables/RESTART-0001 from the still-open embedder bench + KB-substrate sparse build (D-034) + gateway work — ingest is now on its own track. Still gated: owner sign-off on the RESTART-0001 schema + HITL identity prompt before touching live PG. rel: D-034 (real-sparse rides KB ingest, AFTER this), D-035 (Surreal story-layer reopens once this produces hashed rows), D-036 (per-domain split reused for chunking). |
| D-036 | **Topic 4 — per-domain KBs: collection per domain (kb_legal / kb_timeline / kb_code / kb_general) with SPECIALIZED embedders; case_id partition keys from day one; 'symmetric-only' DEMOTED from rule to conditional** | B | decided (owner) | Domains + embedder directions (owner-specified): legal → a legal-TRAINED embedder (candidate search required; asymmetric models now IN PLAY — see correction); timeline/history → MULTIMODAL embedder (Gemini or Nemo/nemotron candidates — timeline carries photos/media); code → codestral-embed + tree-sitter chunking; general → likely bge-m3. Collection-per-domain (agno-validated pattern; per-domain dims possible); partition keys wired into kb_legal + kb_timeline schemas AT CREATION (cheap now, painful retrofit) — OWNER CORRECTION: NOT case_id (there is exactly ONE case — this is a personal single-case platform, usage-scope personal); the partition dimension will be something useful WITHIN the one case (source-type / party / time-period — chosen at KB-build design with owner). OWNER CORRECTION recorded: "symmetric-only" was never a rule — it was the workaround for one client (NIM embedqa) that couldn't pass `input_type` per call; with a proper integration path asymmetric embedders are fine. OPEN SUB-TASK (gates collection creation — dims lock): short embedder-candidate bench for the legal + multimodal lanes. Chunking per domain already decided (docs/planning/agno-chunking-strategy.md). rel: D-034 (real-sparse rides bge-m3 lanes). |
| D-035 | **Topic 3 (Surreal consolidation/story space) — design TEMPORARILY DEFERRED by owner** | B | deferred, on the board | Owner (2026-07-11): "not worried about it yet — once we get it normalized, into the databases, hashed, and run against Semantica + entity extraction + sentiment, THEN we'll figure it out." Write-path pattern (sole gated writer vs HITL tools vs batch job) explicitly undecided ("idk yet"). PRECONDITIONS before this topic reopens: RESTART-0001 ingest live → custody hashing on rows → Semantica/entity/sentiment analysis producing real outputs. Surreal's consolidation-space ROLE remains owner-affirmed (see research log) — only the schema/design work is deferred. Reopen as its own ADR with real data in hand. |
| D-034 | ~~**KB substrate = Milvus CONFIRMED (ADR-0027 holds); sparse lane gets the REAL fix (in-repo)**~~ **SUPERSEDED by ADR-0040 (2026-07-27): Weaviate is the locked vector substrate; Milvus is sidelined.** Recorded here unchanged for provenance -- it was true when decided (2026-07-11) and the reasoning about agno's hashed-TF-IDF fake-sparse lane is still the reason a real sparse lane matters on whatever substrate wins. | B | ~~decided~~ superseded (Topic 2, owner-decided 2026-07-11; overtaken 2026-07-27) | SurrealDB does NOT take the KB role — it keeps operational store + the Topic-3 consolidation/story space. The Surreal-retriever challenger is closed (not piloted). Sparse: agno's Milvus client fakes the sparse half with hashed TF-IDF; Milvus 3.0 supports REAL sparse (`SPARSE_FLOAT_VECTOR` GA, BM25 server-side function, or true BGE-M3 sparse — our text embedder already emits both lanes). Decision: build an in-repo custom insert/search path for genuine dense+sparse hybrid (no upstream PR for now). **Sequencing: build lands with the KB-ingest work AFTER Topic 4** (per-domain collection/partition design) to avoid schema rework — tracked in COORDINATION TODO so it cannot slip. Evidence: `topic-2-kb-substrate-brief.md`, files 02/05, owner-supplied milvus.io/docs/sparse_vector.md. |
| D-033 | **LearningMachine persists to PostgresDb (not SurrealDb); entity_memory fix TEMPORARILY DEFERRED to Topics 5/6; decision_log enabled (ALWAYS)** | B | done (Topic 1 of the memory/storage decision agenda, owner-decided) | agno 2.6.13's SurrealDb backend stubs all four learning methods (`NotImplementedError`, `agno/db/surrealdb/surrealdb.py:1990-2034`) — `user_profile`/`user_memory`/`session_context`/`entity_memory` were silent no-ops in production; only `learned_knowledge` worked (bypasses db via Knowledge/Milvus). Fix: `build_learning()` now uses `get_postgres_db()` (restores the documented ADR-0004 intent; smallest diff, no agno patch ownership); sessions/chat-history STAY on SurrealDb (those roles work there); Surreal's consolidation-space role untouched. `entity_memory`: the fix is **temporarily deferred, NOT dropped** — agno silently degrades PROPOSE→ALWAYS (no genuine HITL) and entities get their real home via Semantica + Graphiti custom entity types; it returns in Topics 5/6 with a genuine gate. `decision_log` store enabled in ALWAYS mode (its only active mode) as the durable agent-decision audit lane. Owner also set a wording rule: never label deferred work "disabled" — that's how things get forgotten; say "temporarily deferred". Evidence: `docs/reference/agno-memory-and-storage/07-platform-mapping.md` §A.3/§D-Topic-1, `01-memory-and-learning.md`. |

## 2026-07-10

| # | Decision | Lane | Status | Rationale / notes |
|---|---|---|---|---|
| D-029 | **AGENTS.md progressive-disclosure reconfiguration** — root `AGENTS.md` rewritten as a concise map of the real `server/*` layout + 5 nested `AGENTS.md` drill-downs (`server/`, `server/tools/`, `server/evidence/`, `server/agents/`, `server/contracts/`) | — | done | Root `AGENTS.md` still described the pre-ADR-0033 flat-package layout (`agents/`, `app/`, `db/`, `evidence/`) and promised per-directory `README.md` files that never existed — the progressive disclosure it advertised didn't exist. Closest-file-wins nesting now backs that promise for real. Doc-only, gates re-verified green. |
| D-028 | **Facade-collapse premise DISPROVEN — the facade STAYS; Batches B/C are MOOT** | A/C | corrected | `docs/planning/facade-collapse-plan.md`'s core premise — that `agno`'s `enable_mcp_server` re-exports granular `@tool` functions over `agentos-mcp`, letting ContextForge repoint there and the facade be removed — is false: verified from agno source (`agno/os/app.py:588-595`), AgentOS's MCP surface exposes only ~19 AgentOS *operations*, never the parser/SBV `@tool`s. Batch A (G4 gateway + SBV toolkit as agno `@tool`s) shipped anyway (useful on its own); Batches B/C do not proceed. All 14 facade tools instead registered directly in ContextForge as REST tools (5th virtual server `platform_tools`, alongside `agno`/`coolify`/`graphiti`/`exa`). rel: `docs/planning/facade-collapse-plan.md` (superseded banner), `docs/COORDINATION.md` FACADE COLLAPSE entry. |
| D-027 | **ADR-0035 Option A — record contract's home is `server/contracts/records.py`, not `server/core/`** | A | done | Owner initially picked "promote to `server/core/records.py`" (the literal reading of "promote out of evidence"), but `server/core/__init__.py` eagerly imports `server.core.session` (sqlalchemy/agno/duckdb) — routing the record contract through it would FATAL-loop the dep-light `docker/tools` facade the moment any parser imports it (the same failure class as the 2-day ADR-0033-era outage). `server/contracts/` is a new, deliberately import-light package created to be facade-safe by construction; `server/contracts/__init__.py` stays dependency-free. `server/evidence/normalize.py` kept as a deprecated re-export shim (nothing deleted). Also executed same-ADR: `server/evidence/tool_finder/` → `server/tools/gateway/`; `server/tools/` sub-namespaced into `parsers/{messaging,ai_chat,generic}/` + `extractors/`; registry discovery switched `pkgutil.iter_modules` → `pkgutil.walk_packages` (recursive). Merged `main` (`8240205`), deployed, verified (facade `/health` 23 tools). Gates green: ruff/mypy/pytest 208. rel: ADR-0035 (supersedes/relates ADR-0033). |

## 2026-07-09

| # | Decision | Lane | Status | Rationale / notes |
|---|---|---|---|---|
| D-026 | **tools + registry moved out of evidence to `server/tools/` (cross-domain capability layer)** | A | done | Tools are cross-domain: evidence/analysis/agents/workflows/CLI all consume them, so they don't belong nested under the evidence spine. `git mv server/evidence/tools server/tools`; `git mv server/evidence/registry.py server/tools/registry.py`; ~150 import-statement + string-path substitutions across `.py`/tests/facade; the two near-identical auto-discovery loops in `registry.py` collapsed into one, made package-name-agnostic (`__package__`, not hardcoded); intra-package imports (`registry`, `_common`, `_chatminer_adapter`, sibling parsers) converted to relative imports. Also fixed a live mount regression: `compose.yaml`/`compose.exec.yaml` mounted `./evidence:/opt/tools/evidence:ro` (a dir that no longer exists post-ADR-0033, so the tools facade served zero parsers) → now mounts the WHOLE `server/` tree (`./server:/opt/tools/server:ro`, not just `server/tools/`, because `server.tools.*` has real transitive deps on `server.evidence.normalize` + `server.vendored.chatminer`, both lightweight); `docker/tools/tools/facade.py` imports plain `server.tools.registry`/`server.tools._sbv_client`, same path as the main app. Verified (outside the repo's own venv, which has `server` editable-installed and would mask this) with an isolated-Python simulation of the container's actual import graph — `load_builtin_tools()` returns all 23 tools. Gates GREEN: ruff clean, mypy clean, **pytest 186**. rel: ADR-0033. |
| D-025 | **Repo repack = Option A** (full `server/{api,core,agents,evidence,analysis,vendored/chatminer}` + `ui/` + `shared/`) | A | **EXECUTED on branch, gated** | Mirror the prior iteration's one-backend-boundary discipline; current 8 flat sibling packages are historical drift. Alternative B rejected. **ADR-0033 authored.** Done 2026-07-09 via `scripts/repack_to_server_layout.py` (152 files, 240 import rewrites, path-depth + string-module + config-split fixes); gates GREEN: ruff clean, mypy 106 files, **pytest 186**. `podman build` proof + merge DEFERRED (owner to config podman later; merge auto-deploys exec tier). `REPO_STRUCTURE.md` updated. |
| D-024 | **Repack runs as a one-window RUNBOOK, not a branch/drive-by** | A | standing | Merging `main` auto-deploys the exec tier (D-011); repack moves ~200 import sites + the uvicorn entrypoint + Dockerfile COPY paths, so it needs a keyboard-present window with a local `docker build` proof and Lane C watching the redeploy. A long-lived branch would rot against B's `.py` + C's `docker/` edits. |
| D-023 | **UI / G1 DEFERRED; decoupled from the repack** | A | decided | Don't race the CopilotKit shell. Repack proceeds on its own coordinated schedule; G1 is no longer sequenced "before/after" it. |
| D-022 | **`shared/` deferred** — create only when `ui/` needs shared types | A | decided | Consistent with deferring UI; avoid a speculative empty package. |
| D-021 | **`visualizations/` → `docs/visualizations/`** | A | **done** | Q1. Not a product surface; belongs with docs. |
| D-020 | **`configs/` → `docker/milvus/`, `deploy/n8n/` → `docker/n8n/`** | A | **done** | Q3. DEPLOY-NEUTRAL: compose mounts Milvus configs from absolute VPS host paths (`/data/agno/config/milvus/…`); repo copy is only the scp source (comment repointed). ⚠ Lane C to confirm n8n isn't deployed from the old path. rel: ADR-0007 (n8n+R2), ADR-0026/0027 (Milvus). |
| D-019 | **Process rule:** an open question leaves the annotate list the moment it's acted on | A | standing | Fix for the `.planning/build` / venv ghost-question confusion (owner: "you asked me to review a folder you already moved"). Spec split into 4a DONE / 4b genuinely-open. |
| D-018 | **`.planning/build/` = LIVE architecture directives** → `docs/planning/architecture-directives/` (+ INDEX.md) | A | done | Owner: "most of that was good directives." ContextForge/SurrealDB/DNS/Traefik/topology; reconcile against live infra, do NOT `_stale`. |
| D-017 | **Multi-chat war room** = `docs/COORDINATION.md` (Lane A/B/C, append-only ledger) | A | standing | Three chats work the repo concurrently; shared ledger prevents collisions. |

## 2026-07-08

| # | Decision | Lane | Status | Rationale / notes |
|---|---|---|---|---|
| D-016 | **Seed reconciliation RESOLVED — no action** | A | closed | Live ontology (164 cat / 527 pat) == exact `0007` prefix of the committed chain `0006+0007+0008`; `evidence/patterns.py` OntologyChain validator OK; P2.1 corpus fully homed (0 missing). Earlier "drift 153→164" read used the wrong baseline (0006 alone) — withdrawn. |
| D-015 | **Ontology = a migration CHAIN validated by `evidence/patterns.py`**; P2.1 parallel-tables approach superseded | A/B | standing | Invariant: "live == a prefix of the committed chain" detects both data loss and uncaptured drift. 4 `contradiction_rules` remain unhomed pending a table decision (B). rel: ADR-0018 (bitemporal evidence), ADR-0031 (entity layer). |
| D-014 | **Live PG data + ingestion/detection LOGIC FROZEN** until the Lane-B brainstorm lands | B | frozen | Structure work moves code only; behavior identical. |
| D-013 | **Sealed-lexicon rule:** committed seeds keep `[REDACTED:*]` placeholders only; real values load out-of-band, never git | A/B | standing | Court-safety (0006 rule). Read-only dumps → gitignored `live-dumps/`. rel: ADR-0018 (disclosure tier). |
| D-012 | **Tier 0/1 hygiene** — delete dead venvs (~577 MB, regenerable so never-delete N/A); recall fragments → `../_stale/`; `goals/`+`.planning/`+`plans/` → `docs/planning/` | A | done | — |
| D-011 | **exec-tier Coolify app deploys from `main`** (repointed from `hotfix/agent-ui-lockfile`) | C | standing | ⚠ Any merge to `main` auto-redeploys exec tier (+ webhooked coolify-mcp/portkey/data-* apps). Keep `docker/` config paths stable. rel: ADR-0009 (deploy on OVH), ADR-0016 (tool containers). |
| D-010 | **`embed-text` MUST stay `nvidia/nv-embed-v1` (4096-d symmetric)** | C | standing | Graphiti Neo4j graph is embedded at 4096-d; any dim change breaks vector search (bit us twice). Do not swap in asymmetric embedqa models. rel: ADR-0011 (NIM embedder dimension contract), ADR-0010 (two-collection embedding), ADR-0014 (Neo4j+Graphiti). |
| D-009 | **Repointing a Coolify app hotfix→main surfaces every hotfix-only file main lacked** (audit first) | C | lesson | Seven hotfix-only pieces (agent-ui Dockerfile, `app/mcp_main.py`, agentos-mcp compose svc, `fastmcp` install, …) caused staged crashloops; also the FastMCP host-header 421 bug (same as graphiti). |

## Carried context (decided in prior sessions / other lanes)

| # | Decision | Lane | Notes |
|---|---|---|---|
| D-008 | **DB schema RESTART**; hash placement = **Option A** (h1/h2/h3 custody hashes as COLUMNS per row) | B | Initial ingestion schema dead; 6 per-source RAW tables + `file_custody` anchor; ingest lands raw, no transform. DRAFT until owner approves live. rel: ADR-0018 (bitemporal/custody), ADR-0029 (dedicated ingestion). |
| D-007 | **Prior GitHub iterations are the design SoT** — honor, don't re-invent | B | Iterations index → `D:/casebible/iterations_index.duckdb` (~2080 artifacts). |
| D-006 | **ContextForge = the MCP tool gateway; LiteLLM/Portkey = the model gateway** (distinct layers) | C | CF v1.0.4 live; 4 virtual servers (agno/coolify/graphiti/exa); coolify-write is a separate write cluster from the hosted read-only bundle. rel: ADR-0025 (gateway topology), ADR-0023 (universal API+MCP), ADR-0015 (LiteLLM). |
| D-005 | **Swarm network REJECTED** by owner | C | (was floated during the CF upgrade chat.) |
| D-004 | **Auto-memory canonical store = the PARENT workspace dir**; teleport/subdir sessions sync up first | — | `memory-parent-dir-canonical`. Subdir `MEMORY.md` is a redirect pointer. |
| D-003 | **TheBigOne = donor dump** (prior iterations + un-integrated tools/schemas) — mine, never build there | — | — |
| D-002 | **Never trim memory lossily** to hit a size target — merge preserving all facts/refs | — | Owner rule. |
| D-001 | **Rotate the Cloudflare GLOBAL API key** (leaked in old repos, redacted 2026-07-04) | owner | ⏳ OPEN — only the owner can rotate. |

---

**ADR convention (owner rule — always note when applicable):** every entry is checked against
`docs/adr/`. Use `rel: ADR-NNNN` for an existing ADR that governs/relates to the decision;
use `→ author ADR-NNNN` when the decision is architecturally significant and needs its own ADR
(then create it and keep the ref). A decision with no applicable ADR is fine — but the check
is mandatory, not optional.

## 2026-08-02 — H3 chain tags disambiguated (hashing audit finding 2)
Two valid H3 constructions shared the tag `h3-chain-v1`: the SBV Go fold
(genesis `""`, `sha256(prev + "\n" + h2)`) and the Case Bible chain (genesis
`H1`, `sha256(prev_hex + h2_hex)`, 1,918 links live-verified). New rows
written by `server/evidence/custody.py` now carry
`h3-chain-sbv-genesisempty-v1`; the Case Bible writer (out-of-repo, case-bible
vault tooling) should adopt `h3-chain-h1genesis-v1`. **Crosswalk:** rows
tagged `h3-chain-v1` predate this decision and are disambiguated by WRITER
(SBV import batches → SBV construction; Case Bible vault → H1-genesis), never
by relabelling — recorded custody rows are append-only. _Byline: Claude Code ·
Fable 5 · 2026-08-02._

## 2026-08-02 — SBV demoted from forensic-primary to shadow (gap-review P0)
The parser-gap review (docs/HANDOFF-2026-08-02-sbv-chatminer-parser-gap-review.md)
found the SBV adapter reads `GET /api/activity` after upload — the service
account's ENTIRE persistent corpus, not the new import — so a second upload can
attribute earlier records to the new artifact's custody event (false
provenance). `accept()` now also requires `SBV_PRIMARY_ENABLED` (default
unset); `messages.sms-xml` (pure-Python) is the effective primary and prod
flips to it on next exec-tier deploy (intended). SBV stays callable by id for
shadow/diagnostic runs. Its mapper was fixed in the same commit (bodyless
retention + outbound role types 2/4/5/6) so shadow output is comparison-grade.
**Restore conditions** = the review's acceptance criteria: upload returns an
immutable import_id, activity reads scoped to it, primary/fallback equivalence
on a golden corpus, mandatory custody binding. _Byline: Claude Code · Fable 5 ·
2026-08-02._

## 2026-08-02 — PG moved to ovh-files (wave 1 of the ovh-data retirement)
Platform PostgreSQL now runs on ovh-files as Coolify app `data-pg-files`
(PG 18.1, pg_duckdb+postgis+pgvector, BIND_IP 100.91.190.107:5432). All four
databases transferred (pg_dumpall) and count-verified; exec-tier repointed via
the new `PG_HOST` env (`DB_HOST: ${PG_HOST:-${OVH3_HOST}}`); live API verified
connected to the new host with zero clients left on the old; old `data-pg` app
STOPPED, never deleted (datadir intact at /data/agno/volumes/pgdata).
Remaining on ovh-data for waves 2-4: SurrealDB, Weaviate, Neo4j-dev + graphiti
trio. _Byline: Claude Code · Fable 5 · 2026-08-02._

| D-108 | 2026-08-30 | Claude · Opus 5 | **The migration chain alone cannot build this database, and never could.** `evidence.source` (124 code refs), `working.normalized_record` (145 refs), and `working.message` are created by NO numbered migration — only by `sql/bootstrap/schema_baseline.sql`, a pg_dump from 2026-08-10. Every attempt tonight to build from `0001` forward failed for this reason, as did Codex's on 2026-08-29. The canonical build is **baseline first, then all numbered migrations on top**. Recorded in `ops.migration_ledger` under `migration_id='baseline'`. | applied |
| D-109 | 2026-08-30 | Claude · Opus 5 | **`public.schema_version` is NOT a migration ledger.** Its status vocabulary is `active/superseded/deprecated` and its columns are `applies_to`/`ddl_uri`/`supersedes` — it is a data-contract version table that merely resembles a ledger. That resemblance destroyed migration state on 2026-08-29 (a `CREATE DATABASE ... TEMPLATE ai` inherited its rows) and cost ~7 hours of reconstruction. **`ops.migration_ledger` is now THE ledger** (sql/0055 PART 5), carrying per-migration SHA-256 so a file changed after application is detectable. Do not conflate them again. | applied |
| D-110 | 2026-08-30 | Owner (ruled ≥5×) | **Nothing is immutable. Immutability applies ONLY to promoted evidence, behind a development feature flag, and no evidence has been promoted.** The 2026-08-30 rebuild deliberately skipped 253 guard statements — ~50 functions and ~75 triggers. This is intentional, is NOT technical debt, and must not be reported as a defect. Replaying the procedural half of migrations 0035–0054 is the precondition for flipping the dev flag, not an outstanding repair. **Do not ask about this again.** | applied |
| D-111 | 2026-08-30 | Owner | **Quarantine is a FILE rule, not a table rule.** Files that cannot be recreated are quarantined, never deleted. Empty or superseded TABLES are deleted outright — they are recreatable from migrations and quarantine schemas clutter comprehension. `quarantine_20260830` (5 empty superseded tables) was dropped under this ruling. | applied |
| D-112 | 2026-08-30 | Claude · Opus 5 | **Lost SAT/Semantica implementation recovered from bytecode.** Five `.pyc` files compiled 2026-08-27 survived with no `.py` and were never committed. They contained the `GraphRagLane` discriminator (`semantica`/`sat_temporal`) and the full SQL for 7 `analysis.graphrag_*` tables plus `seal_graphrag_manifest`. Preserved to `docs/recovered/` and reconstructed as schema in `sql/0055` PART 1. A `git clean` or Python version bump would have destroyed the last copy. | applied |
| D-113 | 2026-08-30 | Claude · Opus 5 | **A schema prune must also edit `sql/bootstrap/schema_baseline.sql`.** Five superseded tables quarantined earlier on 2026-08-30 were resurrected by that evening's rebuild because they live in the baseline dump. Any prune applied only to the live database is undone by the next rebuild. | applied |

| D-114 | 2026-08-31 | Owner ("This seems logical") | **One universal change-proposal spine, not per-domain candidate families.** Anything that changes canon — AI extractor suggestions, Workbench/UIW edits, Semantica-vs-SAT graph reconciliation, agent output, human correction, metadata correction, promotion — files a proposal in `canon.change_proposal`; a human (or a logged auto_adopt tier) rules; adoption snapshots prior values, bumps the table generation, and enqueues `canon.recompute_queue` rows so downstream derived work recalculates. Existing typed candidate tables stay for NEW-thing extraction; the spine governs changes to canon and adoption. Risk tiers live as data (`canon.change_tier`) because the review valve is one human. Staleness guard: a proposal reasoned against a superseded generation requeues, never applies. sql/0056. NOT DONE until a minimal review surface and one recompute consumer exist — the 0024 outbox died consumer-less and this must not. | applied |

| D-115 | 2026-08-31 | Owner | **Identity lives in `reference`, cumulative and atomic from the very beginning.** Owner verbatim: "the identity tables... can be cumulative and atomic and just be referential... we could keep it from the test ingest... The more completed those are from the very beginning, the less work there is to do down the road." Eight tables moved by 0057: device, person, entity, entity_alias, location, id_xref, court_case, matter. Identity discovered during TEST ingests accumulates and survives test-data purges. evidence→working backwards FKs: 7→0. Code: 11 files retargeted, zero stale refs. timeline_event deliberately not moved (conclusion vs identity — separate ruling). | applied |

| D-116 | 2026-08-31 | Owner (blueprint reviewed, method-confirmed) | **The reckoning (sql/0058): 58 tables deleted, the three vocabulary wars ended, two schemas re-chartered.** Rulings now permanent: "conversation" = per-source, "thread" = cross-platform human (winner: first/third_party_context_thread; working.conversation deleted). Chunk = working.content_chunk only, hash column content_sha256 (chat_chunk family of 8 deleted; content_chunk_projection created as successor for fresh-ingest projection state — deliberately separate from canon.recompute_queue, which handles re-projection after approved changes). Review = the canon spine only (19-table context_review_* apparatus deleted). raw schema created: verbatim landing (13 tables moved from evidence). evidence = PROMOTED material with custody; analysis.evidence_item moved there as THE promotion target. Litigation loop confirmed pre-existing and kept: finding → evidence_task → evidence_item → factor_citation → legal_timeline_event; weaker working.investigation_* duplicate deleted. Every delete empty-verified at apply; every deleted table's PROCESS has a named successor or was confirmed duplicate. 268 tables. | applied |

| D-117 | 2026-08-31 | Owner | **Wrong ≠ wipeable: identity survives purges but nothing in it is trusted until the owner rules.** "Waiting for approval" is a COLUMN, not a schema address. `verification_state` (proposed/confirmed/disputed, default proposed) added to device, person, location, court_case, matter (sql/0059). All 8 identity tables + evidence_item registered in `canon.canonical_table` — a misattributed pile ("you decide it's from the wrong device, it needs to be fixed") is corrected via change_proposal → owner ruling → prior values snapshotted → recompute, never by direct edit and never by wiping the registry. court_case/matter/evidence_item are explicit_review; nothing about them is ever auto-anything. | applied |

| D-118 | 2026-08-31 | Owner | **The reset button exists and is the ONLY sanctioned purge.** "The only time things get purged is during testing, so that we start fresh." `ops.reset_test_data('RESET')` (sql/0060): truncates raw/evidence/working/context/timeline/analysis + canon traffic, resets generations to 0; PRESERVES reference (identity is cumulative, incl. test-ingest discoveries), canon rulings (boundary + tiers), ops ledgers, public, ai. Refuses without the literal confirmation string. Live-tested: 191 tables truncated, 8 boundary rows + 3 tiers + 55 ledger rows survived. Ad-hoc teardowns are done. | applied |
| D-119 | 2026-08-31 | Owner | **Confidence decides the lane.** "Super clear signal — awesome. Sketchy signal — waits for approval." `identification_confidence` + `identification_signal` on device/person/location (sql/0061); `auto_confirm_min_confidence` as DATA on canon.canonical_table (0.95 for identity, NULL = never-auto for court_case/matter/evidence_item); `canon.route_tier()` routes: >= threshold → auto_adopt (logged, reversible), below or unknown → waits. Live-tested all four paths. | applied |

| D-120 | 2026-08-31 | Owner (method ruling) | **Zero-rows and no-code-references are INVALID necessity evidence while the pipeline has never run.** Owner: emptiness is circular ("you can't get ingest right, so how do we make it to analysis"), and a missing caller is ambiguous between unnecessary-table and unwritten-code — "is it the caller that's missing or the table that's unnecessary? Where is the gap?" The tiebreaker is the CONVERSATION THAT CREATED THE TABLE. Origin trace of all 61 accused tables: 60 PURPOSE-ALIVE (documented design intent in migration headers/ADRs/forensic-db-reconciliation docs — they move to the BUILD list), 10 of those PARKED per D-044, 0 never-established, 2 genuinely superseded and retired by sql/0063 (public.agent_run, public.approval_request — sql/0002's own header: "no code writes here anymore"). The flawed method's source identified: docs/schema/ai-schema-reckoning.html justified drops by zero-rows + not-on-layer-map. Near-casualties: canon_registry (4 live rows of custody-hash governance), transcript_insight (Phase 10 landing), change_log (live hash-chain trigger). | applied |

| D-121 | 2026-08-31 | Owner | **The geo/location lane is real, its code comes later, and it does NOT sit in an unproven database.** "That is the use for those tables, the code will come later... I don't want to recreate them later so back them up... why am I gonna bring in the key to the application I've been working on even longer than this, just to have it fucked up too." All 10 tables (stay_point, gps_track, geocode_request/resolution/result, home_base, waypoint_device_split, vehicle, geofence, geocode_audit) backed up as complete one-file-restore DDL in sql/parked/geo_lane_parked_20260831.sql, RESTORE-PROVEN against the test database before removal, then dropped by sql/0064. The lane re-enters only after ingest is proven on lesser data. | applied |

| D-121 | 2026-08-31 | Owner | **Geo/location lane parked OUT of the live database until ingest is proven.** "That is the use for those tables, the code will come later... I don't want to recreate them later so back them up... why am I gonna bring in the key to the application I've been working on even longer than this, just to have it fucked up too." All 10 tables backed up as one-file restore (sql/parked/geo_lane_parked_20260831.sql, two-phase DDL, RESTORE-PROVEN 10/10 on a clean target before removal), then dropped by sql/0064. Recorded in AGENT_MEMORY.md so no future session treats their absence as a gap or rebuilds them ad hoc. | applied |

| D-122 | 2026-09-01 | Owner | **Coolify environment variables carry no secret values; rotation must never require a rebuild or redeploy.** "We were supposed to be moving away from env-based password management which involved a rebuild every time we change a password… we have multiple means of managing passwords — Authentik, Keycloak built into ContextForge, Portkey OAuth, or the database." Chosen mechanism (owner: "just go with option one, make it work"): **Infisical secrets broker** (`deploy/infisical.yaml`, own DB+role on the shared cluster, Dragonfly for Redis) issuing **dynamic short-lived PostgreSQL credentials** to services at process start (`server/core/url.py::lease_dynamic_db_credentials`); services authenticate with a machine identity whose client secret is the ONLY bootstrap secret they carry. Human auth = Authentik; service/MCP auth = OIDC client credentials via ContextForge/Keycloak once deployed; LLM provider keys = Portkey virtual keys; app-consumed third-party creds live in Infisical. **Target end-state recorded, not adopted yet:** PostgreSQL 18 native OAuth with Keycloak as issuer (validator `kc_validator`/`pg_oidc_validator` still pre-production, service clients need a libpq token hook) — revisit when Keycloak is live. Supersedes ADR-0030's "creds in Coolify" for secret VALUES. Static `DB_PASS` env survives only as an explicitly-logged bridge for broker outage. Recorded by Claude Code · Fable 5.1. rel: D-091, D-094, D-096, ADR-0030. |

| D-123 | 2026-09-01 | Owner | **SBV becomes the desktop-first ingest workspace — a Tauri-wrapped client composing the atomic Temporal Activities with preview gates.** While the Workbench stays web-only, the SBV client (Vite/React frontend, Tauri desktop shell) is the primary operator ingest surface, modeled on the Case Bible workspace discipline. Owner verbatim (2026-09-01): it is "a surface that would call the appropriate temporal io activities … such as hash. It would decide whether or not it can proceed straight to chunking rather than parsing; it could parse; it could extract; it could send it straight to DuckDB; and then view preview, chunk preview, commit … with a preview, as well as having a surface to input context that is not necessarily immediately available, or to modify or change or fill in missing metadata." Concretely: (1) the client calls the Go Temporal `UniversalImportWorkflow` Activities ATOMICALLY (hash first, always — the dedicated custody-hash Activity family per the 2026-08-25 boundary ruling); (2) per-file routing decision surfaces the D-051 detection router verdict — skip-to-chunk / parse (engine-dynamic D-049) / extract / structured-lane straight to pg_duckdb (`raw.*` targets, H-07); (3) EVERY stage gates on a preview (UIW preview projection, sql/0050) before commit — view preview → chunk preview → commit-with-preview; (4) the surface accepts operator-supplied context not available at ingest time and metadata fill-in/correction via the UIW source-context revision (sql/0053) and repair activity (sql/0051) paths — never direct edits. This EXTENDS ADR-0061 (interim amendment, same date), does NOT reverse it: SBV stays storage-free per the 2026-08-29 split ruling (no SQLite canon, no local auth, no bespoke ingestion); a confirmed preview still starts the same headless Temporal workflow; `engine/uiw` remains the only custody writer. Pulls forward D-055's two open desktop-route choices (SBV auth passthrough; desktop `/r2` mount vs PG-staged blobs) — still owner-open. The 2026-09-01-morning "temporarily web only, no tauri" statement applied to the WORKBENCH, not this client. rel: ADR-0048/0049/0052/0061, D-049, D-051, D-055, sql/0050/0051/0053, H-01/H-07. _Byline: Claude Code · Fable 5 · 2026-09-01._ |

| D-124 | 2026-09-02 | Owner (delegated: "What makes sense? What works? That is what it is.") | **"Four hashes" names the four hash MOMENTS in a record's lifecycle — never a fourth digest level.** (1) context fingerprint at intake (pre-parse, never H-named); (2) normalized digest at normalization; (3) custody H1/H2/H3 verification at governed promotion; (4) later evidence reverification (append-only). Grounded in the owner's own D-077 (2026-08-25), which defines exactly these four hash-Activity groups. Value taxonomy UNCHANGED: custody = 3 levels (H1 rawbytes / H2 rawelement / H3 chain fold, ADR-0034 + 2026-08-27 ruling), context fingerprints = 3 kinds, normalized digests = 2 kinds (context.hash_receipt carries 5 hash_kind values, matching D-087's five computations). No H4 exists; chunk content_sha256 remains a dedup key, never a custody construction. Closes the "which four?" open question in docs/reference/HASH-TAXONOMY-2026-08-29.md (updated same change). Reconciliation: 3 = custody levels · 4 = lifecycle hash moments · 5 = receipt hash kinds. rel: D-075, D-077, D-087, D-088, ADR-0034. _Byline: Claude Code · Fable 5 · 2026-09-02._ |

| D-125 | 2026-09-02 | Owner | **Dev-flag auth bypass for the ingest surfaces while the authorization system is stood up.** Owner (verbatim intent): "We are on a secure network — bypass all auth using a dev feature flag; change the flag once the authorization system is up." Implementation contract: one explicit env flag (`PLATFORM_DEV_AUTH_BYPASS=1`) honored by the UIW starter HTTP surface and the Workbench BFF; when set, bearer/Authentik checks are skipped and every service logs a loud startup warning naming the flag; default is OFF (fail-closed) — the bypass exists only where the flag is explicitly set in the deployment env. Scope rationale: every surface involved binds tailnet-only (BIND_IP pattern); single-owner platform; context-only ingest (D-069), custody untouched. The flag is REMOVED from deployment envs (not just unset) when Authentik/authorization goes live. rel: D-110 (dev-flag precedent), D-122/D-123, ADR-0061 auth invariants (temporarily flag-suspended, not repealed). _Byline: Claude Code · Fable 5 · 2026-09-02._ |

| D-126 | 2026-09-02 | Owner | **Pre-launch the case registry uses a forced, obviously-synthetic DEV identity behind the dev feature flag; real UUIDs are minted only at go-live.** Owner verbatim: "Until we go live, force an ID of devmode. Put it behind the dev feature flag. And then once we go live, it gets a new UUID." Context: the fresh `platform` DB minted `01a055b0-...` for Matter/CourtCase via a migration-0030 replay (2026-08-31, verification_state=proposed) while `uiw_schema_probe.go` and `sql/validation/0054_platform_case_registry_adoption.json` pin the 2026-08-23 identities `01a03136-...`. Owner ruling: neither accidental UUID becomes canon — both are meaningless serial numbers on placeholder rows ("Primary matter"/"Primary proceeding") with zero referencing data. Instead: (1) a fixed, visually-obvious DEV identity is forced pre-launch, titled so no one mistakes it for real case identity; (2) the UIW schema-admission identity + import-receipt checks are BYPASSED under the same dev flag as D-125 (`PLATFORM_DEV_AUTH_BYPASS`), with a loud startup warning naming the flag — every other admission check stays enforced; (3) **no fabricated owner-approval receipt is ever written** (an `approved_by='owner'` row the owner never approved would be a false record — the exact class of defect `docs/CLAIMED_COMPLETE_LIKELY_LIES/` exists to catch); (4) at go-live the dev flag is removed from deployment envs, test data is purged via `ops.reset_test_data` (D-118), and the real Matter/CourtCase are created fresh with new UUIDs and a genuine owner approval. Supersedes the pending "adopt 01a03136 vs keep 01a055b0" question entirely. rel: D-115, D-117, D-118, D-125, sql/0054, sql/0066, sql/0067. _Byline: Claude Code · Opus 5 · 2026-09-02._ |

| D-127 | 2026-09-02 | Owner (standing principle) | **A feature flag is never a shortcut to skip building something. It is a shortcut to stop our own development-stage gates from blocking us — and the gated process must still be fully built, still function, and still be tested.** Owner verbatim: "Feature flags are not a shortcut to not do work. They're a shortcut to not get stopped by our own processes that are only meant for development. Those processes still need to function, still need to be developed, still need to be tested. They just don't need to stand in our way." Consequences, binding on every flag in this platform: (1) a flag may relax WHEN or AGAINST WHAT a gate asserts, never WHETHER the gated machinery exists; (2) the strict (flag-off) path must be proven working before the flag is relied upon — an unproven path hidden behind a flag is prohibited, and the proof must be reproducible (see the D-126 pattern: prove strict in a rolled-back transaction, then run flagged); (3) both flag states carry tests; (4) every flag names its removal condition and its go-live runbook step; (0) THE TEST: a development flag's default IS the production behavior, and dev inverts it temporarily - at go-live it flips back to the opposite of how it ran in development, and that inversion is the entire point. It follows that if the honest answer is "we are never going to turn this on", it is not a flag, it is deletion: stop developing it, remove it, and say so. Owner, 2026-09-02: "There's no point creating a feature flag if we're just gonna say fuck it all. If we're gonna say fuck it all, then we just don't develop it and it's no longer a feature and we forget it." The existence of a flag is therefore a standing commitment that the gated capability is real and will be enforced; (5) a flag must never suppress a check into silence — flag-on paths log loudly what is relaxed and why. This reads on D-110 (dev-mode immutability flag), D-125 (dev auth bypass) and D-126 (dev registry identity): each of those gates remains fully implemented and exercised; the flags only move the assertion target for pre-launch work. _Byline: Claude Code · Opus 5 · 2026-09-02._ |

| D-128 | 2026-09-02 | Owner (amends D-110) | **The immutability guards are OWED WORK, not abandoned work — build them, prove they work, and make them switchable off until we want them on. Immutability is not a feature of this platform; it is the property that makes it an EVIDENCE platform at all.** Owner, 2026-09-02: "The entire platform rests around immutability... it's the only thing that makes it viable as an evidence platform." Without enforced immutability there is no chain of custody, nothing is court-defensible, and the product is a database of unverifiable claims. Any future reading that treats the guards as optional, retired, or satisfied-by-documentation is wrong on the product's core premise. The reason they are currently off is narrow and temporary: so development, testing, and repeated purge/re-ingest cycles are not blocked (the 2026-08-24..31 rebuild ordeal is the cautionary case — days lost because a database could not simply be copied). Owner verbatim: "For instance, the immutability guards. We still need to develop them. We need to make sure that they work. They just need to be able to be turned the fuck off until we want them there." This corrects how D-110 has been read. D-110 said the 2026-08-30 rebuild deliberately skipped ~253 guard statements (~50 functions, ~75 triggers), that this "is NOT technical debt", and "do not ask about this again" — which has been treated as *the guards are gone and the subject is closed*. The accurate reading, per D-127: what was closed is the demand to have them ON during pre-launch development; what remains OPEN is the obligation to develop and test them. Binding consequences: (1) the append-only / immutability guard set is a real deliverable with a build item, not a permanently retired idea; (2) it must be exercised by tests in BOTH states (enabled: writes to guarded relations are rejected; disabled: development proceeds unblocked) — an untested guard hidden behind an off switch violates D-127; (3) the switch is the existing dev feature flag (`sql/0031_dev_mode_immutability_gate.sql` lineage + D-125's flag pattern), and its removal condition is go-live promotion of real evidence; (4) nobody is to report the guards' current OFF state as a defect (D-110 stands on that point), but nobody is to report the guards as *done* either. Prerequisite for enabling remains unchanged from D-110: replaying the procedural half of migrations 0035-0054. rel: D-110, D-127, D-031/sql-0031, sql/0017 (superseded guard set), ADR-0045. _Byline: Claude Code · Opus 5 · 2026-09-02._ |
| D-129 | 2026-09-02 | Owner | **The Workbench frontend stack is a named standard, and Evidence.dev is part of it. Recorded because it was nowhere: the stack existed only in a `package.json` and a doc filed under `CLAIMED_COMPLETE_LIKELY_LIES`, so every session had to rediscover it by archaeology.** Owner, 2026-09-02: "it's obviously not committed anywhere as a fucking standard as the way to go... I meant data grids." AS INSTALLED AND VERIFIED in `modules/workbench/web/package.json`: React 19.2.3 - Vite (build `tsc --noEmit && vite build`) - **TanStack Router** ^1.170.32 (code-split routes) - **Storybook** ^10.5.10 with `@storybook/react-vite`, `addon-docs`, `addon-a11y` - Radix UI ^1.4.3 + shadcn ^3.8.4 + Tailwind 4 - lucide-react, cva/clsx/tailwind-merge, sonner, react-dropzone, `@ai-sdk/react`. DECLARED BUT NOT YET INSTALLED: **a Glide-class virtualized data grid** - its only trace is the capability string "Glide-backed document and evidence review" at `platform-ui/surfaces.ts:24`; it is a queued integration owned by a separate lane (per the 2026-08-30 refactor doc, which deliberately did not duplicate it), NOT a loss and NOT a re-decision. **Next.js is SUPERSEDED** by the Vite conversion; residual `next.config.ts`, `next-env.d.ts`, and `out/` in the web tree are drift to clean, not a second build path. TWO SURFACES, unchanged: `src/surfaces/primary/` = the Evidence Operations Desk; `src/surfaces/advanced/` = the Modular Service Cockpit; shared contracts in `src/platform-ui/`. **Storybook is the component workshop and visual contract only - never a data, workflow, or authority layer.** ON EVIDENCE.DEV vs THE GRID - THEY DO NOT CONFLICT AND ARE NOT ALTERNATIVES: the data grid is a live read/write React component inside the SPA for operating on data (sort, review, HITL metadata correction); Evidence.dev is a separate SQL+markdown static-site build, iframed at `/x/reports/`, with no live database connection at view time, for reporting on data. They intersect only in reading the same `analysis` schema / lakehouse through `pg_duckdb`. **Evidence.dev is retained specifically because nothing else in the stack can emit a frozen, citable artifact** - an exhibit handed to an expert or attached to a filing must render identically months later, which a live SPA cannot promise. That is an admissibility property, not a convenience. HISTORY, so this is never re-litigated as neglect: Evidence.dev was added here (`21262cc`), extended (`28e2102`, `667bd24`, PR #15), then its ONLY project was moved out by owner order on 2026-08-25 (`557294c` - "visit-locations moved out to Projects/traceIQ/traceiq-rebuild... never belonged in Agno"), and `analytics/` became gitignored (`.gitignore:97`). The decision to use Evidence.dev was never reversed; what lapsed is that **no platform-owned Evidence project was re-established after its tenant left**. Re-establishing one is owed work, not a new proposal. Two documents asserting the stale state are corrected in this same change: `docs/REPO_STRUCTURE.md` (described `analytics/` as live) and `docs/planning/gui-integration-spec.md` ("already running"). rel: D-123 (SBV desktop ingest workspace is a separate lane; this is browser-first), ADR-0033. _Byline: Claude Code · Opus 5 · 2026-09-02._ |
| D-130 | 2026-09-02 | Owner | **Every unit of work must be written so it can be handed to exactly one Temporal Activity, or wrapped as an n8n node that itself runs as one - and it must not know which caller it got.** Owner verbatim: "Everything needs to be modular so that it can be assigned to Temporal activities. We can't be conflating or mixing a bunch of processes into one. Yes, the engine can call individual ones, but it's going to be calling the Activity more likely than 99.9% of the time," and "Or to be added into an n8n node which gets run as an activity, however that shape looks." Binding rules, now written into every applicable `AGENTS.md` (root, `modules/engine/`, `server/` and its subsystems, `modules/workbench/web/`): (1) one unit does one thing - a parser parses and does nothing more (owner 2026-08-29), a chunker chunks, a hasher hashes; a function doing two must be split before it is wired to anything; (2) custody hashing is its own Activity family and never folds into parsing, chunking, or normalization (D-077); (3) the Activity is the normal caller, so signatures take bounded inputs, return bounded outputs, hold no ambient state, do no hidden I/O, are deterministic given inputs, and are safely retryable - anything that breaks on a second identical call is a defect; (4) three call shapes, one unit: direct in-process, as a Temporal Activity, or wrapped as an n8n node executed as/within an Activity - n8n owns visual flow, Temporal owns durability, the unit owns one job, and a unit that must know its caller contains a boundary violation; (5) references travel, payloads do not - sources move by locator (`upload://`, `r2://`, sealed `file://`), never through Temporal history, an n8n payload, or a PostgreSQL activity request; (6) no orchestration inside a unit - sequencing, fan-out, retries, and human gates belong to the workflow and to n8n; (7) new capability means a new Activity registered in the stage graph, never widening an existing one for convenience. The standing test: could this be scheduled alone, retried, wrapped as an n8n node, and reasoned about in isolation? Also creates `modules/engine/AGENTS.md`, which did not exist - the directory most governed by this rule had no agent instructions file at all. rel: D-077, ADR-0061, the 2026-08-25 boundary ruling. _Byline: Claude Code / Opus 5 / 2026-09-02._ |
| D-131 | 2026-09-02 | Owner | **A DONOR is not a FORK, and SBV is a donor. Name donor-derived code for what it does now; keep the provenance in-file.** Owner, 2026-09-02: "SBV has moved so far away from its source, it's not even really a fork anymore. It's an extracted source for code. We've taken the guts of it and the schema and the extraction and utilized it for our SMS parser, and then we're borrowing the front end for a client... maintaining the SBV naming I think confuses its real new scope." THE DISTINCTION, now canon: a **fork** tracks upstream, rebases, and may contribute back, so it KEEPS the upstream name; a **donor** has had its guts extracted and permanently diverged with no rebase path, so it is NAMED FOR WHAT IT IS NOW with provenance credited in-file. ("Donor" is already this repo's vocabulary - ADR-0034 credits the dial-stack that way.) SBV was mis-filed as a fork under `modules/forks/`. It is now two things and must be named as two: (1) the multi-format **decoder library** - schema, extraction, and 13 format decoders (smsbackuprestore_xml, ndjson, csv, messages_transcript, imessage_txt, imessage_html, facebook_messenger_html, google_voice_html, email_eml, email_mbox, google_chat_json, facebook_messenger_json, chatgpt_official_json) - destined for `modules/engine/decode/`, matching the engine's existing short functional package names (`parser/`, `chunk/`, `normalize/`, `acquisition/`); and (2) the borrowed **Vite frontend**, which goes with the D-123 desktop ingest client on its own lifecycle. MECHANISM: absorb as a **subtree**, not a submodule - a submodule remains a separate repo pinned by SHA requiring recursive clone, which is the opposite of the owner's stated intent ("absorb it and just have it as part of the mono repo"); `UPSTREAM.md` records it was originally a subtree before the 2026-09-01 restructure turned it into a gitignored nested repo. This drops `replace github.com/lowcarbdev/sbv => ../forks/sbv`, ends the gitignored-nested-repo state, retires the separate `sbv-forensic` CI/digest contract, and removes "SBV" as a name meaning three different things. ATTRIBUTION: the donor is **lowcarbdev** (MIT, Copyright (c) 2025), not "danzek" as `AGENTS.md:145` currently states - that misattribution is to be corrected. `UPSTREAM.md` (upstream repo, license, fork remote, version pulled, and a precise per-file "what we changed") survives verbatim as the provenance record. LICENSING: the project is personal and unpublished, so MIT's notice requirement - which attaches to distribution - is not currently triggered; revisit only if publishing is ever scheduled (owner: "at the time that we decide to publish or go public, which is not anywhere on my timeline at all, we can reconsider"). Accurate provenance is retained anyway as a credibility asset: parser origin is a fair question if evidence from this toolchain is ever challenged. _Byline: Claude Code / Opus 5 / 2026-09-02._ |
| D-132 | 2026-09-02 | Owner | **Build the Go tool gateway with its own tsnet identity. Do NOT stand up a second platform-tools as an interim.** Root cause it fixes (found live 2026-09-02): `assess_source_repair_activity` hands platform-tools a worker-local filesystem path, but the worker runs on ovh-files and platform-tools runs on ovh-app, where that volume does not exist - the 404's body is literally the path. `repair.detect` IS registered (platform-tools exposes 39 tools); the defect is a host-local path crossing a host boundary, the same class as the acquisition seam. Two options were weighed: (A) run a second platform-tools on ovh-files with the volume mounted and repoint `PLATFORM_TOOLS_BASE_URL` - config-only, unblocks the same evening; (B) a Go gateway on tsnet. **Owner ruled (B) and rejected (A):** "Otherwise what will end up happening is what we joke around about when we're working on houses about things that get done temporarily. It becomes temporary-permanent." Standing consequence beyond this decision: an expedient that would survive its own justification is not to be built merely because it is faster today. THE GATEWAY: a Go service with its own Tailscale (tsnet) identity - per the owner's standing 2026-09-02 instruction to give Go services their own service IP - that indexes the platform-tools registry, accepts **locators** (`upload://`, `r2://`) rather than host paths, resolves bytes through the existing `acquisition.NewSchemeRouter` (one resolver implementation platform-wide, not a second copy), materializes them locally, calls the Python tool with a path that genuinely exists, and cleans up. The Python tools keep their one-job `{"path": ...}` contract unchanged, satisfying D-130 rule 1. Each gateway call is a bounded, retryable atomic unit - schedulable as a Temporal Activity or wrappable as an n8n node per D-130 rule 4 - and the tsnet identity makes which host a service lands on permanently irrelevant. rel: D-130, D-123, `docs/reviews/2026-09-02-uiw-rehearsal-acquisition-seam.md`. _Byline: Claude Code / Opus 5 / 2026-09-02._ |
| D-133 | 2026-09-02 | Owner | **Authentik integrates as an OIDC provider, NOT as a Traefik forward-auth proxy. Traefik is only for genuinely external-facing surfaces; everything else just gets Tailscale addresses.** Owner, 2026-09-02: "Traefik is only for external-facing surfaces, unless you need to use it for the auth process," then "OIDC is the way to go... otherwise it just gets tailscale addresses." WHAT THIS SUPERSEDES: `deploy/authentik.yaml` (committed 2026-08-29, `0f53e3a` "feat(auth): gate Workbench through Authentik and Traefik") encodes the rejected architecture end to end and must be reworked, not deployed as written - it requires `AUTHENTIK_LISTEN__TRUSTED_PROXY_CIDRS` with an exact Traefik CIDR (hard-fails without Traefik), carries Traefik router labels for `auth.int.mitechconsult.com` with a letsencrypt cert resolver, and defines an outpost router on `workbench.int.mitechconsult.com` + `/outpost.goauthentik.io/`, which is specifically the single-application forward-auth pattern. THE RULED SHAPE: Authentik runs on the tailnet and is reached at a tailnet address (optionally its own tsnet identity, consistent with D-132); each service is an OIDC client that validates JWTs in-process against Authentik's JWKS endpoint; browser surfaces (the Workbench) run a standard authorization-code flow. No reverse proxy, no outpost, no letsencrypt, and no public hostnames are required for internal services, because a tailnet address plus in-process token validation already provides both reachability and identity. Traefik remains legitimate ONLY where a surface is actually internet-facing. STATUS AND CONSEQUENCE: Authentik has never been deployed - no Coolify application exists for it (33 apps, none named authentik/keycloak/traefik) and no container runs on ovh-files or ovh-app; the compose file is written-but-never-deployed work, the same failure pattern as the Evidence.dev lane (D-129) and the DuckDB ELT lane. Because the authorization system is NOT up, **D-125's removal condition for `PLATFORM_DEV_AUTH_BYPASS` is nowhere near met** - the flag remains load-bearing and every service still depends on tailnet-IP checks plus bearer tokens. Nobody is to report the dev bypass as nearly retired until OIDC issuance and in-process validation are live and proven. rel: D-125, D-127, D-132, `deploy/authentik.yaml`. _Byline: Claude Code / Opus 5 / 2026-09-02._ |
| D-134 | 2026-09-02 | Owner | **Every service gets its own Tailscale identity. For Go applications and modules, evaluate a dedicated Tailscale Service even for a submodule - because beyond serving HTTPS endpoints it serves a Go application, and that deserves to be a first-class network citizen.** Owner, 2026-09-02: "Creating IP addresses and tailscale addresses for each fucking service," and "if it's a Go based application or module then we need to consider whether or not, even as a submodule, it would benefit from having its own service - because aside from it serving HTTPS endpoints, it will serve a Go application." THE PATTERN ALREADY EXISTS AND IS PROVEN: `tailscale serve status` on ovh-app shows `https://workbench.tilapia-skilift.ts.net (tailnet only) (svc:workbench)` proxying to a loopback-only host port. That is why the Workbench's `127.0.0.1:18080:8020` mapping is CORRECT and not a defect - an earlier reading of it as a bind bug was wrong, and the 404 seen on the Coolify sslip.io FQDN was caused by using the proxy hostname instead of the Service FQDN. CONSEQUENCES: (1) internal services are addressed by their Tailscale Service FQDN or their own tailnet address - NOT by Coolify proxy hostnames, which are for genuinely external surfaces only (D-133); (2) Go services embed tsnet and advertise via `ListenService("svc:<name>", ServiceModeHTTP{HTTPS:true, Port:443})` rather than depending on the host to proxy a loopback port - this decouples a service's identity from whichever host it lands on, which is precisely what made the cross-host path defect (D-132) possible; (3) Tailscale Services require a TAG-BASED identity, so each service's auth key must carry a tag (the tailnet's existing nodes use `tag:docker`); (4) per-service identity also gives each service its own ACL surface, so access control stops being all-or-nothing per host. The tool gateway (D-132) is the first service built this way and is the reference implementation: `modules/engine/cmd/tool-gateway/main.go`. Existing services migrate off the host-proxy pattern over time; no big-bang conversion is required. rel: D-132, D-133. _Byline: Claude Code / Opus 5 / 2026-09-02._ |
| D-135 | 2026-09-02 | Owner | **CONTENT is immutable; METADATA is ASSERTED BY THE CUSTODIAN - these are different invariants, and conflating them is the defect. Preview must IDENTIFY, PARSE, and DERIVE, then ask the human only to FILL THE GAPS.** Owner, 2026-09-02, stating the custody argument directly: "I am the evidence, the chain of evidence. I acquired it, I'm presenting it, I'm filing the affidavit against it. I don't want to modify the actual message content. But I do need to fill in the gaps." THE PRINCIPLE: the operator IS the custodian and the affiant. A custodian asserting acquisition context is not contaminating evidence - **that assertion is the affidavit**, and it is what makes the material admissible in the first place. What must never change is the message content. A restriction that blocks metadata completion in the name of immutability is protecting the wrong thing: it leaves content unprotected-by-implication while forbidding the very sworn assertion the record requires. THREE TIERS OF METADATA, which drive the form: (1) **DERIVED - first party.** Owner: "if it's first party, it has almost all of the data... the timestamps, that's half the fucking data right there." Date range from min/max timestamps, participants, record and thread counts, source application and export version, device hints from export headers. The system must ATTEMPT all of this and pre-fill it. (2) **ACQUISITION-ADDED - third party.** Material obtained from someone else carries additional metadata that only the acquisition event supplies: from whom, when, by what means, under what authority. This aligns with ADR-0059, which already separates `occurred_at` (first-party event time) from `source_available_from` (custody-backed acquisition time for acquired third-party sources) - the form follows the schema that already exists. (3) **UN-INFERRABLE.** Owner: "there's some stuff it just can't infer." These are the only fields a human should be typing, and they are exactly what the affidavit is for. AS-BUILT DEFECT (verified 2026-09-02): `modules/workbench/api/app/service/source_inspection.py:84` sets `declared_format = _DECLARED_FORMATS.get(extension, 'unknown_binary')` - format identity comes from the FILE EXTENSION alone. No content sniffing, no parse, no derivation anywhere in preview; the UI states its basis plainly as "Filename extension." So preview is a hash plus a filename guess and structurally cannot answer the three questions it exists to answer: did it parse, did it read correctly, was the metadata applied correctly. REQUIRED SHAPE: identify by content with the extension as a weak hint only; parse a bounded sample through the real selected parser so the operator sees actual decoded records; derive every field the data already carries; pre-fill the form and mark each field derived-vs-asserted; ask the human only for what could not be derived or looks wrong. Every field keeps its provenance so a later reader can tell what the data said from what the custodian swore. RECONCILING WITH IMMUTABILITY (D-128): preview-stage entry is not a custody write. Assertions gathered in preview are PROPOSED context; intake seals them into the governed acquisition and decision receipts. Editing during preview is therefore correct and does not weaken D-128 - what must never happen is a durable custody record written outside the governed path, or any edit to content. The current UI copy "Previewing is read-only. Intake creates the governed acquisition and decision receipts." is misleading on both counts: the assertion fields ARE editable (they sit on the `metadata` tab and nothing disables them), and the sentence should say preview edits become durable at intake - not that preview is read-only. rel: D-123, D-128, D-130, ADR-0059. _Byline: Claude Code / Opus 5 / 2026-09-02._ |
| D-136 | 2026-09-02 | Owner (simplifies D-135) | **THE WHOLE RULE, in the owner's words: "Ensure that every last bit of data is extracted. Don't modify the messages and the timestamps. Let me do what the fuck else I want. That's all it is."** Owner, 2026-09-02, explicitly rejecting accumulated over-restriction: "I'm way overthinking the restrictions on this thing." This is the governing statement; where D-135's elaboration conflicts with it or adds ceremony beyond it, THIS wins. Three clauses, nothing more: (1) **EXTRACT EVERYTHING** - extraction is maximal, not conservative. A parser that silently drops records it could have kept is a defect (see the 516 attachment-only MMS records dropped by an early parser whose test asserted the bug). If the data is in the source, get it out. (2) **NEVER MODIFY MESSAGE CONTENT OR TIMESTAMPS** - these two are the immutable core. Everything custody protects reduces to this. (3) **EVERYTHING ELSE IS OPERATOR DISCRETION** - the custodian may add, correct, and complete any other field at any stage without asking the system's permission, because the custodian is the affiant (D-135). CONSEQUENCES FOR BUILD: do not add permission gates, workflow-stage locks, or read-only modes on metadata fields; do not require justification to edit a non-content field; do not design ceremony whose only purpose is to feel rigorous. Derived-vs-asserted provenance marking remains USEFUL and should be kept where cheap, but it is a convenience for later readers, not a gate on the operator. The one thing to enforce mechanically is clause 2. rel: D-135, D-128 (immutability now precisely scoped to content and timestamps), D-123. _Byline: Claude Code / Opus 5 / 2026-09-02._ |
| D-137 | 2026-09-03 | Owner | **`propria` is the UMBRELLA product name.** Owner, 2026-09-03: "The propria. For the umbrella." (Naming thread of 2026-09-02: from *in propria persona* - the self-represented litigant speaking in their own person; see `README.md` mission section.) NOT RULED, explicitly open: the name of THIS repository (the evidence-record product: ingest, custody, normalize, analysis). Owner rejected `openspine` ("absolutely not") and rejected reusing `propria` for it - the umbrella and this repo get DIFFERENT names. Also still open: the legal-side product name (`openbrief` proposed), and the desktop ingest client name (`intake` proposed). Owner is taking the remaining naming discussion to a forked session. Until those slots are ruled: no rename executes (repo `mcp-platform-agno-mvp`, Go module path, parent gitlink, Coolify remotes, path-keyed memory dir all wait). Component naming rule (lowercase functional: `engine`, `workbench`, `uiw`, `parser-runtime`, `decode`) and donor/fork distinction stand as proposed under D-131. rel: D-131, `README.md`, `docs/reviews/2026-09-02-ingest-day-board.md`. _Byline: Claude Code / Opus 5 / 2026-09-03._ |
| D-138 | 2026-09-05 | Owner | **Product naming canon under the `propria` umbrella (D-137), ruled 2026-09-05 ("That's all fine").** PRODUCTS: (1) **Indicia Probata** - the main application: ingest, sorting, custody, normalize; THIS repository. Latin "proven signs" (neuter plural noun + agreeing participle); short form / slug / Go module / Coolify prefix = **`probata`** (e.g. `github.com/Cursedpotential/probata/engine`, `probata-*` apps and tsnet services). (2) **vestigia** - the Vault and Case Bible system ("the traces left behind"). Product name only: the `casebible-*` R2 buckets, the `casebible` PG schema, the catalog skill and the `cb-*` commands KEEP their identifiers; renaming those is a separate decision, not implied here. (3) **advocatio** - the legal workbench (`Legal-Workspace`), twist on the rejected `advocatus`. (4) **traceIQ** (geo) unchanged. (5) The analysis engine SPLITS OFF as its own product with its own Go front end, its own tsnet identity (D-134), and its own name - name NOT yet ruled; `indagatio` ("the tracking-down", Latin root of investigate) is the standing proposal, collision-clear. REJECTED WITH REASON (collision-checked 2026-09-05): `openspine` (owner: grotesque); `indicia` alone (`indicia.app`, live OSINT forensics platform - same field); `indica` (cannabis connotation); `indicium` (Thinkwise app tier, INDICIUM DM suite, Indicium AI); `tessera` (Tessera Data, criminal-records vendor); `mosaic` (Mosaic Legal Ops); `advocatus` (Advocatus Digital, AdvocatusMobile, advocatus.ro); `indicata` (Italian analytics firm); `horizon`, `antecedent`, `priora`, `testimonium`, `palimpsest`, `sequela`, `ordo`, `firsthand`, `verbatim` (owner: did not like). COMPONENT RULE (stands from the 2026-09-02 thread): components get lowercase functional names, never brands - `engine`, `workbench`, `parser-runtime`, `tool-gateway`, `decode` (D-131), `intake` (the D-123 desktop ingest client). Still open from that thread: `uiw`/`universal-import`/`ingest` -> one word in both Go and Python (`import` proposed); the `agentos`/`agno`/`platform-api` Coolify names; retirement of `graphiti*`, `phase1-surreal*`, `unified-operator-surface` compose files. EXECUTION is NOT authorized by this entry: rename of the GitHub repo, Go module path (97 imports + vendor), parent gitlink, Coolify apps, and the path-keyed memory dir happens under its own plan after the ingest-simplification plan is ratified, or when the owner says go. rel: D-131, D-134, D-137, `README.md` mission. _Byline: Claude Code / Fable 5.1 / 2026-09-05._ |
| D-139 | 2026-09-05 | Owner | **The analysis engine is named `indagatio`** ("the tracking-down"; Latin root of *investigate*; collision-clear 2026-09-05 - only a Portuguese clothing brand). Owner: "that's good to go... if there's something we can use to expand upon the full name, let me know. Otherwise, roll with it." Short form / slug / Go module / tsnet service = `indagatio`. PROPOSED full two-word form, parallel to *Indicia Probata*: **Indagatio Veri** - "the tracking-down of the truth". Cicero, *De Officiis* I.13, uses the phrase *propria veri inquisitio atque investigatio* - "the search and investigation of truth is proper to man" - which ties the umbrella (`propria`) and this product in one attested sentence. The full form is offered, not ruled; the short form stands either way. SCOPE (D-138 item 5): the analysis engine - horizon walks, ignorant/hindsight agents, the delta, SurrealDB as its store (D-073/D-080) - splits out of `probata` as its own product with its own Go front end and its own tsnet identity (D-134). What moves, what stays, and the boundary contract between `probata` (evidence record) and `indagatio` (analysis over it) are for a separate plan; nothing splits under this entry. rel: D-134, D-137, D-138, D-073, D-080. _Byline: Claude Code / Fable 5.1 / 2026-09-05._ |
| D-140 | 2026-09-05 | Owner | **Two rulings that amend D-138.** (1) **The import lane is named `proffer`** (owner: "I like proffer"). Legal meaning: to offer evidence to the court for consideration - presented, not yet accepted - which is exactly what ingest does before promotion. It replaces the three names for one lane: `uiw` / `universal-import` (Go: `uiw`, `uiwworker`, `universal-import-worker`, `universal-import-starter`, task queue `universal-import-v1`) and `ingest` (Python `server/ingest/`). Targets: package `proffer`, binaries `proffer-worker` / `proffer-starter`, queue `proffer-v1`, Python `server/proffer/`. Companion terms proposed, not ruled: `intake` for the D-123 operator client (unchanged from D-138), `admit` for the promotion-to-`evidence.*` activity family (evidence is *admitted* into the record). (2) **`vestigia` moves to the GEO product** ("footprints, tracks" - the literal geo word), replacing `traceIQ` as the product name. traceIQ's repo/identifiers are not renamed by this entry. CONSEQUENCE: the Vault / Case Bible product name ruled in D-138 is VACATED and reopened. Candidates under collision check 2026-09-05: `tabularium` DEAD (Tabularium AI - official records/deeds platform; plus a PDF evidence-numbering tool and five others); `custodia` DEAD (Custodia Technology - regulated data capture with WORM/integrity archiving, same space); `scrinium` (Roman document chest) - fintech, a reading tracker, a text tool; none legal; `depositum` (legal: a thing entrusted for safekeeping) - no hits; `arca`, `armarium` - pending. rel: D-123, D-134, D-138, D-139. _Byline: Claude Code / Fable 5.1 / 2026-09-05._ |
| D-141 | 2026-09-05 | Owner | **The Vault / Case Bible product is named `consignatio`** (owner: "perfect"). Latin: the affixing of a seal; an attested document; written proof - the custody guarantee itself, not merely a storeroom. Collision-clear 2026-09-05 (dictionary entries only). Rejected for this slot with reason: `archa` (homophone of `arca.legal`, a funded legal-AI platform - unsafe when spoken aloud in court), `cella` (three unrelated companies; reads as "storeroom"), `horreum` (Hyperfoil's results-repository service + Horreum Apps), `reconditorium` (UK security firm), `tabularium`, `custodia`, `arca`, `armarium` (D-140), `thesaurus`/`firmamentum` (modern meanings dominate), `crypta` (tone), `depositum`/`scrinium` (clean, not chosen). Identifiers are NOT renamed by this entry: `casebible-*` R2 buckets, the `casebible` PG schema, the catalog skill and `cb-*` commands keep their names until a separate decision (same reservation as D-138 item 2). **The naming canon is now complete at the product tier:** `propria` (umbrella, D-137) · **Indicia Probata** / `probata` (evidence record - this repo, D-138) · **Indagatio Veri** (proposed full form) / `indagatio` (analysis engine, D-139) · `consignatio` (vault, this entry) · `advocatio` (legal workbench, D-138) · `vestigia` (geo, D-140). Component verbs: `proffer` (ingest lane, D-140), `intake` (operator client, proposed), `admit` (promotion, proposed). The chain reads: deposited into consignatio, proffered through probata, admitted into evidence, investigated by indagatio, argued from advocatio, located by vestigia. rel: D-137, D-138, D-139, D-140. _Byline: Claude Code / Fable 5.1 / 2026-09-05._ |
| D-142 | 2026-09-05 | Owner | **ZERO committed live evidence exists; stop protecting empty stocks. Teardown + golden re-clone replaces purge/migrate. Memory stores alias on rename; canon replaces.** Owner, 2026-09-05 (late): "We have zero committed and live evidence... All we have is reference materials, training materials, tables of examples, definitions," and: "trying to preserve shit that isn't actually there is what drove two weeks worth of bullshit with the databases... you need to remember that." RULINGS: (1) **State of the platform:** everything under `working.*`, `evidence.*`, `context.*`, the retained-original volumes on the VPS, Temporal history, and the platform's Weaviate/Milvus collections is REHEARSAL FIXTURE data. The precious set is exactly: `reference.*`, `analysis.human_label*`, the Case Bible catalog, and human-curated docs. Sharpens the 2026-08-02 disposable-data ruling. (2) **Design discipline:** no plan, pre-mortem, migration step, or "keep this path for the data" caveat may be written to protect a stock that is empty. First question is always "does that data exist yet?" Consequence for the rename: host volume paths (`/data/agno/volumes/universal-import/...`), DB object names, Temporal queue/workflow-type names, vector collections and container names are renamed NOW, cheaply, and rehearsal workflows are terminated; after first real ingest these become custody migrations. (3) **Golden clone + teardown** (owner proposal, accepted): maintain a template database holding schema + `reference.*` + labels; re-create the working database FROM the template instead of purging rows. Hand-off: the owner is assigning this to the ingest session that has a full ingest test on hold (hundreds of errors) awaiting the rename. (4) **Feature flags everywhere** (owner: "feature flag, feature flag, feature flag everywhere"): any cutover that must coexist for a window (dual queue names, dual hostnames, gate on/off) goes behind a D-127-style flag rather than a hard switch. (5) **Memory tier aliases; canon replaces.** Recall stores - the path-keyed Claude memory dir, `.remember/`, memsearch journals, `.cnf/memories/project_memory.json` (shared with Codex), the knowledge graph, and every `AGENT_MEMORY.md` router - get the new name APPENDED beside the old ("probata (formerly Agno-MCP-Platform)") so either term resolves; never rewritten. Canon/truth docs (README, AGENTS.md, PROJECT_CANON, INDEX, registers) get strike-through + the new name. (6) **Hierarchy scope:** every `AGENTS.md` / `AGENT_MEMORY.md` / `CLAUDE.md` router and index from this repo up through `E:\AI_Workspace\Projects\the-platform-workspace`, `E:\AI_Workspace\Projects` (incl. `REPOSITORY_BOUNDARIES.md`), and the `E:\AI_Workspace` root is in scope, committed from the parent repo root with an explicit path allowlist. Recorded the same night in: auto-memory `ingested-data-disposable-until-design-settles.md`, `docs/registers/SETTLED.md`, `.cnf/memories/project_memory.json`, `.memsearch/memory/2026-09-05.md`. rel: D-127, D-137..D-141, the 2026-08-02 wipe ruling. _Byline: Claude Code / Fable 5.1 / 2026-09-05._ |
| D-143 | 2026-09-06 | Owner | **LlamaIndex and LangGraph are IN. FINAL. This row exists so the question is never reopened.** Owner, 2026-09-03 18:1x: "NO, LLAMAINDEX IS PART OF THIS" (Fable misread it as "no LlamaIndex" and struck it for four hours; owner 2026-09-06: "no less than 4 hours… crossing that shit… less than 15 times"). Owner, same day: "n8n is based on LangGraph, so why wouldn't we use it?" ROLES, fixed: **LlamaIndex = the retrieval/indexing library** — property-graph and hierarchical retrieval over Neo4j `sat-temporal` + Weaviate; node relationships (SOURCE/PARENT/CHILD/PREV/NEXT) built FROM the PostgreSQL-authored provenance (`working.content_chunk_message`, D-116/Q9); it is a rebuildable projection like Weaviate, NEVER a second chunker and NEVER a writer of truth. **LangGraph = the deterministic retrieval state machine** — query parse → parallel PG/Weaviate/Neo4j → timeline verify → independent verification that can disagree → cite by PG source coordinate → scope report; runs as Temporal Activities and inside n8n agent nodes (LangChain-based); it does NOT orchestrate ingest (Temporal + n8n do, D-130). SUPERSESSION: ADR-0041's "LangGraph only reconsidered if a control-flow need arises" and "orchestration settled Agno-native" are superseded — the Agno-native premise was retired by D-107, and the owner has ruled both frameworks in; ADR-0041's "LlamaIndex permitted as a library inside tools" stands and is now a dependency, not a permission. TIMING: retrieval is built AFTER ingest runs end to end (owner Q17, 2026-09-06: "one step at a time"); this decision fixes the frameworks so that later work does not re-litigate them. rel: D-093, D-107, D-116, D-130, ADR-0041, ADR-0053, the owner's SAT-Graph guide (2026-08-26, a guide not a bible). _Byline: Claude Code · Fable 5.1 · 2026-09-06._ |
| D-144 | 2026-09-06 | Owner (amends D-143) | **LlamaIndex and LangGraph are NOT retrieval-only: they also serve the ingest-side STITCHING, the SAT-lane EXTRACTION, and the assembly that feeds SurrealDB at promotion.** Owner, 2026-09-06: "LangGraph or one of the LlamaIndex tools is also going to be critical to properly stitching all of the data together, extracting all of the data, and properly getting it to Surreal. Once it's in Surreal, Surreal is its own beast and will handle it there. We're not there yet." ROLES, amended: **LangGraph** = any stateful, branching, loop-and-verify step — conversation stitching across mixed file types (rules → `registry.id_xref` as-of-date entity resolution → judgment → human gate), extraction verification loops, the promotion-time assembly from PG + graph lanes into Surreal, and retrieval — always executed AS a Temporal Activity (D-130), always writing results only through the PostgreSQL contract (conversation-group rows, extraction candidates, promotion candidates). **LlamaIndex** = indexing/retrieval AND the SAT-lane extractor (property-graph extractors producing Item/Version/Action/TextUnit nodes and edges keyed to the PG source coordinate) — which makes the D-093 two-lane comparison concrete: Semantica's extractor vs our LlamaIndex-built SAT extractor, never fused. **The line that does not move:** neither tool ever writes `raw.*`, the normalized hub, `working.content_chunk`, or `working.content_chunk_message`; Go, DuckDB ELT, and Temporal write truth; these tools consume truth and produce groupings, extractions, indexes, and answers. **SurrealDB** owns analysis after promotion (D-073/D-080/D-107) — nothing here reaches into it. TIMING unchanged: designed now so the shape holds; built after ingest runs end to end (Q17). rel: D-143, D-093, D-107, D-116, D-130, ADR-0041, the SAT-Graph guide. _Byline: Claude Code · Fable 5.1 · 2026-09-06._ |
| D-145 | 2026-09-06 | Owner | **THE LIFECYCLE ORDER, FINAL — analysis in SurrealDB comes BEFORE evidence promotion, not after.** Owner, 2026-09-06 (verbatim): "When things get to context, I'm gonna start analyzing, looking and reading. It's probably gonna go into Surreal from that point. And I'm going to analyze it some more, and when I decide that it's important, that it's relevant, at that point we can package it and promote it to evidence." And: "This is again a conversation we have had more times than I'd like to count." THE ORDER: (1) ingest → `raw.*` → normalize → **context / working** (PG truth); (2) the OWNER reads and analyzes; (3) on the owner's decision, material goes INTO **SurrealDB** for deeper analysis — walks, deltas, the horizon mechanism (D-073/D-080) — this is the "manual promotion" D-107 governs: promotion INTO SURREAL FOR ANALYSIS, an owner click, never automatic; (4) after analysis, when the owner decides something is important and relevant, THEN it is **packaged and promoted to evidence** (`evidence.*` mirrors, H1/H2/H3 custody, `working_evidence_link`) — a second, separate owner click. CONSEQUENCES: "promotion" is never one thing — say **"send to Surreal"** or **"promote to evidence"**, never bare "promote"; nothing reaches Surreal or evidence without the owner clicking; the two-lane adjudication (D-144) happens on context material and its verdicts travel INTO Surreal as inputs to analysis, not after evidence; **the plan's Stage 5 ("Promotion to evidence") sits AFTER Surreal analysis, and every place the plan or a desk question implied evidence-before-Surreal or automatic promotion is wrong.** Fable's repeated inversion of this order is the defect this row exists to end. rel: D-073, D-080, D-107, D-069, D-128, D-136, D-142, D-144. _Byline: Claude Code · Fable 5.1 · 2026-09-06._ |
| D-146 | 2026-09-06 | Owner (amends D-145) | **Exception, owner-only: material may be promoted to evidence directly from context, skipping Surreal, when the owner already knows it is evidence — "but that's not the rule."** Owner, 2026-09-06: "Some things may get promoted to evidence aside from Surreal, or beforehand, just because I already know that it's evidence. But that's not the rule." The D-145 order stands as the rule; this is a manual shortcut available at the owner's click only. Nothing in the system may take it on its own, infer it, or default to it. Both paths land in the same `evidence.*` mirrors with the same custody (H1/H2/H3) and the same link-table provenance; the promotion receipt records which path was taken. rel: D-145, D-107, D-069. _Byline: Claude Code · Fable 5.1 · 2026-09-06._ |
| D-147 | 2026-09-06 | Owner | **In the SAT graph, an Action is a HUMAN ACTION asserted or evidenced in the messages — not a record operation.** Owner, 2026-09-06: "Think about human actions. Did they say that they're going to work? But then a later conversation with somebody else shows that they went over to some guy's house for a party. They tell me that they went to the store, but in reality they went and got drunk. These are actions. That's a human action. That's what we're extracting as entities into the graph. It's an action." THEREFORE: the SAT lane's `Action` node = a claimed or evidenced real-world act (went to work, went to a party, went to the store, got drunk), with who did it, when it was said to have happened, who said so, to whom, and in which message (PG source coordinate). The SAME act asserted differently in two conversations (told to the owner one way, shown to a third party another way) is two Action assertions about one event — and their disagreement IS the horizon delta the platform exists to surface (AGENTS.md §WHY). Deletion/edit/unsend of a message is a record event, tracked separately (ADR-0062 claim edges / D-135 metadata), NOT an Action. Extraction of Actions runs on context material (D-144 SAT-lane extractor; Semantica lane in parallel, D-093); disagreements between lanes go to the owner (Q4). rel: D-093, D-144, D-145, ADR-0062, the SAT-Graph guide (Item/Version/Action/TextUnit). _Byline: Claude Code · Fable 5.1 · 2026-09-06._ |
| D-148 | 2026-09-06 | Owner (amends D-147) | **Every Action carries its CONTRADICTION: a "changed action" or "contrary action" is a first-class thing in the SAT graph, linked to the Action it contradicts.** Owner, 2026-09-06: "Did I say I was gonna pay the bills but then bought drugs? Those are actions. With contradictions." "Did you tell me I was gonna be able to come get my daughter, but then she changed her mind because she was with some other guy? Action and a contradiction." "For a changed action or contrary action, whatever that looks like." SHAPE: an Action node (what was said would happen / what was promised / what was claimed) and, when the record shows otherwise, a second Action node (what actually happened, or the later reversal) joined by a **CONTRADICTS** (or CHANGED_TO) edge carrying: the two PG source coordinates, the two timestamps, who asserted each, and who each was told to. Kinds seen so far: promise-then-contrary-act (pay bills → bought drugs), stated-plan-then-different-act (going to work → party; store → got drunk), commitment-then-reversal (you can pick her up → changed mind, other guy present). The contradiction edge is what the ignorant-agent walk experiences as gaslighting and what the hindsight agent sees whole; it is the deliverable (AGENTS.md §WHY). Extraction proposes these on context material; the two lanes may disagree about whether a contradiction exists — that disagreement goes to the owner (Q4). rel: D-147, D-144, D-093, D-145. _Byline: Claude Code · Fable 5.1 · 2026-09-06._ |
| D-149 | 2026-09-06 | Owner | **Storage tiers and derived layers (rulings 11:16–12:14).** (1) Custody sealing/H1-H3 at PROMOTION only; ingest retain = working copy + SHA-256 row (`context.retained_object`, `context.hash_receipt` context_source_fingerprint). Seal code parked at `modules/engine/parked/seal/`, reused by the promote activity. (2) Tiers: **vault** `r2:casebible-sorted` (cold; home of everything already sorted; B2 later, not Fable lane) · **block** `/data/test_data/<source_type>/<export-folder>/` + `.sha256` sidecars (working AND test area) · `nexus` = workbench `upload://` staging only. No test bucket, no second transfer. (3) Block TTL: a working copy is deletable once the run is terminal AND its SHA is validated against the vault; promotion re-reads the original from the vault; sweep is automatic and reports removals. (4) Destination gate (test/vault/discard, `permanent_home` on the source row) fires ONLY for one-offs (upload/drop/test); vault sources skip it. (5) Attachments are EXTRACTED at parse into a subfolder beside the object, named, converted, indexed, own SHA + `source_version_object` role attachment; byte range = provenance only; permanent, deduped by SHA. (6) Raw records: locator + byte range + H2 (already built: `raw_record_identity`, `source_range_locator`); drop the `stored_bytes` branch (Q1=C carried through). (7) Chunks = ids only: drop `working.content_chunk.content` + CHECK, add `working.content_chunk_text` view over `normalized_record`; migrate readers in the same change. (8) Weaviate holds the chunk objects (vector + PG coordinate + member ids), no text; text lives once in PG; PG bridge keeps the id list for rebuild on embedder change; new `EvidenceChunkV2` class. **Confirmed 12:2x by four explicit answers** (what lives in Weaviate = searchable object only; hit returns messages from PG; rebuild from PG id lists; write path = PG row + outbox, never direct). Record: `docs/reviews/2026-09-06-chunks-live-in-weaviate-systems-pass.md`. (9) ~~DuckDB ELT is PRIMARY for extract-only text exports … SBV decoders = logged-failure fallback~~ **CORRECTED 15:04 (owner): routing is NOT priority-based.** The Go router reads the file, matches its fingerprint/signature (Q18 matcher), and selects the ONE parser or extractor registered for that signature per task (coverage-based, ADR-0052 Q3). DuckDB ELT templates and SBV decoders are both registry entries keyed by signature; which one a signature maps to is a registry row, not a ladder. **Registry fill rule (owner 15:05): if DuckDB can process a signature, DuckDB IS its registered handler; a Go decoder is registered only for signatures DuckDB cannot handle.** **Decoder retention (owner 15:06): the Go/SBV decoders are KEPT, built, and tested, but are NOT in the workflow for signatures DuckDB handles; they remain available for atomic direct calls and as a backup if DuckDB fails. Not deleted, not actively used.** Lint/validate (assess_source_repair → HITL → resolve_source_repair) precedes selection so the chosen unit runs without error; malformed input is a lint finding, not a parser crash. The only fallback edge is on a LOGGED FAILURE of the selected unit. `execute_structured_elt_activity` still must be registered on a worker. (10) SBV = decoder library → `modules/engine/decode/` subtree FIRST in Stage 3 (D-131), then in-tree fixes (XML scanner resync, imessage_html depth flush, `messages_transcript` router row); Vite viewer = client on its own lifecycle, ≠ HITL preview. (11) Reading context, sending to Surreal, and Surreal analysis are the platform in use, not build stages. Sources: `docs/reviews/2026-09-06-storage-copies-recall-pass.md`, `…-thinking-pass.md`, `docs/planning/2026-09-06-derived-layers-change-map.md`. Execution in a separate session, order: decode subtree → ELT wiring → 1.3 GB SMS export via DuckDB → migrations 0073–0077. _Byline: Claude Code / Fable 5.1 / 2026-09-06._ |
| D-150 | 2026-09-06 | Owner | **The desktop ingest client is named `intake`** (owner: "commit intake name"). Component tier, lowercase functional (D-138 rule): the D-123 desktop-first ingest workspace built on the SBV donor front end, distinct from the `workbench` (browser operator surface) and from `proffer` (the ingest lane it drives). It is a probata component, not a product. Sequencing with the other lane verbs stands: material is deposited into consignatio, staged through intake, proffered through probata, admitted into evidence. `admit` (promotion activity family) remains proposed, not ruled. rel: D-123, D-131, D-138, D-140. _Byline: Claude Code / Fable 5.1 / 2026-09-06._ |
| D-151 | 2026-09-06 | Owner | **No chunk bake-off; SurrealDB is the analytical surface; the evidence lane is out of scope until ingest is through.** (a) The chunk-window bake-off (ingest plan §9 / desk Q6) is not needed and is struck; ingest proceeds on the provisional window, and re-chunking later is a build task, not a comparison study. (b) SurrealDB is the platform's analytical surface (reaffirms D-073/D-080/D-145): reading context, sending it to Surreal, and analysing there is the platform in use; any doc or extraction that lists "SurrealDB retirement" or a Surreal conflict as open work is stale — only the legacy Agno operational adapter/instance was retired (D-061). (c) Nothing ingested is evidence and evidence-lane work is not in the current discussion; the only lane in scope is ingest through to context (D-142 reaffirmed: zero committed live evidence). (d) Audit method for the docs reconciliation: identify every claim first, audit each against live reality, then resolve conflicts with the newest owner-approved statement winning, and only then compile — no conclusions drawn from a stale document's own status words. rel: D-073, D-080, D-145, D-142, D-061. _Byline: Claude Code · Sonnet 5 · 2026-09-06 (recorded for Fable 5.1 session)._ |
| D-152 | 2026-09-07 | Owner | **Ingest first: get the data in and normalize; no custody hashing until promotion, and promotion is not being built now** (owner 02:57: "we don't even need to worry about placement of the H2 hash at this moment ... without hashing until it's promoted, we're not handling promotion"; 02:58: "just get the database rebuilt without it and let's move on"). Applied the same hour: (a) the DuckDB ELT lane's `content_canon` is a context fingerprint - constant renamed to `eltContextFingerprintCanon` = `context-rawrecord-fingerprint-duckdb-json-v1`, guard test `TestEltCanonIsAContextFingerprintNotCustodyH2`, registered in HASH-TAXONOMY; (b) `raw.*.content_canon` DEFAULT moved to `context-rawrecord-fingerprint-v1` (rebuild script edit step 5); (c) platform rebuilt from `sql/bootstrap/schema_snapshot_20260907.sql` with the keep set (49 COPY blocks; all 26 live tables with rows matched before/after: labels 1,918/1,918, media.enrichment 15,252, reference.detection_pattern 527); post-rebuild: 276 tables, 9 proffer_*, 0 uiw objects, 0 FKs leaving reference, agno_app role gone. **Near-miss recorded:** the 09-06 rebuild had never run, and the script's keep-set export (`--schema ... -t ...`) carried only 2 public tables because pg_dump ignores `-n` when `-t` is present; fixed with `-t "schema.*"` patterns and a mandatory `verify-keep` gate. The crew's CR-3/GAP-3 'move H2 into a hash activity' remedy is struck (re-opens D-124/D-149). rel: D-124, D-142, D-149, D-151. _Byline: Claude Code · Fable 5.1 · 2026-09-07._ |
| D-153 | 2026-09-07 | Owner | **No migrations, ever. Reference and state tables are the fixed core; everything else is created in its final form from the snapshot.** (owner 07:24: tables with real reference and state data never change or delete; create the rest in their final form). Executed the same hour: 65 numbered migrations + the parked 0073 + five pre-snapshot bootstrap files retired to `sql/_stale/` (git mv, never deleted); `sql/bootstrap/README.md` rewritten as the doctrine; AGENTS.md / CONVENTIONS / REPO_STRUCTURE / PROJECT_CANON / the cutover plan §0 corrected; the migration-text tests replaced by `tests/test_schema_snapshot_contract.py`; the Go preview-store test and the timeline live test no longer apply DDL; fresh-container initdb mounts (`deploy/compose.yaml`, `deploy/data-pg.yaml`) point at the snapshot. `ops.migration_ledger` / `public.schema_version` stay as state history. Any plan or crew output proposing `sql/NNNN` is a defect. rel: D-142, D-152. _Byline: Claude Code · Fable 5.1 · 2026-09-07._ |
